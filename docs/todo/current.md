# 現況（生きた引き継ぎ）

> **これは「いまの状態」を書く1枚**（現在形・上書き）。その日の記録は `docs/daily/`（過去形・追記しない）。
> **最終更新: 2026-08-26 02:0x〔`date` 実測〕**

## いまどこ

**`nene2-ui` は、載せ替えた艦から出た差分に一通り応えた状態。** vault の実ブラウザ突合から出た分はキット側に着地し（0.17.1 は vault の**本番まで**検収済み）、**影の次元がスロット化された**ことで「スロット化されていない次元」が消えた。
**`nene2-standards` 2.4.0 で測定経路が HTML 埋め込み `<style>` を読む**ようになり、#164（G-6 の現物）は4タスクとも着地して close。

**4パッケージとも main と npm が一致している**〔2026-08-26 01:54 `npm view` 実測〕。

| パッケージ | `main` | npm の latest | `fleet-baseline` の floor | タグ以降の未リリース |
| --- | --- | --- | --- | ---: |
| `nene2-tokens` | 1.3.0 | **1.3.0** | `^1.1.0` | **0件** |
| `nene2-standards` | 2.4.0 | **2.4.0** | `^2.1.1` | **0件** |
| `nene2-i18n` | 0.3.2 | **0.3.2** | `^0.3.0` | **0件** |
| `nene2-ui` | 0.17.1 | **0.17.1** | `^0.14.0` | **0件** |

|                  | 値〔すべて自艦で実測〕 |
| ---------------- | ---------------------- |
| テスト           | **802** / 50ファイル   |
| open な PR       | **0本**（本文書を運ぶ PR を除く） |
| `npm run check`  | 🟢 EXIT=0（8ステップ） |

🔴 **floor が npm より低いのは正しい。** 理由は「採らないと決めたもの」の節。

🔑 **版の一致は「中身が出ている」ことを意味しない。** 未リリース差分は
`git log <pkg>-v<version>..HEAD -- packages/<pkg>` で見る。⇒ この見方で
**最長34日の未 publish が3パッケージで見つかった**（版の欄は4つとも 🟢 だった）。**右端の列が正本。**

## 🔴 いちばん上にあること

0. **#439 は `nene2-ui` 0.17.1 で出て、vault の本番まで検収済み**〔publish は 2026-08-26 01:19 `npm view` 実測・shasum `374a1eac`／
   検収は伝聞: vault ローカル build 01:2x → 本番 0.9.2.1 配備 01:32:50・ariaSnapshot 同一・vault #469 close・原文は #439 のコメント2本〕。
   **Firefox は代替テキスト構文未実装で二重のまま＝退行ではなく現状維持（未測定）。**
   🔴 **訂正**: 前版の「Tailwind の生成結果はキット側で確認できない」は**誤りだった**（tailwindcss 4.3.2 は自艦 `node_modules` に居る）。⇒ 型10。
1. **艦の実インストール版の確認**。hub が全艦通知（`_work/handoff-fleet-2026-08-25-npm-releases-notice.md`・板 L128・due 08-27）を
   出している。とくに `nene2-standards` は **26日ぶんの修正が届いていなかった**ので、**上げると挙動が変わる艦がある**。
   その後に **`nene2-ui` 0.16.1**（Modal の位置・vault #441）と **`nene2-i18n` 0.3.2**（nested lookup のドット付きキー・vault #453）が
   出た〔2026-08-25 21:48 `npm view` 実測〕。**中央復帰と Action 列の表示は vault の live で確認するまで「直った」と書かない。**
2. **`Modal` が4点を prop で持った**（可視ヘッダ＋閉じる / `size` / ボトムシート / スクロール）。
   ⇒ **vault の本番 a11y 欠陥（`aria-modal` を名乗って Esc も focus trap も無い・取り消せない操作を含む
   5画面）の是正が、上流で塞がっていた状態は解消した。** キットは native `<dialog>` ＋ `showModal()` なので
   **focus trap と Esc はブラウザが持つ。**
3. **0.11.0 → 0.14.0 の周知は済んでいる**（`nene2-ui` の消費艦は `nene-vault` の1艦のみ〔実測〕）。
   **0.15.0 / 0.16.0 はどちらも描画が変わらない**ので、周知の緊急度は低い。
4. **`nene2-ui` 0.17.0 は出ている**〔2026-08-25 23:16 `npm view` 実測〕: #390（`className` 4部品・Modal は意図して除外）＋ #422（Badge `success`/`warn`/`info`・パレット 8→14色）
   ＋ #421（Pagination `size`/`stackOnMobile`/`statusPlacement`・「空で非描画」は prop にしない）＋ #424（DetailList `layout`）＋ #423（DataTable `collapse`）＋ #428（版節「各艦でやること」）。
   🟢 **vault の live 実測は返った**（08-25 23:5x・伝聞・原文は #439）: #431 の縦積みは破綻なし。**#423 は懸念どおり読み上げが二重**
   （`::before` のラベルがセル名に入り、sr-only の見出し行も残る）。表示・機能は正常。
   `Card` の `as`（#394）と部品→スロット配布（#393・hub の台帳粒度待ち）は**入っていない**。
   ⚠️ **積み上げ PR は squash と組み合わせない**（base 枝の削除で GitHub が後続 PR を自動 CLOSE・reopen 不可。日報 08-25-3）。次は1本ずつ main 直下で。
5. **`nene2-standards` 2.4.0 が出ている**〔2026-08-26 01:54 `npm view` 実測・shasum `a63bb8dd`〕: #164 タスク2（測定経路に HTML 埋め込み `<style>`・postcss-html）。
   **上げると変わる艦** = 台帳に `.html` を legacy-manifest 登録している艦（scan-coverage が unknown → green・その HTML の (rule,index.html) が lint-baseline に現れる）。
   実艦で該当しうる concierge は **registries.jsonc 未導入**〔実測〕なので、いま壊れる艦は無い。
   実艦陽性対照: concierge `public_html/admin` を読み取りのみで実走 → `app.css` 218（106/55/43/14）・`index.html` 487（409/49/27/1/1）＝ 07-29 / 07-30 と内訳まで一致。
   Wave 表の訂正は xi-workspace PR #2（hub マージ `523ab22`）。**#163 の残**（`nene2/style-prop-css-vars-only` が非 Tailwind 艦で外れる）は
   断片の分割＝配布 API 変更で **hub 裁定待ち**。非 Tailwind 4艦には赤が1行残る。
6. **C8 は完了**（xi-workspace PR #1・hub 検収合格・`97d2feb`・板 L96 done）。`seed-all.py` は tracked な `.mcp.json` を書かない＋`--dry-run`。
   **本走は hub が「次に handles.tsv を触る時」**。vault の作業木に残る `.mcp.json` の 12/1 差分は **vault の手**。
7. 🔴 **`_work` main の `3ca8b8e`（2026-08-26 01:49）は自艦の誤コミット**——hub の未コミット変更（advice / board / done）を私の日報メッセージで commit・push した。
   **hub 裁定＝据え置き**（内容は正しく欠損なし・共有 main の書き換えは他者の pull を壊す）。構造原因は xi-workspace issues #128（規約化は施主判断）。⇒ 型11。

## ⚠️ 0.11.0 → 0.14.0 で見た目が変わる4点

| 変わるもの                 | 版     | 影響                                                               |
| -------------------------- | ------ | ------------------------------------------------------------------ |
| 警告が error 色 → warn 色  | 0.12.0 | `InlineAlert tone="warn"`。**これまで警告が赤で描かれていた**      |
| コントロールが妥当性を塗る | 0.12.0 | **これまで無塗装** ⇒ 新たに枠と背景が付く                          |
| `Button` が `inline-flex`  | 0.13.0 | **文章の途中に置いたボタン**のみ。ボタン列・ツールバーでは見えない |
| タッチ端末で最低 44px      | 0.14.0 | **スマホ・タブレットのみ**。デスクトップは不変                     |

🔴 **0.11.0 には破壊的変更もある** — `Checkbox` / `Radio` の `className` が `<label>`（root）へ。箱は `inputClassName`。
**今のところ壊れる艦は無い。ただし理由は「使っていないから」ではない**〔2026-08-24 16:5x JST 実測〕。

消費艦は **`nene-vault` の1艦のみ**で、その全数は **3箇所**（`ExportPage.tsx:126` の `Radio` は2値の map ＝ 描画2個・
`ExportPage.tsx:144` の `Checkbox`・`DocumentSearchForm.tsx:78` の `Checkbox`）。
うち **1箇所は `className` を渡している**（`ExportPage.tsx:145` の `className="self-start"`）。`inputClassName` の実使用は **0件**。

壊れないのは、**その1箇所が 0.11.0 の意味へ既に載せ替え済み**だから——現物のコメントが
「`Said directly on the control since 0.11.0`: `className` lands on the `<label>` now」と書いている。
⇒ **`className` は「使われていない」のではなく「新しい意味で既に依存されている」。**
**次にこの受け口を変えると壊れる**ので、危険度の判定は「触られていないので安全」ではなく「依存されている」側で読むこと。

## 次の順（2026-08-26・残り）

**#410 は判定済み・close**〔02:0x〕: 候補18件（hub の数と一致）→ close 4（#136 #302 #391 #410）・理由コメント 5（#176 #289 #389 #393 #394）・触らず 9。閉じ忘れは **2件**（#302 #391）だけだった。open Issue **72 → 67**。

1. **#289 の陽性対照4項目**（notify-failure・`workflow_dispatch` で `simulate_failure=true`・Issue が1本立つので時間帯を選ぶ）。
2. **#389 の README 旗艦例**（存在しない `--spacing-x-slot-card-pad` を実在スロットへ・`readme.test.ts` に「例のスロット名は `slot-pairs.json` に在る」を足す）。`src/index.ts:58` の古いコメント（Textarea 未出荷）も同じ PR で。
3. その後は「未着手」の節（型スケール 13px・契約28色の未定義・`className` 7部品）から、判断が要らないものを拾う。

🔴 **PR は1本ずつ main 直下で出す。積み上げ PR は squash と組み合わせない**（08-25 に踏んだ。日報 08-25-3 §積み上げ）。
🔴 **マージと publish は施主の直接 GO をこの会話で取る**（hub 中継の GO だけでは実行しない・07-17 の取り決め・08-25 も同じ形で通した）。

## 未着手（記録のみ・判断が要るもの）

- **型スケールに 13px が無い。** vault の本番 Button は本文14px に対して13px。🔑 **型スケールは「艦が Tailwind クラスで書いているもの」から測ったので、段が無い値は原理的に見えない**——radius で `border-radius` 直書きを数え落としたのと同じ形。⇒ **vault の Playwright で本番の計算済みスタイルからヒストグラムを取れば、それが正しい分母。**
- **契約28色のうち20色が未定義。** `surface-overlay` `surface-sunken` `text-faint` `text-inverse` `border-strong` `accent-hover` `accent-soft` `on-danger` `success` `success-soft` `on-success` `info` `info-soft` `on-info` `focus-ring` `scrim`。⚠️ **`focus-ring` と `scrim` は 0.9.0 でスロット名として発明したもの**が、契約に既にパレット項目として在った。
- **`className` を prop として受けない部品は 7つ**〔実測 2026-08-24〕: `Badge` / `FormField` / `Modal` / `ConfirmDialog` / `DataTable` / `Pagination` / `ToastProvider`（受けるのは21部品）。**前の版の「他にもありそうだが数えていない」に実数が付いた。** 🔴 **7件は一律ではない**——オーバーレイ／プロバイダの3つは**受けないのが設計意図の可能性**があり、`DataTable` は**設定駆動 API なので `className` を開けても解けない**。**まとめて「不揃い」と呼ぶと、意図的な非公開を不揃いとして潰す。**
- **W2 の設計問題**（未決）: `max-*` のデスクトップ優先・多態 `as`・片側だけの padding。

### 採らないと決めたもの（書かないと、次の人が「不揃い」と読む）

- 🔴 **`fleet-baseline.json` の floor を npm の latest に追随させない。** 4つとも floor < npm だが、
  これは**上げ忘れではない**。**hub 裁定（2026-08-15）で据え置き**が決まっている——**上げても実効ゼロ**
  （floor を機械判定する器が無い）で、**実艦の6割が現 floor すら満たしていない**。
  ⇒ **上げるべきは floor ではなく、各艦の実採用版。** **解除条件 = 対象艦がすべて追いついた時点。**
  ⚠️ **publish 直後は「自然な後片付け」に見えるので、毎回ここを読むこと。**
  （`nene2-ui` も同じ——唯一の消費艦 vault が 0.14.0 なので、**先に上げると vault が floor 未達になる**。）
- 🔴 **`Modal` に高さ上限のスロットを置かない。** `showModal()` した dialog は
  **UA スタイルが既にビューポートで頭打ち**にする。キット側でもう1つ上限を作ると `80vh` のような
  リテラルがテーマに住み、**「スロットの既定はスケール参照のみ」に反する**——実際 `readme.test.ts` が
  赤にした。⇒ **足りないのはスクロールのほうだけ**で、`scrollable` がそれを持つ。
  ⚠️ **UA の上限は jsdom で検証できない**ので live レーン。**直せない場所で緑にしない。**
- 🔴 **`--shadow-focus` は置かない。** 契約 v1 の `SHADOW_KEYS` には `focus` が在るが、**キットは focus を `outline` で描く**（`ring-*` は box-shadow なので `overflow: hidden` の親に切られる — `lib/states.ts`）。⇒ **利用者のいない値になる。**
- 🔴 **`PageHeader` / `Section` に `as` を開けない。** `<header>` / `<section>` は**要素が部品の意味そのもの**。（`Card` など汎用の箱は別——`Text` に「閉じた union の `as`」の前例が在る。）

## 板（横断・hub の持ち物）

**期限切れ4件**が残っている（`timeout-minutes` の横断配布ほか）。⚠️ **自艦は #287 で `timeout-minutes` を入れ済み**〔実測〕だが、**profile は `backend-ci.yml` / `frontend-ci.yml` とも 0件**〔実測 2026-08-23〕で、`#137` の e2e が **6h0m15s（既定360分）** で吊って止まっている。

## このリポで効いている型（次に座る人が踏まないように）

1. 🔑 **列挙で書いた検査は、列挙に無いものを緑にする。** 08-23 だけで**4件**（lock ゲート / スロット既定の名前空間 / 部品の到達プレフィクス / baseline の floor）。⇒ **#353 では列挙をやめ、パレットをテーマから読む形にした。**
2. 🔑 **平均誤差だけを見る検査は「意図して引かれた区別の削除」を誤差として通す。** ⇒ **実在する2つの値が同じ段へ潰れないか**を判定に足す（vault 提案）。radius と色の両方で使っている。
3. 🔑 **テストは「本体」ではなく「本体の代理」を検査していることがある。** 代理と本体が同じ部品に居るあいだは等価だが、**本体が上流へ移った瞬間に別物になり、代理は緑のまま残る。** ⇒ **実装を上流へ移す作業は、この形の回帰を構造的に作る。**
4. 🔑 **道具の窓を「干し草の性質」で切ると、静かに外す。** `grep -A3` は範囲を跨げない・1行 grep は複数行 JSX を跨げない・トークン定義だけ数えると直書きが落ちる。

   ⚠️ **2026-08-24 に同じ型を1日で4回踏んだ**（すべて自分で見つけて訂正）。**切った窓はどれも「干し草の性質」で、探しているものの性質ではなかった**:

   | 切った窓（干し草） | 本当に気にしていた性質（針） | 起きたこと |
   | --- | --- | --- |
   | **行頭にあるか**（`^\s*`） | 宣言かどうか | 1行 CSS を取りこぼし**検査が空振り** |
   | **接頭辞の綴り**（手書きの列挙） | `-x-slot-` を持つか | `font-weight` / `brightness` が**まるごと不可視** |
   | **拡張子**（`.tsx`） | スロットを読むコードか | `.ts` の8本を**「孤児」と誤報** |
   | **その語がファイルに在るか** | **prop として受けるか** | 内部利用を拾い**受けない部品を🟢と誤判定** |

   ⇒ **代理を検査して本体を検査しない、が道具の側に出た形。型を知っていることは防御にならない。**

   **対策（この順で効く）:**
   - 🔴 **既知の陽性を1件、新しい道具に通す。** 3件目の陽性は**数十分前に自分で読んでいた**（`lib/states.ts` の `TOUCH_CLASS`）——**陽性対照は作る必要すらなく手元にあった。**
   - 🔴 **新しい道具が出した驚きは、発見より道具の欠陥である確率が高い。** 誤りの瞬間は `宣言122 / 参照114 / 差8`、正しくは `122 / 122 / 0`。**非対称そのものがアラームだったのに、発見として読んだ。** 自分の検査を通っているコードに孤児が8本ある確率より、**30秒前に書いた glob が壊れている確率の方が高い。**
   - 🟢 **列挙をやめて導出する。** 失敗4件は**すべて列挙**、成功した測定は**すべて導出**。**型1は検査だけでなく道具にも当たる。**
5. 🔑 **測れなかったものは「測れなかった」と書く。** 08-23 は数字を出さない判断を**2回**した（Button の display 分布・`className` を受け取らない部品数）。
6. ⚠️ **PR が2本並んでいるとき、それぞれの緑は「相手が入る前の緑」。** 片方が検査を足し、もう片方がその検査の対象を変えるなら、**`BEHIND` のままマージすると組み合わせを誰も走らせない。**
7. 🔴 **この文書に「いま open な PR 番号」を書かない。** その行は**必ず**、それを運ぶ PR のマージで偽になる。⇒ **マージ後も真である形で書く。**

   ```
   🔴 PR #375 が施主のマージ待ち                      ← マージで偽になる
   🔴 baseline は publish 済みの版に追随済み〔実測〕   ← 真のまま
   ```

   ⚠️ 2026-08-23 に**3度**踏んだ（`Checkbox` の doc コメント／PR #373 の直前／**その直後に PR #375 で作り直した**）。**2度目で気づいて止めたのに3度目を作った**ので、「気づく」では止まらない。**書き方の側で塞ぐ。**

8. 🔑 **結論が当たっていると、理由は検算されない。** 2026-08-24 に本文書のこの誤りを含めて**3件**出た——
   いずれも**結論は正しく、理由が違っていた**。

   | | 結論 | 理由 |
   | --- | --- | --- |
   | 本文書の `className` 10箇所 | 🟢「壊れる艦は無い」 | 🔴 逆（使っていないから、ではなく**載せ替え済み**だから） |
   | `--spacing-x-slot-button-gap` | 🟢「vault は 2xs を書く必要があった」 | 🔴 逆（欠落ではなく**好みの上書き**・既定は `:117` に在る） |
   | `themes/default.css:367` | 🟢「今日は何も壊れない」 | 🔴 **条件を落としている**（片側上書きを見ていない・#380） |

   ⚠️ 本文書の1件目は、**前セッションの自分が書いた数字を、次のセッションの自分が検算せずに他艦へ渡した**。
   そこから hub の日報・vault へと**3艦を通って誰も数えなかった**。数え直したのは、hub が「これも伝聞です」と
   **札を剥がした後**の vault。⇒ **`current.md` は永続記録なので、ここに書く数字は自分で測ってから書く。**
   🔑 **偽の〔実測〕札が伝播を設計するのに対し、「これは伝聞」の札は検算を起動する。**

9. 🔑 **時刻を名乗るなら `date` の出力を持つ。** 2026-08-24 に、`git log` で読んだ最終コミット時刻から
   「今は 06 時台」と推論し、**〔実測〕の札を貼って4箇所に書いた**（実際は起動の9時間前）。
   ⇒ **錨にしてよいのは、いま自分が走らせた行為が生んだ時刻だけ**（`date` / いま回したテストの `Start at` / `uptime -s`）。
   **`git log %ci` と `stat` の mtime は錨にしない**——**対象の時刻であって、観測の時刻ではない。**

10. 🔑 **「確認できない」は、道具を探してから言う。** 08-25 に「Tailwind をビルドしないので arbitrary value の生成結果は
    キット側で確認できない」と書き、検収を vault の Playwright へ回した。実際には tailwindcss 4.3.2 が自艦の `node_modules` に
    居て（standards の eslint plugin の依存・`npm ls tailwindcss`）、`compile` API 直叩きで生成 CSS が出た。Playwright も
    vault の `node_modules` と `~/.cache/ms-playwright` の Chromium で自艦から回せた（陽性対照つきの accessible name まで）。
    ⇒ **「無い」は `find … node_modules` を打ってから。「このリポはビルドしない」は「この道具が無い」を意味しない。**
    型4の裏返し——型4は「道具の窓が狭い」、これは「道具があるのに探していない」。どちらも**測れるものを伝聞に落とす**。

11. 🔴 **`_work`（hub の共有作業木）では `git -C` ＋絶対パスだけ・`git commit -a` を打たない・worktree の後片付けは単独の Bash。**
    2026-08-26 01:49、1つの Bash で `cd _work && git worktree remove …; python3（相対パスで失敗）; git commit -qam …; git push` と連ねた結果、
    失敗後も cwd が `_work` のまま `commit -a` が走り、**hub の未コミット変更を自艦の日報メッセージで共有 main に push した**（`3ca8b8e`・据え置き裁定）。
    枝は origin/main から scratchpad の worktree で切る（C8・#164 タスク3 はこの形で無事だった）。**事故は「同じ手順の最後の1回」で起きた。**

## 日報の置き場について（hub へ要確認）

⚠️ hub の通達は「置き場は `nene-origin/internal-docs/<product>/daily/`（P3 移設済み）」だが、**自艦の受け皿は存在しない**〔実測〕。`internal-docs/nene2/` は **PR 番号 #1613〜#1656** で `hideyukiMORI/nene2`（PHP フレームワーク）のもの（自艦は #371）。

規約正本 `_work/daily-report-convention.md` §1 は「**§11 の発効は P3 Phase 0 完了・hub 通達時。それまでは `docs/daily/` が現行**」。⇒ **`docs/daily/` に書いている。**
