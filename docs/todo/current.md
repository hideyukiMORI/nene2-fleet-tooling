# 現況（生きた引き継ぎ）

> **これは「いまの状態」を書く1枚**（現在形・上書き）。その日の記録は `docs/daily/`（過去形・追記しない）。
> **最終更新: 2026-08-26 20:4x〔`date` 実測 20:48:56〕**

## いまどこ

**キットは「載せ替え中の艦から来た PR を受け入れる側」に回った。** 本セッションで **deal が4本の PR を出し、4本ともマージした**（#455 / #456 / #457 / #463）。`nene2-ui` は **0.18.0 / 0.18.1 / 0.19.0** と3回出荷。

🔴 **マージ権限が変わった**（施主裁定・#461 / `954c0d7`）。**他艦から来たキット PR のマージは自艦裁量**。
**publish と、自艦が書いた PR のマージは従来どおり施主の直接 GO。** 正本は `CLAUDE.md`。

| パッケージ | `main` | npm の latest | `fleet-baseline` の floor | タグ以降の未リリース |
| --- | --- | --- | --- | ---: |
| `nene2-tokens` | 1.3.0 | **1.3.0** | `^1.1.0` | **0件** |
| `nene2-standards` | 2.4.0 | **2.4.0** | `^2.1.1` | **0件** |
| `nene2-i18n` | 0.3.2 | **0.3.2** | `^0.3.0` | **0件** |
| `nene2-ui` | 0.19.0 | **0.19.0** | `^0.14.0` | **0件** |

|                  | 値〔すべて自艦で実測・2026-08-26 20:48:56〕 |
| ---------------- | ---------------------- |
| テスト           | **814** / 50ファイル   |
| open な Issue    | **66**                 |
| `npm run check`  | 🟢 EXIT=0（8ステップ） |

🔑 **版の一致は「中身が出ている」ことを意味しない。** 未リリース差分は
`git log <pkg>-v<version>..HEAD -- packages/<pkg>` で見る。**右端の列が正本。**

🔴 **そして逆向きも素通りする**（本セッションで実際に踏んだ）。**修正が `main` に在っても、タグより後なら出荷物には入っていない。**
`nene2-ui` の `files` は `["dist","themes"]` なので **README も `themes/` も tarball に同梱される** ——
#389 の README 修正が 0.18.0 のタグ後に入ったため、**0.18.0 を入れた人は直る前の README を読んでいた**。
⇒ 0.18.1 は**そのためだけの doc-only patch**。**doc しか変えていなくても、その doc が `files` に入っているなら publish が要る。**

## 🔴 いちばん上にあること

0. 🟢 **`nene2-ui` 0.19.0 は publish 済み**〔`npm view` 2026-08-26 20:45:32 実測・shasum `40d2c54f`・
   `npm pack` で3スロットの実在と `dist` の `BASE_CLASS` を確認〕。**残るのは各艦の取り込み。**
   中身は **#463（Badge の gap / font-size / font-weight）** で、🔴 **既定の描画が変わる唯一の版**
   （gap: 未指定 → 4px ／ font-size: 継承 → 11px ／ weight: 継承 → 600）。
   - **vault**: 段階1（スロット行）は **完了**〔`f52c200`・不活性を自艦で裏取り: 入っている 0.17.1 の dist は
     当該スロットを **0箇所**参照・陽性対照 `x-slot-badge-pad-x` は 1箇所〕。
     **段階3（caret 上げ＋`gap-1.5` 除去）は施主判断待ち**——**寝かせる公算が高い**（gap 6px→4px は
     見える変更で**目視ゲートを1回消費する**ので、他の変更とまとめたい／0.17.1 に留まる実害は現状なし）。
   - **deal**: 0.19.0 を受け取って目視束へ。**gap 5px→4px・字 11.5px→11px** は**寄せられない差**
     （スケールは 4/8/12/16/20/24/32/48px で 5px も 11.5px も段ではない）⇒ 理由つきで目視束に載せる。
   - 🔴 **「npm に出た」と「艦の本番に載った」は別に数える。** 現時点で **0.19.0 を本番に載せた艦は 0**。
1. **`cx` の docstring が誤っている**（#469・**未着手**）。「caller's classes come last so Tailwind's later-wins
   cascade lets them override」と書いてあるが、**class 属性の順序は優先度を決めない**。実測で
   **キットの `BASE_CLASS` が3項目とも勝った**。**README の設計原則2にも書かれている**ので規約と実装の食い違い。
   🟢 **hub 裁定が出た = (a) のみ**——**記述を実態に合わせ、意匠の上書きはスロットへ誘導する**。
   🔴 **(b) `tailwind-merge` は入れない**（className で意匠を上書きする経路を正式化すると、
   スロット中心の設計と逆行する。依拠していたのは vault の `BADGE_CHROME` 1件で、それはスロット化で解けた）。
   **第三艦で同じ依拠が出たら再考。**
2. **#289 の陽性対照4項目**（notify-failure・`workflow_dispatch` で `simulate_failure=true`・**Issue が1本立つので時間帯を選ぶ**）。

## いま外に出ている手番

| 相手 | 何を待っているか |
| --- | --- |
| **vault** | #463 の**段階3**（caret＋`gap-1.5` 除去・vault #481）。**施主判断待ち**・寝かせる公算 |
| **deal** | W1 の目視束（`kit-slots.css` のコメント3行を外す）。**栓は外れている** |
| **hub** | 🟢 **どちらも裁定済み** — #469 は (a) のみ／板 L83 は施主裁定 (c)（移行時に line-height を確認・局所 `leading-inherit`）で done |

## ⚠️ 0.11.0 → 0.14.0 で見た目が変わる4点

| 変わるもの                 | 版     | 影響                                                               |
| -------------------------- | ------ | ------------------------------------------------------------------ |
| 警告が error 色 → warn 色  | 0.12.0 | `InlineAlert tone="warn"`。**これまで警告が赤で描かれていた**      |
| コントロールが妥当性を塗る | 0.12.0 | **これまで無塗装** ⇒ 新たに枠と背景が付く                          |
| `Button` が `inline-flex`  | 0.13.0 | **文章の途中に置いたボタン**のみ。ボタン列・ツールバーでは見えない |
| タッチ端末で最低 44px      | 0.14.0 | **スマホ・タブレットのみ**。デスクトップは不変                     |

🔴 **0.11.0 には破壊的変更もある** — `Checkbox` / `Radio` の `className` が `<label>`（root）へ。箱は `inputClassName`。
**壊れる艦は無い。ただし理由は「使っていないから」ではない** — 唯一の消費艦 vault の1箇所が
**0.11.0 の意味へ既に載せ替え済み**（`ExportPage.tsx:145`）。⇒ **`className` は「使われていない」のではなく「新しい意味で既に依存されている」。**

⚠️ **この表の消費者は `nene-vault` の1艦だが、vault は既に 0.17.1**〔2026-08-26 20:2x・vault 実測の申告を自艦で裏取り〕。
**この節は 0.19.0 の移行が済んだら畳んでよい。**

## 未着手（記録のみ・判断が要るもの）

- **型スケールに 13px が無い。** 🔑 型スケールは「艦が Tailwind クラスで書いているもの」から測ったので、**段が無い値は原理的に見えない**。⇒ vault の Playwright で本番の計算済みスタイルからヒストグラムを取れば、それが正しい分母。
- **契約28色のうち20色が未定義。** ⚠️ `focus-ring` と `scrim` は 0.9.0 でスロット名として発明したものが、契約に既にパレット項目として在った。
- **`className` を prop として受けない部品**: 0.17.0 で `Badge` / `FormField` / `DataTable` / `Pagination` の4つが受けるようになったので、残るのは **`Modal` / `ConfirmDialog` / `ToastProvider` の3つ**。🔴 **一律ではない** — オーバーレイ／プロバイダは**受けないのが設計意図**（0.16.1 の `<dialog>` の margin 事故が実例）。
- **W2 の設計問題**（未決）: `max-*` のデスクトップ優先・多態 `as`・片側だけの padding。

### 採らないと決めたもの（書かないと、次の人が「不揃い」と読む）

- 🔴 **`fleet-baseline.json` の floor を npm の latest に追随させない。** **hub 裁定（2026-08-15）で据え置き。**
  根拠は **floor を機械判定する器が存在しない**（＝上げても実効ゼロ）ことと、**実艦の6割が現 floor すら満たしていない**こと。
  ⇒ **上げるべきは floor ではなく、各艦の実採用版。** **解除条件 = 対象艦がすべて追いついた時点。**
  ⚠️ **publish 直後は「自然な後片付け」に見えるので、毎回ここを読むこと。**
  ⚠️ **旧版に在った「vault が 0.14.0 だから」という理由は成り立たない**（vault は 0.17.1）。**裁定は上の2点で立っている。**
- 🔴 **Badge の `gap` 既定を 6px にしない**（vault の照会への回答・2026-08-26）。
  実測: スケールは **4 / 8 / 12 / 16 / 20 / 24 / 32 / 48px**。**vault は 6px・deal は 5px で、どちらも段ではない。**
  ⇒ **「艦に合わせた既定」は存在しない** — どちらを採っても片方の drift をキットが正典にするだけ。
  **移行は drift を段に載せ替える作業**であって、避けるべき損失ではない。
- 🔴 **`--text-x-slot-badge` に相棒 `--line-height` を持たせない。** 実測: 相棒が未定義の `--text-*` は
  **`font-size` しか出さない**（陽性対照 `text-sm` は両方出す）。⇒ 艦の `leading-*` は**上書きされない**。
  キットで相棒を持つスロットは**全数0本**なので、足すと初の例外になり、艦の移行が1つ増えるだけ。
  板 L83 の横断課題で方針が決まったら**全スロット一括**で。
- 🔴 **`Modal` に高さ上限のスロットを置かない**（UA が既にビューポートで頭打ちにする。足りないのはスクロールのほうで `scrollable` が持つ）。
- 🔴 **`--shadow-focus` は置かない**（キットは focus を `outline` で描く。`ring-*` は box-shadow なので `overflow:hidden` の親に切られる）。
- 🔴 **`PageHeader` / `Section` に `as` を開けない**（要素が部品の意味そのもの。汎用の箱は別＝`Text` に前例）。
- 🔴 **`Card` の padding をスロット化しない**（本セッションでは採らなかった・#389 の射程外）。
  **layout 部品（`Box` / `Card` / `Stack` / `Section` / `Grid`）が prop から寸法を引くのは設計**で、
  呼び出し側が instance ごとに決めるもの。スロット化は**描画に影響する**ので要判断。

## 板（横断・hub の持ち物）

**期限切れ4件**が残っている（`timeout-minutes` の横断配布ほか）。⚠️ **自艦は #287 で導入済み**。
**Dependabot 是正の実行順**は本セッションで hub へ提出済み（`_work/reports/2026-08-26-dependabot-order-nene2-first.md`）——
🔴 **NENE2 のゲートは 2026-08-22 に NENE2 レーンが既に入れていた**ので、板 L78 の前提は解消済み（hub が done 化）。

## このリポで効いている型（次に座る人が踏まないように）

1. 🔑 **列挙で書いた検査は、列挙に無いものを緑にする。** ⇒ **導出する**（`grep` の結果から作る）。
   🟢 **本セッションで、この型が他艦を実際に捕まえた** — deal が `mt-x-3xs` とスケールを直書きし、
   `readme.test.ts` の「no component reaches past the slots into the scale」が落とした。
   🔑 deal の実感: **「規約を知らない他艦を止めたのは、検査の存在ではなくテスト名の具体性」**。
2. 🔑 **平均誤差だけを見る検査は「意図して引かれた区別の削除」を誤差として通す。**
3. 🔑 **テストは「本体」ではなく「本体の代理」を検査していることがある。** ⇒ **#389 がこの型の教科書例**。
   `readme.test.ts` は「README が文字列を含むか」（代理）を見て「そのスロットが実在するか」（本体）を見ていなかった。
   **緑のまま誤りを固定し、直そうとすると赤になる形**だった。⇒ 本体を検査する形へ移した。
4. 🔑 **道具の窓を「干し草の性質」で切ると、静かに外す。** ⇒ **既知の陽性を1件通す。**
5. 🔴 **そして「対照を置いた」では足りない。「対照が発火することを見た」までやる**（deal の定式化・2026-08-26）。
   **本セッションの誤り3件はすべて「対照を置いてはいた」**:

   | 何を測ったつもり | 実際 | なぜ気づけたか |
   | --- | --- | --- |
   | 全艦の alert 集計 | 本文を `grep 'disabled'` で 403 判定 ⇒ **生きた艦が未計測へ落ちた** | **既知の陽性**（NENE2=4件）と矛盾した |
   | 省略時 DOM の一致 | `queueMicrotask` が流れず**空同士を比較**して「一致」 | 出力を目で見た（`''` だった） |
   | className と BASE_CLASS の勝敗 | 比較対象のクラスが**そもそも生成されていなかった**（位置 -1） | 位置が -1 だった |

   🔑 **`'' === ''` は緑になる。** ⇒ **比較する前に「測れているか」を問う。**
6. ⚠️ **PR が複数並んでいるとき、それぞれの緑は「相手が入る前の緑」。**
   🟢 **本セッションで実証** — #458/#459/#460 は**3本とも `themes/default.css` を触った**。
   隔離 worktree で段階マージして実走（804 → 806 → 810）してから `update-branch` で CI を当てた。
   **リポ側も BEHIND を蹴った**（この規律は機械で守られている）。
7. 🔴 **この文書に「いま open な PR 番号」を書かない。** その行は必ず、それを運ぶ PR のマージで偽になる。
8. 🔑 **結論が当たっていると、理由は検算されない。**
9. 🔑 **時刻を名乗るなら `date` の出力を持つ。**
10. 🔑 **「確認できない」は、道具を探してから言う。**
11. 🔴 **共有の作業木では枝を切らない。** `_work` 限定の話ではなかった——
    **1つの作業木に2人が居ると、後から枝を切ったほうが相手の未コミット変更を攫う。**
    本セッションで **deal が自艦の常駐作業木を直接編集しており、私の `git checkout -b` が deal の
    未コミット変更を私の枝へ持ち越した**（即戻して無傷・同一 SHA だったので内容中立）。
    ⇒ **他艦のリポで作業するときは `git worktree add` で自分の場所を作る。**
12. 🔴 **中継した数字には、自分が測ったかどうかの札を貼る。**
    本セッションで **「vault の本番実測」と書いて deal の数値（11.5px）を配った**——
    **vault の font-size を一度も測っていなかった。** vault に訂正された。
    🔑 **測っていない相手の数字を、測った相手の名前で配ると、訂正の宛先が消える。**
13. 🔑 **慣習に合わせる前に、慣習の中身を読む**（#463・deal の自己診断）。
    既存の `--font-weight-x-slot-*` 6本がすべて `medium` なのは**設計された慣習ではなく、
    `font-medium` が4部品にインラインで在ったものをスロット化した＝既存描画の保存**だった
    （`themes/default.css` のコメントが自分でそう書いている）。
    **Badge にはインラインの weight が無かった**ので、慣習は当たらない。
    ⇒ **「揃っている」は「揃えるべき」の理由ではない。**

14. 🔴 **部分文字列は「在る」を水増しする。** 0.19.0 の出荷物を検証したとき
    `grep -c 'text-x-slot-badge'` が **7件**を返した。多すぎると思って調べたら、
    **`text-x-slot-badge-danger-fg` など tone 系6件との部分一致**で、**単独クラスは1件**だった。
    ⇒ **型4（道具の窓）の族だが、向きが逆**——型4は「窓が狭くて取りこぼす」、これは
    **「窓が広くて水増しする」**。**存在確認は境界つきで測る。**
    🔑 **多すぎる結果も、少なすぎる結果と同じくらい道具を疑う理由になる。**

15. 🔴 **観測していない時刻に観測の札を貼る経路は2つある。**

    | | 機構 | 本日踏んだ艦 |
    | --- | --- | --- |
    | (a) | **相手の名乗りを錨にする**（往復のたびに自分の数字を1つ進める） | vault（自己訂正） |
    | (b) | **自分の過去の観測から外挿する** | 🔴 **自艦** |

    自艦は本セッションで `date` を **8回**走らせたが、**「18:07」「18:19」「20:0x」「20:1x」は測っていない**
    ——直前の `date` からの経過を頭で足して書いた。
    🔑 **(b) のほうが止まりにくい**（vault の指摘）: **実測が手元にある分だけ、疑う契機が生まれにくい。**
    「さっき測ったから、そこから20分」は記憶からの推定より**実測に近いと感じられる**。
    ⇒ **時刻を書く直前に、その1通のために単独で `date` を打つ。**
    ⚠️ 本日この形は **hub / deal / vault / fleet の4艦すべて**で出た。**誰も自力では気づいていない**
    （最初の1件を hub が検出し、そこから各艦が自分のぶんを見つけた）。
    🔑 **訂正を当事者間で閉じずに配ると、同型を持つ相手が自分のぶんを見つける。**

16. 🔴 **出荷とドキュメントを別 PR に分けると、ドキュメントはマージ直後に古くなる。**
    本文書は #472 でマージされ、**その約20分後**に 0.19.0 を publish したので、
    **冒頭の「いちばん上にあること」0番が「publish 待ち」のまま**になった——
    **次に座る人が最初に読む行が、済んだ仕事を最優先として示していた。**
    🔑 **本文書は §型 に「living doc ほど参照側は最終更新を見ない」と自分で書いている。その実例を自分で作った。**
    ⇒ **担当が同じでも、PR が分かれていれば起きる。** 出荷 PR とドキュメント PR が並ぶときは、
    **ドキュメント側を後に回す**か、**出荷 PR にドキュメントの当該行を同梱する**。

## 日報の置き場について（hub へ要確認）

⚠️ hub の通達は「置き場は `nene-origin/internal-docs/<product>/daily/`」だが、**自艦の受け皿は存在しない**〔実測〕。
規約正本 `_work/daily-report-convention.md` §1 は「**§11 の発効は P3 Phase 0 完了・hub 通達時。それまでは `docs/daily/` が現行**」。
⇒ **`docs/daily/` に書いている。** 同一日に複数セッションある場合は**連番**（`YYYY-MM-DD-2.md`・#248）。
⚠️ 板 L107 が「日報を origin へ移設」を A 優先で持っているが、**先行条件つき**なので勝手に始めない。
