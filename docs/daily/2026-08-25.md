# 2026-08-24 セッション日報

> **対象期間: 2026-08-24 16:2x 〜 2026-08-25 02:2x（JST）**〔`date` 実測。機械の起動は `uptime -s` = 15:02:11〕
> **ファイル名は終了日（08-25）**。1セッション = 1本・日付では割らない、の規約に従う。
> 前セッション（08-23 3本目・`docs/daily/2026-08-23-3.md`）の続き。
> ⚠️ **この日報は3回上書きしている**（`#400` / `#408` / `#414`）。18:5x に書いてマージした後、
> **セッションの過半がその後に起きた**ため。🔑 **記録を書いた後に記録の対象が増える**のは
> セッションが続く限り起き続けるので、**畳むときに必ず読み返す**。

`/repo-status` から始めたセッション。リポの健康状態は良好で（`npm run check` EXIT=0・open PR 0本・stale 枝0）、
出てきたのは**動かないバグではなく、文書・検査器・リリース工程の「名乗りが実体より広い／古い」**類だった。
後半は施主 GO で是正に入り、**PR 15本をマージ**して**4パッケージすべてを publish** した
（`nene2-ui` は 0.15.0 と 0.16.0 の2回）。
**自分の誤りは8件**で、うち**1件は日報を書いた後に見つかった自分の版の取りこぼし**である。
そしてその取りこぼしを直す過程で、**同じ欠陥が3パッケージに最長34日ぶん埋まっていた**ことが出た。
8件目は最後の実装中の道具の誤判定で、**これだけは結果を疑う前に道具を疑って、その場で捕まえた。**

## ヘッドライン

- 🔴 **測っていない時刻に〔実測〕の札を貼った。** `git log` で読んだコミット時刻から「今は 06 時台」と推論し
  4箇所に書いた。実際は機械の起動の9時間前。**hub がそれを検算せずに自分の日報へ写した。**
- 🔴 **同じ型（道具の窓を「干し草の性質」で切る）を、1日で4回踏んだ。** 全件自分で見つけて訂正。
- 🔴 **マージ済み2本が版を上げていなかった**（`#396`）。`package.json` は 0.14.0 のままで npm latest と
  同じに見えるのに、main には未 publish の配布物が入っていた。**日報を書いた後に発見した。**
- **`check:exemplars` の drift を再現確認したら 1件実在**——しかも**予想と逆向き**（コードが正しく、条文が動いていた）。
- **影がスロット化されていない唯一の次元**だった。**キット自身の既存テストに止められ**、影のスケールも 1段 → 3段にした。
- 🔴 **`nene2-ui` の版の取りこぼしを直したら、同じ欠陥が残り3パッケージに埋まっていた**（最長34日）。
  **版は npm と一致していたのに中身が出ていない**状態で、**`#163` / `#164` の修正が26日間 艦に届いていなかった。**
- **4パッケージすべてを publish。** 未リリース差分は**0件**になった。
  🔴 **その後 floor を上げようとして、`#271` の hub 裁定（据え置き）に止められた。**
- **`Modal` に4点を prop で足し、0.16.0 を publish。** ⇒ **vault の本番 a11y 欠陥
  （`aria-modal` を名乗って Esc も focus trap も無い・取り消せない操作を含む5画面）の是正が、
  上流で塞がっていた状態が解消した。**
- 🔴 **陽性対照だけでは偽陽性を捕まえられない**ことが実測で出た。**窓を3回外し、3回とも陽性対照は通っていた。**

## 本日の成果（時系列）

### 1. `/repo-status` の実測とセッション間通信

`main` = `e51e83c`・clean・枝は main 1本（`git ls-remote`）・**open PR 0本**・**open Issue 64件**
（`--limit 500 --json number -q length` で総数測定）・CI 直近6本 success。
`npm run check` **EXIT=0**・**テスト 736 / 46ファイル**。

hub（`docker-c8`）へ `SendMessage`。**名前から役割を推定せず、宛先違いの可能性を明記して短く投げ、
名乗りが返ってから本題**の型に従った。以後この日ずっと往復した。

前セッションの `current.md` の 🔴 いちばん上だった「**艦への周知が未実施**」を hub が受領して**その場で実行**
（消費艦は `nene-vault` 1艦のみ）。**自艦の 🔴 が1件、通信で外れた。**

### 2. 🔴 測っていない時刻に〔実測〕の札を貼った（自分の誤り①）

hub の指摘で発覚。`date` を一度も打たずに「06:1x 実測」と**4箇所**に書いた。
実測は `date` = **16:42:38 JST** / `uptime -s` = **15:02:11**——**名乗った時刻は起動の約9時間前**。

発生源は、直前に `git log -1 --format='%ci'` で最終コミット `05:45:11` を読んでいたこと。
**推論であることを忘れて実測の札を貼った。**

実害: **hub が検算せずに自分のメッセージ2通と `_work/daily/2026-08-23-hub.md` へ写した**（hub 側で訂正済み）。
🔑 **もっともらしい推論に〔実測〕を貼るのは、数字を間違えるより悪い。読み手は札を見て検算をやめる。**

処置: `#378` の本文と直前コメントを訂正し訂正コメントを追加。会話メモリに `measured-label-needs-a-command` を作成。
**錨にしてよいのは、いま自分が走らせた行為が生んだ時刻だけ**——**`git log %ci` と `stat` の mtime は錨にしない**
（**対象の時刻であって、観測の時刻ではない**）。

### 3. 🔴 前セッションの数字を検算せず他艦へ渡した（自分の誤り②）

`current.md` の「**艦10箇所すべて `className` を渡していない**」を検算せずに hub へ渡した。
実測は **3箇所・うち1箇所は渡している**。数え直したのは、hub が「これも伝聞です」と札を剥がした**後**の vault。
**私 → hub の日報 → vault と3艦を通って誰も数えなかった。**

🔑 **結論の一致は、理由の検算を免除しない。**「壊れる艦は無い」は当たっていたが、理由は逆で、
**その1箇所が 0.11.0 の意味へ既に載せ替え済み**だから（現物のコメントが `Said directly on the control since 0.11.0`）。
⇒ **「使われていない」ではなく「新しい意味で既に依存されている」。**

### 4. 齟齬の起票（前半）

- **`#378`** … `publish.md` が「描画が変わるのは3点」と名乗るのに表は4行。**表が正**。
- **`#379`** … 上記 3. の数字。
- **`#380`** … `--text-x-slot-button-sm-size` の既定 `inherit` が **md だけ上書きした艦で sm > md に反転**。
  **キットのコメントが3行のうちに自己矛盾**していた（`:365-366` で「md=13px / sm=12px の艦が実在する」と
  実測値つきで書き、`:367` で「今日は何も変わらない」）。
- **`#381`** … `check:exemplars` の `resolved` は**アンカーの実在しか示さない**のに green として扱われる。

⚠️ hub 経由の「`--spacing-x-slot-button-gap` は既定なし」は**誤り**で、キットは既定を配っていた〔実測〕。
後に hub から「**その断定は hub が中継時に足したもので、vault の PR には `既定 4px` と明記されていた**」と
訂正があり、`#380` の出所を直した。

### 5. `#381` タスク1（再現確認）— drift は 0 件ではなかった

規約 dir の `[X:` を全量列挙 **60件**、`#nene2-exemplar:` 形式のユニーク参照 **8本**。
**8本すべて anchor-present ＝ green**。うち **1本が drift**。

**条文 I18N-20** は `expectCatalogParity` を self-registering と規定し **`test()`/`describe()` で包むこと MUST NOT**
としているが、**その条文が自ら指す exemplar が `describe()` + `it()` で包んでいる**。
実装（`parity.ts:118`）は **`test()` を1つも登録せず throw するだけ**〔自艦実測〕。
⇒ **艦は実装に従って条文に違反しており、条文どおりに書くと CI の診断能力が落ちる。**

🔑 **出方が予想と逆だった**（コードが正しく、条文が動いていた）。⇒ **`resolved` は一致を一度も見ていないので、
向きに関係なく素通りする。** ⚠️ **同じ節は 07-21 に一度追随改訂されている**（1引数→2引数）——
**同じ節に同じ種類のズレが2つあり、片方だけ直った。** ⇒ **`#385`** を起票。

### 6. `#76` は成果物が既に出荷済みだった

`#381` の副産物。`@hideyukimori/nene2-i18n@0.3.0` の **npm の `exports` に `./testing` が在り**、
payout が実際に import して解決している〔実測〕。**自分では閉じず**、測定値だけコメントで置いた。
🔑 **会話メモリに残っている自分の事故と同じ issue**（2026-08-05 にも「未実装の証拠」として誤用）。
**2回とも探しに行ったのではなく別件の副産物**として出た。

### 7. 施主 GO の確認と PR（前半3本）

hub 経由で「4件とも施主GO」が届いたが、**この会話では未確認だったので施主へ直接確認**してから着手した。
（vault も同じ理由で hub の中継 GO を保留していたと後で判明。）
⇒ **`#382`**（`publish.md` の点数を消して表を正本に）・**`#383`**（`current.md` の数字と理由）・
**`#384`**（`slot-pairs.json` の配布＋片側上書きの検知＋`:367` を条件つきに）。

🔴 **`#384` の実装中に型4を踏んだ（誤り③）。** 検知器の正規表現に行頭アンカーが付いており、
`:root { --x: 1; }` の1行記述を取りこぼして**検査が空振り**していた。是正ごと PR に入れた。

### 8. 依頼の判定（vault → hub → 自艦）

**現物を読み、キット側は全部自分で測って**判定した。**5件とも、依頼元の申告より射程が広かった。**

| Issue | 依頼 | 判定 |
| --- | --- | --- |
| `#386` | `Button` に影のスロット | 🔴 **影はスロット化されなかった唯一の次元**。`Card`/`Toast` は直書き ⇒ **一括スロット化を推す** |
| `#390` | `Badge` に `className` | 🔴 **受けないのは 7部品**。ただし**一律ではない**（オーバーレイ3つは意図の可能性・`DataTable` は開けても解けない） |
| `#391` | `DetailList` の2カラム | 🔴 **vault 単独ではない。5艦8ファイル**（`field` が vault より多い）⇒ 受ける |
| `#394` | `Card` の `as` | 🟢 **`Text` に「閉じた union の `as`」の前例が在る** ⇒ 前例の延長 |
| `#393` | 部品→スロットの配布 | 🟢 **導出できる**（宣言122 ⇄ 参照122・孤児0）⇒ **整合検査にもなる** |

🔴 **`#393` の実測で型4を踏んだ（誤り④）。** glob を `.tsx` に限り、`src/lib/states.ts`（**`.ts`**）が読む
8本を **「誰も読まないスロット」と誤報**した。
🔴 **`#390` の最初の測定でも踏んだ（誤り⑤）。** `grep -q 'className'` で数え、**内部利用を拾って
`Badge` を 🟢 と誤判定**した——**窓が広すぎて別のものを数えた**形。

⚠️ **`#392`（`Modal`）は施主決定「キットへ載せ替えてキットを拡張する」を受けた起票**で、
現物を読んで**依頼外の齟齬を1件**見つけた: `onClose` の JSDoc が
「`Esc, the close control, the backdrop`」と名乗るが、**実際に効くのは Esc だけ**
（閉じるボタンは存在せず、native `<dialog>` は backdrop クリックで閉じない）。

### 9. 🔴 `#389` — README の旗艦例が存在しないスロットだった

`#386` の数字を測り直す過程で出た副産物。README §②「Slots — 🟢 redefine these」の**最初の例**
`--spacing-x-slot-card-pad` が**存在しない**。`Card` の padding は **`pad` prop** 由来。
⇒ **README のとおりに書いた消費者は、何も起きない。エラーも警告も出ない。**

🔴 **その2行上の「Every design value a component uses comes from a slot」が、この例によって偽になっている。**
🔴 そして **`readme.test.ts:110` が「README が文字列を含むこと」だけを見てこの例を固定している**——
**緑。しかも直そうとすると赤になる。検査が誤りを固定している。**

### 10. `#386` 実装 — キット自身のテストに止められた

施主裁定は **(b) 影の次元を一括スロット化**。スロット3本を足し、既定を**載せ替え前の描画の写し**にした
（`Button` = `none` / `Card`・`Toast` = `var(--shadow-sm)`）。

🔴 **`readme.test.ts` の「every namespace that has slots offers more than one step to choose from」が赤になった。**
**影のスケールは `--shadow-sm` の1段しかなく、そこへスロットを向けるのは 0.7.0 の radius 事故の再現**
（8スロットが唯一の値を指し、構造検査は全部緑なのに1つの角丸しか描けなかった）。**テストが正しく、実装が不完全だった。**

⇒ **影のスケールを1段 → 3段**。値は**フリートの実測から導出**（`--shadow-*` を定義している艦: sm 9 / md 9 / lg 7、
各段は offset・blur・alpha の中央値・`sm` と同じ単層）。**spacing と radius を決めたときと同じ手。**

⚠️ **`focus` は契約キーだが段を置かない**（キットは focus を `outline` で描く）。**採らない判断として明記した。**
**描画不変は Tailwind を実際にコンパイルして確認した**（陰性対照つき）。
⚠️ その検証で**能力の差**が1つ見つかった: スロット経由は `--tw-shadow-color` を経由しないので
**`shadow-<color>` で色を差し替えられない**。現状使っている部品は無いが記録した。

### 11. 🔴 版の取りこぼし（自分の誤り⑥）— 日報を書いた後に発見

マージ後に気づいた。**過去の `feat(ui)` / `fix(ui)` は題名に版を書き、同じコミットで `package.json` を上げている**
（0.10.0〜0.14.0 の5件で確認。`docs`/`chore`/`test` は上げない、も4件で確認）。
**`#384` と `#395` はどちらも上げていなかった。**

⇒ `package.json` が `0.14.0` のままだと **npm latest と同じに見える**のに、main には
**`themes/slot-pairs.json`（新しい配布物）と影のスロット3本・スケール2段**が入っていた。
🔑 **名乗り（版）が実体より古い**——本日 `#378` / `#389` / `#392` で追ってきた形が、**自分のリリース工程で出た。**

**`0.15.0` を1つ振り、`publish.md` に2件を含む理由を明記**した。
⚠️ **`0.16.0` を振って「0.15.0 は欠番」とはしていない**——欠番は「マージしたが publish しなかった」という
**実際に起きたこと**の記号であって、**取りこぼしの隠蔽に使う記号ではない**。
🔑 **番号を捏造して辻褄を合わせない。何が起きたかを書く。**
**lockfile は `check:lock` に止められた**ので追随（前後差分1行・パッケージ総数 646 で不変）。

### 12. マージと現況の更新

**PR 7本をマージ。** ⚠️ **型6 が実際に発火した**——1本目をマージした瞬間、残りが
「Required status check is expected」で弾かれた。**それぞれの緑は相手が入る前の緑**だったので、
1本ずつ `update-branch` → CI 緑を取り直して順に入れた。**`#395` は `#384` が入った後の main で緑を取り直した**
ので、**組み合わせが実際に走っている**。

**`current.md` を上書き**（`#398`）。**表に「`main` の版 ≠ npm の latest」という新しい状態が入った**——
これまで一致していたので、**食い違いを書く欄が無かった**。型4も書き直し、**「採らないと決めたもの」の節を新設**した。

### 13. 4パッケージの publish と、そこで出た34日ぶんの取りこぼし

施主 GO で `nene2-ui` 0.15.0 を publish（**dry run → 検証 → 本番**）。**shasum が dry run と一致**、provenance 署名つき、
git tag と GitHub Release も自動作成された。

そのあと **残り3パッケージに未 publish の差分があるか**を `docs/publish.md` の手順どおり測った。**3つとも在った。**

| パッケージ | npm publish | 最後の未リリース変更 | 放置 |
| --- | --- | --- | ---: |
| `nene2-tokens` | 2026-07-21 | 2026-08-05 | **34日** |
| `nene2-standards` | 2026-07-29 | 2026-08-23 | **26日** |
| `nene2-i18n` | 2026-07-21 | 2026-07-30 | **34日** |

🔑 **版は3つとも npm と一致していた。** `npm view` と `package.json` を見比べる確認は**この形を通す**。
**タグ以降のコミットを見て初めて出た**（`git log <pkg>-v<version>..HEAD -- packages/<pkg>`）。

🔴 **いちばん重いのは `nene2-standards`。** `#163` と `#164` の修正が **2026-07-30 にマージ済み・未 publish**
のまま26日経っており、⇒ **艦が 2.2.0 を入れている限り、その2件の欠陥は残っていた。**
（⚠️ **issue が open な理由が「未 publish だから」か「射程が commit より広いから」かは測っていない。**）

⇒ 3本の版上げ PR（tokens **1.3.0** / standards **2.3.0** / i18n **0.3.1**）を出してマージし、
**`tokens → i18n → standards` の順で publish**。順序は**依存を測ってから**決めた——
**宣言依存も `import` 文も4パッケージ間で0件**で、`standards` の `FLEET_PACKAGES` は
`require.resolve` が失敗したら `null` を返すので**順序制約は無い**。

**i18n を patch にした**のは、**`0.4.0` が `#171`（`./format` 供給計画）に予約済み**だから。
🔑 **番号は、それを指している文書の側にも属している。**

### 14. 🔴 floor を上げようとして裁定に止められた

publish の後、`fleet-baseline.json` の floor を追随させるのが自然に見えた（4つとも floor < npm）。
**測ったら `#271` に hub 裁定（2026-08-15）があった** —— **(b) 据え置き**。

> 上げても**実効ゼロ**（floor を機械判定する器が無い）／**実艦の6割が現 floor すら満たしていない**
> ⇒ **上げるべきは floor ではなく、各艦の実採用版。**

⇒ **上げなかった。** `current.md` の「採らないと決めたもの」へ理由ごと移した。
🔑 **publish の「自然な後片付け」に見えたものが、既存の裁定に反していた。**
**裁定が書かれていなければ、そのまま上げていた。**

### 15. 型6 が3連発した

版上げ3本が **`docs/publish.md` の同じ位置に節を足す**ため、**`gh pr update-branch` が
「Cannot update PR branch due to conflicts」で失敗**した（2回）。
⇒ ローカルで `git merge origin/main` → **衝突を「両方の節を残す」で解消** → `check` を通してから push。

🔑 **自動更新が使えない衝突は、手で中身を見るしかない。** 機械的に片方を採ると**もう片方の版節が消える**
（＝`publish.md` が版を持たない版を作る）。**3節が全部残っていることを実測で確認した。**

### 16. hub の周知を止めていた判定（`#163` / `#164`）

hub が**フリート全艦への周知をまだ1通も出しておらず**、「`#163`/`#164` が open な理由が
**未 publish だからか、射程が commit より広いからか**」の判定を待っていた。**読んで答えた。**

**どちらも「未 publish だから」ではなかった。** 両方とも PR の trailer が **`Refs`（`Closes` ではない）**で、
**当時の作者が意図的に open のままにし、残スコープを issue コメントに明記していた**〔実読〕。

| | 残っているもの | publish で解けるか |
| --- | --- | --- |
| `#163` | 🔴 **`nene2/style-prop-css-vars-only` は未修正**。断片を「Tailwind 依存部／非依存部」に分割する要あり（**配布 API の変更＝hub 裁定が要る**） | **解けない** |
| `#164` | タスク2（`postcss-html` 追加）・タスク3（Wave 表の再実測）が**「standards 2.3.0 publish 後」に置かれていた** | 🟢 **ゲートは外れた**（艦の作業ではなく自艦の作業） |

🔑 **「issue が open」と「修正が届いていない」は独立だった。** 両方 open だが、
**片方は publish で解け、もう片方は publish と無関係。** ⇒ **同じ「open」でも、周知に書くべきことが逆になる。**
⚠️ **非 Tailwind 艦（concierge / serve / corpus / suite）には赤が1行残る**ので、
**「2.3.0 で全部消える」と読ませないこと**を周知の要点として hub へ返した。

### 17. `Closes` の監査（`#410`）— vault と同型は無く、別の形が在った

vault が「PR に `Closes` を書き忘れて issue が open のまま残っていた」件を報告したので、自艦を測った。

| | 件数 |
| --- | ---: |
| merged PR | **174** |
| 🟢 **`Closes` 宣言されたのに open な Issue** | **0件** |
| ⚠️ **`Closes` 行を持たない merged PR** | **39件**（22%） |

🟢 **宣言された分は毎回自動クローズが効いている** ⇒ **vault と同型の欠陥は無い。**
⚠️ 在るのは「宣言していない」ほうで、**5つの open Issue に実装 PR が在る**。
🔴 **ただし「`Closes` が無い＝書き忘れ」ではない**——**1つの Issue を複数 PR で実装するときは、
1本目に書くと早すぎるクローズになるので書かないのが正しい**（本日の `#403`/`#404`/`#405` がまさにそれ）。

### 18. 🔴 陽性対照だけでは足りない — **窓を3回外し、3回とも陽性対照は通っていた**

`#410` の計測で窓を切り直した:

| 窓 | 結果 | 何が起きたか |
| --- | ---: | --- |
| 題名の `（#N）` 厳密 | 3件 | **偽陰性** — `（#164・Wave 表…）` を落とす |
| 題名に `#N` 緩い | 12件 | **偽陽性** — 「0.4.0 は **#171** に予約済み」を拾う |
| 題名の**末尾括弧** | 11件 | **まだ偽陽性** — 末尾括弧に `#402` と `#171` が同居 |
| **本文の `Closes #N`** | **確定** | **GitHub が実際に使う機構そのもの** |

🔑 **3回目に気づけたのは、陰性対照（`#171` は落ちるべき）を置いていたから。**
**陽性対照（`#386`）は3回とも通っていた。** ⇒ 🔑 **偽陽性は、陽性対照では絶対に捕まらない。**

⇒ 運用形を **「既知の陽性を1件と、落ちるべき既知の1件を通す。どちらか片方でも外したら道具を疑う」**へ改めた。
⚠️ **`#206`（陰性対照を取らない N 回 green）は自艦に既に在った**——**規律は持っていて、測定器に適用していなかった**
（本日2件目）。**`#206` は「検査の陰性対照」、今回は「測定器の陰性対照」で、適用先が違うだけ。**

### 19. `Modal` の実装（`#392`）と 0.16.0 の publish

施主 GO で着手。**任意 prop を4つ**（`header`＋`closeLabel` / `size` / `sheetOnMobile` / `scrollable`）。
**4つとも既定は 0.15.0 までの描画と同じ**で、**既定の側をテストで固定した**
（`#380` で「既定は無害」が片側上書きで崩れたのと同じ罠を避けるため）。

🔴 **`closeLabel` は `header` を渡したときだけ必須**（判別ユニオン）——**キットが英語の既定文字列を配ると
艦の UI に英語が出荷される**（I18N-2）。**必須 prop は増やしていない。**

**幅は Tailwind の段を選んだ**（値を発明していない）。フリートの dialog は **560px が最頻（15）**で
640(6) / 680(4) / 600(4) / 420(4) / 520(3) / 460(3)〔各艦の `frontend/src` を実測〕。

🔴 **高さ上限は入れられなかった。** `80vh` を入れたら **`readme.test.ts` の
「every slot default is built only from scale references」が赤にした**——キットは**全スロットに
スケール参照を要求**していた（自分の想定より厳格）。⇒ **`showModal()` した dialog は UA スタイルが既に
ビューポートで頭打ちにする**ので、**足りないのはスクロールのほうだけ**と判断し、`scrollable` をその形にした。
⚠️ **UA の上限は jsdom で検証できない**ので **live レーン**へ。🔑 **直せない場所で緑にしない。**

**Tailwind を実際にコンパイルして新クラス13本の生成を確認**（陰性対照つき）。
⚠️ 途中で `max-sm:` の3本を「生成されず」と誤判定したが、**原因は自分の正規表現**だった。
🔑 **今回は結果を疑う前に道具を疑って、その場で捕まえた**（本日5回目の型で、**初めて即座に**）。

### 20. `Pagination` の分類を訂正した（`#390`）

vault が `Pagination` を「`className` 待ち」に分類していたので、**現物を読んで突合**した。
**挙げられた不足6項目のうち `className` で解けるのは2つだけ**〔実測〕:

| 不足 | `className` で解けるか | 要るもの |
| --- | :-: | --- |
| 行の枠 / `leading-inherit` | 🟢 | 外側 `<nav>` に届けば足りる（`line-height` は継承） |
| **`size="sm"`** | 🔴 | **キットは `Button` に `size` を渡していない** ⇒ prop |
| **`total === 0` の非描画** | 🔴 | **常に描画する** ⇒ 振る舞い |
| **モバイルの縦積み** | 🔴 | **`direction="horizontal"` 固定** ⇒ 構造 |
| **`showingLabel` の配置** | 🔴 | **中央は `status` 1枠のみ** ⇒ 構造 |

⇒ **`Pagination` は `#390` の③群（受け皿の不足が prop 1つではない）**で、`DataTable` と同じ側。
🔑 **`className` を開けると「対応した」ように見えて、6件中4件は残る。**

💡 vault の自己分析が効いた: **待ち条件が解けたことと、載せ替えられることは別。
在庫表は「その時点で気づいた不足」であって、不足の全数ではない。**
⚠️ **`#390` の7部品の一覧も同じ性質**——`Pagination` がその実例になった。

## 📊 本日の数字

| | 値 | 出所 |
| --- | ---: | --- |
| **マージした PR** | **15本**（#382 #383 #384 #388 #395 #397 #399 #401 #403 #404 #405 #407 #409 #411 …） | 自分 |
| 起票した Issue | **21本**（`createdAt` で実測） | 自分 |
| クローズされた Issue | **12本**（`closedAt` で実測） | 自分 |
| 測定値をコメントで置いた Issue | **1本**（#76・**状態は変えていない**） | 自分 |
| **publish した版** | `nene2-ui` **0.15.0 → 0.16.0**（2回）／ `nene2-tokens` **1.3.0** / `nene2-standards` **2.3.0** / `nene2-i18n` **0.3.1** — **5本とも provenance 署名つき・dry run と shasum 一致** | 自分 |
| テスト | **736 → 764** passed（46 → 49ファイル） | vitest 実行結果 |
| `npm run check` | **EXIT=0**（8ステップ・main で最終確認） | 自分で実行 |
| open Issue | **64 → 73件** | 総数を測定 |
| `[X]` 参照 | **60件** / 解決対象 **8本** / anchor-present **8本** / **drift 1本** | 自分 |
| スロットの次元 | **6次元 124本**（color 72 / spacing 29 / radius 8 / text 8 / font-weight 5 / brightness 2）／**shadow は 0 → 3** | 自分 |
| **未リリース差分**（`git log <tag>..HEAD`） | **3パッケージ・最長34日 → 全部 0件** | 自分 |
| **自分の誤り** | **8件**（時刻の捏造・無検算の中継・版の取りこぼし・**型4 ×5**——行頭アンカー / 手書き列挙 / 拡張子 / `grep -q 'className'` / `max-sm:` の正規表現） | 自分 |
| hub へ送ったメッセージ | **未計測**（数えていない） | — |
| vault の本番の影・寸法の実測値 | — | **vault 申告（伝聞・自艦では未測定）** |
| hub の reconcile 12件 / precision 0/12 | — | **hub 申告（伝聞・自艦では未検算）** |

## 今日の型

🔑 **名乗りが実体より広い／古い、が5件出た。** `publish.md` の点数（`#378`）／README の旗艦例（`#389`）／
`onClose` の JSDoc（`#392`）／`resolved`（`#381`）／**版**（`#396`）。**最後のは自分が作った。**

🔑 **結論が当たっていると、理由は検算されない。** 3件出て、3件とも結論は正しく、3件とも理由が違っていた。

🔑 **道具の窓を「干し草の性質」で切ると、静かに外す。** 1日で4回。**切った窓はどれも干し草の性質**
（行頭にあるか／接頭辞の綴り／拡張子／その語が在るか）で、**探しているものの性質ではなかった**。
⇒ **代理を検査して本体を検査しない、が道具の側に出た形。型を知っていることは防御にならない。**
**対策**: ①**既知の陽性を1件通す**（3件目の陽性は数十分前に自分で読んでいた） ②**新しい道具が出した驚きは、
発見より道具の欠陥である確率が高い** ③**列挙をやめて導出する**。

## 申し送り

- 🟢 **4パッケージとも publish 済み**で、**未リリース差分は 0件**〔02:2x 実測〕。
  `nene2-ui` は **0.16.0**（`Modal` の4点）。
- 🟢 **vault の `#415` を塞いでいた上流の栓は抜けた。** キットの `Modal` は native `<dialog>` ＋
  `showModal()` なので **focus trap と Esc はブラウザが持つ**。⚠️ **UA の高さ上限は jsdom で検証できない**ので
  **vault の live レーン**で見てもらう。
- ⚠️ **`#413`（`current.md` の 0.16.0 追随）は未マージ。**
- 🔴 **`fleet-baseline` の floor は4つとも npm より低いが、これは上げ忘れではない**（`#271` の hub 裁定＝据え置き）。
  **解除条件は「対象艦がすべて追いついた時点」。** `current.md` の「採らないと決めたもの」に理由ごと置いた。
- 🔴 **艦への周知が要る。** とくに `nene2-standards` は**26日ぶんの修正が届いていなかった**ので、
  **上げると挙動が変わる艦がある。**
- **未着手の Issue**: `#381`（**予防→現物ありへ格上げ**）・`#385`・`#389`・`#390`・`#391`・`#392`・`#393`・`#394`／`#76` のクローズ可否。
- ⚠️ **`#393` は hub の台帳粒度の結論待ち**（明日 hub が形を決める）。**GO が出ても粒度は固めない**取り決め。
- **`#392` は影が抜けて4点に縮んでいる**（可視ヘッダ＋閉じるボタン / `size` / ボトムシート / 高さ上限）。
- **未測定**: `_work/issues.md #119`（別ビルドを測った差分表が全行真だった件）の射程が自艦の `check:*` にも
  当てはまるか。hub は「急がない」。
