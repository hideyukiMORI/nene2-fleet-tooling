# 2026-08-04 — work order の消化と、配る前に測ったこと

> **対象期間**: 2026-08-04 夜〜深夜の1セッション（`/repo-status` の起動から hub の店じまい確定まで）。
> 日中の作業は含まない。前回の記録は `2026-07-31.md`（PR #219・**未マージ**）。

hub が 19:06 に発令した work order を、起動時の指示書チェックで拾うところから始まった。relay 8834 が不達で指示書方式へ切り替わっていたため、発令から約8時間、誰も着手していなかった。項目は3つ（横断調査の配布・backup 検証の標準化・規約質問）。このうち最も時間を使ったのは**配る前に実測したこと**で、結果として配布対象は3艦から1艦に減った。board と work order が「残り3艦」としていた根拠は `package.json` の `check` だけを見た grep で、CI workflow を見ていなかった。今日この repo でマージされた PR は無い。

## ヘッドライン

- **配布対象が3艦→1艦に減った。** clear と payout は CI で本番 build が走っていた（fleet 実測）。真の穴は vault のみで、nene-vault#352 を起票。hub が board 行78 を訂正し、#210 を正本に確定した。
- **規約質問への回答から、規約と配布物の食い違いが1件出た。** 05 §4 が「配布実体」として書く `nene2-standards/depcruise` が存在せず、**艦側も1隻も配線していない**。05 §4 の禁止ルール群が艦隊のどこでも機械強制されていない状態だった（#221）。
- **serve から #161 の同意が返り、PR #223 を出した。** 外したのは注記1項目のみ。誠実性の中核（fleet 未再実測・EX-x は hub レビュー未了・族名は仮）は残した。CI 緑・hub レビュー PASS・**施主マージ待ち**。
- **「3点セット」は3点ではなく2種類・5段だった**（#224）。`--no-tablespaces` は検証ではなく原因除去。

## 本日の成果（時系列）

### 1. 起動時 — 未受領の work order を拾う

`ls -t _work/handoff-fleet-tooling-*` で 19:06 発令の work order を検出。relay 8834 不達により指示書方式へ切り替わっていた。項1（check-build 横断配布・board due 08-05）・項2（backup 検証3点セット・due 08-08）・項3（規約質問）。

あわせて repo の実態を実測した。`npm run check` は **exit 0 / 35 files・517 tests**、AM-2 release gate PASS、conformance は red 0 / unknown 14 / green 0。`docs/todo/current.md` の記載（35 files・517 tests・main 緑）は**追試して一致した**。最終コミットは 07-31 22:31 で、4日間コミットが無かった。

### 2. 項3 — 規約質問への回答（hub へ relay）

問い: hooks→model の射程は `features/` のみか、`pages/<page>/hooks/` も対象か。

**回答: pages も規約射程内。** 根拠は `01-architecture.md:99`（「スライス内（**pages** / features / entities）のサブディレクトリ名は5語のみ MUST・`hooks/` は MUST NOT」）と、depcruise 規範仕様 `05:636` の正規表現が第1セグメントを固定していないこと。hub が根拠として挙げていた `05:916` は payout の現物注記であって射程の条文ではない、という点も添えた。

一方 **道具の射程は features 固定**だった（`applyA1()` が `path.join(srcRoot, 'features')` をハードコード・`move.ts:108`）。records の該当1件は手作業が最安と推した。hub 裁定は「CLI 改修は発注しない・3本目の小 PR で拾わせる」。

### 3. 項3 の副産物 — depcruise が配布されていない（#221）

回答を書く過程で、規約 05 §4.1 が製品側コピペ正本として載せている `require('@hideyukimori/nene2-standards/depcruise')` が**実行不能**であることに気づいた。2.2.0 の exports は `.` / `./stylelint` / `./stylelint-plugin` / `./migrations/hooks-to-model` の4つのみ。

hub 指示（「#69 に含まれるか実測し、(a) コメント か (b) 独立起票 のどちらかへ今夜中に着地」）に従い、#69 の本文41行を実読。**陽性対照**として「検査器」で grep すると 5 hits、depcruise / dependency / cruiser / セグメント / boundaries / fsd / 循環 / 孤児 / orphan / no-circular はすべて 0 hits。射程が違う（#69 は SHOULD へ格下げされた条項の復帰台帳／本件は MUST のままの条文の強制器欠落）と判断し **(b) で起票**。

さらに艦側も測った。`.dependency-cruiser.*` を持つ艦 **0**・`nene2-standards/depcruise` の参照 **0**・`dependency-cruiser` を持つ package.json **0**。陽性対照は `eslint.config.*` **23件** / `nene2-standards` を含む json **27件**で探索は機能していた。つまり「配布されているが壊れている」ではなく**一度も配線されたことがない**。

hub 裁定: 版は 2.3.0 minor・**reportOnly-first**・#163 とは独立。pilot は payout（範艦・陽性対照）→ records（A1' 完了後）で、fleet の提案により **serve（非 Tailwind 対照）を3艦目に追加**して3点測定になった。

### 4. serve からの #161 同意返答と PR #223

07-31 に発行した指示書を serve が起動時に拾い、hub 経由で回答。族編入と `EX-x` 採番に同意。あわせて Q2（複数テーマ艦での宣言側集合の取り方）に4点の踏み所が返ってきた。

反映は **PR #223**（issue #220）。§1.3 に同意を追記、§7 の「確認は未実施」を取り消し線＋同意取得へ、§6 の「未起票」を #222 へ、**§2 の移植注意を1文から4点へ差し替え**。

注記は1項目しか外していない。serve 自身が「EX-x の hub レビュー未了はそのまま残る理解」と明示していたため、①fleet 未再実測 ②EX-x は hub レビュー未了 ③族名は仮 の3つは残し、§7 に「同意が取れたのは仮 ID を変える提案についてだけ」と書き足した。serve から後日「§7 の残し方はこちらの意図どおり」と返信があった。

serve から渡された数字（宣言 **135**・main=`73d072e`・#175 で分割確定）は **fleet では再実測していない**。§2 の既存の「宣言 58」は PR #197 時点なので、上書きせず両方に時点を付けて併記した。

Q2 の受け皿として **#222** を起票。serve から「#175 の分割前後を実験台にする」申し出があり検収設計に入れた。後日 serve から「#175 は日程未定・最優先は #199 → #200」と現況が届き、#222 に追記した（検査器のほうが先に間に合う公算だが、それを理由に着手を遅らせない旨も併記）。

### 5. 項1 — 配る前に測ったら、対象が1隻に減った

work order は「残り = vault / records / clear / payout」としていた。配布前に3艦を実測した結果:

| 艦 | `check` に本番 build | CI workflow に本番 build | 判定 |
| --- | --- | --- | --- |
| payout | 無し | **有り**（`ci.yml:86`） | 塞ぎ済み（#267 のとおり） |
| clear | 無し | **有り**（`frontend-ci.yml` の Build ステップ） | CI では走っている |
| **vault** | 無し（`build-storybook` のみ） | **無し**（build 実行行が 0） | **真の穴** |

board 行78 の「build 系なし = clear / payout」は `package.json` の `check` だけを見た grep 由来で、CI workflow を見ていなかった。3艦のうち2艦が偽陽性だった。**これは #210 が扱っている教訓そのものの再演**で、「`check` に build が無い」と「CI が本番 build を走らせない」は別のレイヤーである。

配布は **nene-vault#352** の1本。vault の外形実測（stories **7本** / コンポーネント **39本**・`vite.config.*` に `cssMinify` 記述なし・CSS 5本で `themes/default.css` を持つ複数テーマ構成）を添え、board 行78 の但し書き（**invoice 実測を一般化しない／共有できるのは対策ではなく測り方**）を本文に写した。受入は「対策より先に ①cssMinify ②stories 網羅 ③負テスト を vault 自身が測る」形にした。

clear の線引き（CI にはあるが `check` に無い）について hub 裁定は「出荷ゲートの正体は CI・配布対象外が正しい。ただし **DX の穴**として層を分けて EX-6 に一行記録すること」。**#225** として起票（doc PR は明朝）。

### 6. 項2 — backup 検証の条件付き MUST（#224）

条文の強さは「backup を取るなら MUST」の条件付き MUST（fleet 推し → hub 採用）。

参照実装（`_work/assets/ayane-wp-band-2026-08-04/apply-ayane-wp.php` L72-95）を実読したところ、**「3点セット」は3点ではなく2種類・5段**だった。`--no-tablespaces` は PROCESS 権限を不要にする**原因除去**であって検証ではない。このまま配ると「検証を1つやった」と数える艦が出るため、条文は **rc / 外形 / 中身 の3層＋負テスト MUST**、原因除去は SHOULD で別立てとした。handoff テンプレ組み込みは案A（条件で発火するチェックリスト＋JSON 機能リスト方式と接続）を推し、hub が承認した。射程実測は**まだ行っていない**（EX-5 に従い条文配布の前に置いた）。

### 7. records 発の A1 codemod 穴2件を裏取りして収蔵（#226）

hub 経由で records #1044 の実測が届いた。fleet で `transform.ts` を実読して裏取りした。

- **② `vi.mock('...')` を書き換えない** — transform が訪問するのは `ImportDeclaration` / `ExportNamedDeclaration` / `ExportAllDeclaration` の3種のみ（`transform.ts:75-77`）で **`CallExpression` を訪問していない**。`vi.mock` / `jest.mock` / dynamic `import()` / `require()` はすべて対象外。型検査で検出できず、**モックが黙って外れて実物が走る**。「別のものをテストしている緑」になりうる。
- **① 深い相対 import** — `moveKeyOf()` の正規表現2本を読むと、正確には「深い相対が全部だめ」ではなく「**スライス名を挟まない深い相対（`../../hooks/`）がだめ**」。`../../features/foo/hooks/use-x` は書き換わる。

hub の定式化「codemod は『import 文』の道具であって『モジュールを指す文字列』全部ではない」を採り、**範艦の選び方の教訓**（payout で露見しなかったのはネスト `ui/` と `vi.mock` が無かったから＝範艦が最も簡単な艦だった）を #221 の pilot 設計へリンクした。消費者残ゼロのため**コード修正はせず doc 注記に留める**判断を fleet 手番で置いた。

### 8. repo-status で拾った齟齬の出口

- **#169 に再実測コメント** — 起票時の (b)「リモート15本」は解消済み（`git ls-remote` で origin の実体は3本）。一方ローカル stale が **3本→15本**（PR state で1本ずつ照合・全て MERGED）、remote-tracking の未 prune ref **20本**、merged 済みブランチを pin した**置き土産 worktree 1件**。auto-delete head branches ではローカルは消えない、という学びを追記した。**削除は実行していない**（施主承認案件）。
- `git branch -a` をリモートの実態として読むと嘘になる点、squash-merge されたブランチは `merge-base --is-ancestor` では検出できない点を、測り方としてコメントに残した。

## 📊 本日の数字

**fleet が自分で実測したもの**

| 項目 | 実測値 |
| --- | --- |
| `npm run check` | **3回実行・すべて exit 0**（35 files / 517 tests・AM-2 PASS・conformance red 0 / unknown 14 / green 0） |
| 起票 | **8本**（#220 #221 #222 #224 #225 #226 #227 / nene-vault#352） |
| PR | **1本**（#223・CI 緑・**未マージ**） |
| issue コメント | **5本**（#169 / #210 / #221 ×2 / #222） |
| relay 送信 | **6通**（hub 4 / serve 2・すべて `ok`） |
| 本日マージされた PR | **0本** |
| depcruise を配線している艦 | **0隻**（陽性対照: eslint.config 23件 / nene2-standards を含む json 27件） |
| 本番 build が check にも CI にも無い艦 | **1隻**（vault・3艦実測） |
| ローカル stale ブランチ | **15本**（remote-tracking の未 prune ref 20本・origin の実体 3本） |

**他艦・hub から渡された数字（fleet では再実測していない）**

| 出所 | 数字 |
| --- | --- |
| serve | 宣言 135・main=`73d072e`・#175 で分割確定・最優先は #199 → #200 |
| records | A1' の codemod 穴2件（#1044）・深い相対 14参照/3file |
| deal | npm audit 新規 advisory 2件を #200 で検出・両方 dev-only |
| board 行78 | 「build 系なし = clear / payout」→ **fleet 実測で偽陽性と判明・hub が訂正済み** |

## 申し送り

明朝の入口は次の順。

1. **npm audit 横断（board 行101・due 08-05）** — 新規 advisory 2件で全艦 frontend CI が赤の公算。deal が (A) 先行 allowlist、fleet は (B) 全艦の lockfile 世代確認＋deal の条文を参照実装として配布（**各艦は自艦で実測を取り直す＝写経禁止**）。指示書 項4 と board 行101 の実在は確認済み。
2. **#225 の doc PR**（EX-6 の層区別・hub 合意済み）。
3. **records への項1 配布**（A1' の2本が閉じてから）。
4. **#224 の射程実測**（艦隊横断で backup を取る箇所を数える・条文配布より前）。

施主手番: **#213 / #219 / #223 のマージ**（すべて CI 緑。#213 が閉じないと tokens 1.3.0 の publish 準備が動かない）・**2.3.0 publish 判断**（#221）・**#169 のローカル15本の削除承認**。

出口の決まっていない齟齬は無い。
