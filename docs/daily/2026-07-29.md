# 2026-07-29

7日ぶりに立ち上がったら、**私宛の引き渡しが 03時に不達で止まっていた**。board 72行「fleet 規約還流バッチ」が due 2026-07-28 を超過したまま、handoff 2本が誰にも拾われずに置かれていた。まず `/repo-status` を一巡して自リポの実態を測り、見つけた齟齬を全件 issue か relay へ落としてから、還流バッチの fleet 手番ぶんを処理した。条文化した規約が**その日のうちに自分に適用される**場面が2回あり、どちらも「再実行したら通ったので流す」ところで踏みとどまって測り直した。夜になって他艦から還流と照会が5件届き、そのすべてに出口（issue）を作った。**自分の誤りを3回、うち2回は相手に届いた後に自分で見つけて訂正した。**

## ヘッドライン

- **規約還流バッチの fleet 手番ぶん完了** — PR **#157**（mori-site 由来6件）・**#158**（contact 由来4件）を**マージ**。条文へ整形し規約 v1 の収載先（章・節）を割り当てた。
- **規約 v1 の章立ての穴を2艦の独立した要求から特定** — 横断的なテスト章が無い。統合リナから新章 `06-testing-verification.md` の**方向承認**（実施は契約v1凍結後）。
- **`/repo-status` の齟齬4件を全件着地** — うち後片付け（#153）は施主承認を得てその場で実施。
- **他艦からの還流・照会 5件に出口を作った**（#161〜#165）。**うち1件は fleet 側の実装欠陥として自分の issue になった**（#159）。

## 本日の成果（時系列）

### `/repo-status` 一巡と、その出口

- **#152 起票（オープン）** — README の版・座席・W0a 状態が実態から系統的に乖離。standards が「npm 公開済み 1.0.1」表記のまま実 **2.1.1**（メジャー1つぶんのずれ）、i18n「0.1.0」→実 0.3.0、`fleet-baseline` 行「tokens/standards/i18n は null=未発効」→実際は4席全充填。**#55 の再発**（07-16 に一度是正して13日で再発）だったので、再発防止の2択まで論点に入れた。統合リナ裁定 = **(ii) 版表を README から落として `fleet-baseline.json`＋`npm view` を正とする**。〔版は `npm view` で自分で実測〕
- **#153 起票 → 実施 → クローズ** — 施主承認（「全部やる」）を得て後片付け。`git branch -r` **60本超 → 16本**（`git ls-remote` の実体と一致）、ローカルブランチ **42 → 15本**（差集合でリモート不在の27本のみ削除・**復旧用 SHA を全件記録**）、残置 worktree 2本削除、`main` を ff で `e10c596` へ。**リポの追跡ファイルは1バイトも変更していない**。〔すべて自分で実測〕
- **`npm ci` で手元環境を是正** — `node_modules` が 07-16 のままで `jsdom` 欠落。**最初に報告した「426 passed」は過小計上**で、正しくは **31/31 ファイル・438 tests**（07-22 日報の 418 からの増分は +8 ではなく **+20**）。〔自分で実測・訂正〕

### 規約還流バッチ（board 72行・due 07-28 超過）

- **#154 / PR #157（マージ済 `00bd31d`）** — hideyuki-mori-site 由来6件。①`@container`→`03 §6.5` ／②checked-replace→`05 §6.7` ／③`data-hm-id`→`01 §3.4` ／④`:target` オーバーレイ→`03 §6.6` ／⑤型の運用規則（停止条件込み）→`05 §0.3` ／⑥規約の届け先→`01 §0.4`。
- **#155 / PR #158（マージ済 `944ab0a`）** — nene-contact 由来4件。①時計注入（テスト側＋実装側 D4・相互参照）／②非同期リストの待ち方／②'新クエリ追加時のスタブ→`05 §6` チェックリスト ／②''2回直らなければ実装を疑う ／③pin の期限切れ→`05 §5.6` ／④CSV 式インジェクション→`02 §9.5` 新設。
- **規範の強さを原文から変えた5箇所を明示列挙した**（mori-site 3・contact 2）。統合リナから「正しい作法」との評価を得て、**起草2艦へ FYI relay して確認を取った**。両艦とも回答あり:
  - contact: **2箇所とも異論なし**。
  - mori-site: 2箇所は同意。**①の MUST には例外条項を足すよう要求**——`container-type: inline-size` はインライン軸のサイズ contain を課すので `@container` 化できない設計が実在し、**逃げ道の無い MUST は黙って迂回される＝それ自体が⑤の「意味の違う正常値」になる**、という指摘。**反映してから**マージした。
- 🔴 **帰属の誤りを1件、起草側から指摘されて訂正した** — ⑤の「270日/14通・93%欠落」は **records リナの測定**であり mori-site の測定ではない。ドラフトの表では正しく帰属されていたが、私が根拠節へ数字だけを再掲したときに潰れた。**帰属は明記されていても、引き写しの過程で消える。**
- **contact から③に追加条文（③'）** — 07-29 夜に追加で踏んだ事象。flat な `^5.0.8` 集約で `minimatch@3` に v5 が入り `TypeError: expand is not a function`、しかし `npm run check` は**全部緑のまま**だった。→ **major ごとの scope・全経路プローブ（hoisted だけでなく nested も走査）・監査ゲートと同じ場所での実行**を MUST 化して収載。〔contact 実測・私は再実測していない〕

### 条文が、その日のうちに私に適用された（2回）

- **#156 起票（オープン）** — `docs/seam/recover-auth-v1.test.ts` の型検査テストがタイムアウト。**再実行したら通ったので流しかけた**が、いま条文化している当のもの（contact ②''「直後の1回で判断しない」）に反するので測り直した。**`npm run check` 経由で 4回中2回（50%）・`npm test` 単体で 4回中0回**。原因は構造的で、テストプロセス内で TypeScript コンパイラを起動しており（負荷なしで 1812ms＝既定 5000ms に対し余裕 2.7倍）、`check` の直列実行で 5秒を超える。**CI はまだ緑だが「まだ落ちていない」は「落ちない」ではない。**〔自分で実測〕
- **NENE2 リナの closing keyword 問題（#160 起票）** — PR 本文の `Closes #NNN` をバッククォートで囲うと Issue が閉じ残る。指摘を受けて**自分の PR 2本を即実測**（該当なし）。起草側は「条文だけで足りる」と見ていたが fleet は「条文＋軽量検査」を推した。その後**起草側が同日に2回目を踏んで前言撤回**（提案・報告・日報記載・対策提案の**後**に再発）。#154 ⑤ の「意識して対策している人が、その対策のコードの中でもう一度踏む」がそのまま再現した実例として #160 に追記。

### deal からの照会 → fleet 側の実装欠陥の発見

- **deal リナが `nene2/data-theme-selector-location` の正本条文を照会**（hub 経由で「fleet が正本を持っている」と案内されたため）。**推測で答えると条文として引用されてしまう**ので実装を読んで確定した:
  - ルール実装は**場所を一切見ていない**（`[data-theme` を含む全セレクタを無条件 reject）。許容は**共有 config の override** で `**/src/shared/ui/theme/themes/!(*.components).css` の rule を null 化することで与えられる。
  - グロブの罠3点（`*.components.css` は許容されない／`themes/` 直下のみ／`app/design/` はどうやっても通らない）を添えて回答。
- 🔴 **#159 起票（オープン）** — その過程で**実装の欠陥**を発見。`themes-token-only` は `additionalScopeSelectors` を受け取れ、プラグインのコメントも registries の `scoped-theme` kind から来ることを前提に書かれているのに、**`stylelintConfigFromRegistries()` が `scoped-theme` を一切読んでいない**。`dist` を直接叩いて実証:

  ```
  registries 内の scoped-theme エントリ: 3（corpus / concierge / records）
  合成 config に additionalScopeSelectors が現れるか: false
  合成 config に ".nene-public" が現れるか        : false
  ```

  **registries に登録しても効かない。** deal の C5 の残り8件のうち **6件（複合セレクタ）がこれで詰まる**（残り2件は移動で解ける、と回答済み）。records の `.nene-public[data-theme]` も登録済みなのに効いていない。〔合成が出さないことは自分で実測。records が実際に赤かは**未計測**〕

### 他艦からの還流・照会に出口を作った

- **#161（オープン）** — serve 由来3条（TC-x トークン参照の解決性／DR-x「記録は出口ではない」／DA-x 時限例外の解除条件）。3条とも「導入 ≠ 実行」の別の面。
- **#162（オープン）** — invoice 由来5節（E2E 移設で ESLint から静かに外れる／@smoke の3層定義／ピンは時限措置／参照実装の移植で lint が壊れる／stacked PR）。
- **#163（オープン）** — concierge の gate-integrity red の扱い。**「11艦中 非 Tailwind が concierge だけか」の分布は fleet にしか測れない**ので横断実測した → **非 Tailwind は concierge / serve / corpus / suite の4艦**。concierge 自身が示した判定基準（1艦なら差異登録・複数なら canonical）に当てはめて **canonical 側で表現すべき**と回答。〔自分で実測〕
- **#164（オープン）** — `enumerateStyleSources()` が HTML 埋め込み `<style>` を原理的に見ないのに `scan-coverage` が green を返す（**G-6 の現物**）。Wave 表の concierge 行 218→487 の訂正も同枠。〔数値は concierge 実測・**私は再実測していない**〕
- **#165（オープン）** — 生成物ドリフト検知 CI ゲート（D-2）。**射程の横断実測は未着手**。「射程が決まる前に参照実装を配らない」判断は統合リナから承認済み。
- **concierge の「二重ベースライン」照会に判定を返した** — eslint-suppressions（TS 債務）と registries.jsonc の lint-baseline（CSS 債務）は**同じ違反を2箇所で数えていない**ので該当しない、2本立ては標準形、と回答。ただし前提確認（registries.jsonc に eslint 由来のエントリが入るなら話が変わる）を付けた。

## 📊 本日の数字

- **マージした PR**: **2本**（#157 / #158・いずれも施主承認後に self-merge）〔`gh` 実測〕
- **起票した Issue**: **13本**（#152 / #153 / #154 / #155 / #156 / #159 / #160 / #161 / #162 / #163 / #164 / #165 / #166）。うち **#153 は同日クローズ**（実施完了）、#154 / #155 は PR マージで自動クローズ。〔`gh` 実測〕
- **open issue**: 本日終了時点で **35本**〔`gh` 実測〕
- **test**: `npm run check` **exit 0** ／ `AM-2 release gate: PASS` ／ **31/31 ファイル・438 tests pass**〔自分で実測〕
- **後片付け**: remote-tracking **60本超 → 16本**、ローカルブランチ **42 → 15本**（削除27本・SHA 全件記録）、worktree **4 → 2本**〔自分で実測〕
- **横断実測**: `frontend/` を持つ NeNe 製品リポの Tailwind 依存 — **非 Tailwind 4艦**（concierge / serve / corpus / suite）〔自分で実測〕
- **relay**: **10通**送信（統合リナ4・concierge・deal・contact・mori-site・NENE2 2）〔送信応答 `ok -> :port` で確認〕
- ※ 他艦の実測値（mori-site の commit / 数値、contact の PR 番号 / GHSA、serve・invoice・concierge の各数値）は**すべて先方の測定であり、私は再実測していない**。各 issue / patch 文書にその帰属を明記した。

## 🔴 自分の誤り・迷いと、どう捕まえたか

- **確認せずに推測を書いた** — 統合リナへの1通目で「8834 は fleet のポートではない（8813 のはず）」と書いたが、`handles.tsv:47` に正しく登録されており**私の推測が誤り**だった（8813 は trace の席）。送信後に自分で気づいて訂正を送った。**相手が handles.tsv を触る前に止められた**が、止められたのは偶然に近い。
- **測れていない状態の数字を報告した** — 「テスト 426・日報比 +8」は `jsdom` 欠落で1ファイルが実行不能だったための過小計上。`npm ci` 後の正しい値は 438・+20。**環境を整えずに測って報告した**のが原因。
- **自分で調べられることを相手に聞いた** — 統合リナへ「`_work/.claude/worktrees/nene2-i18n-0.3.0` に心当たりは」と照会したが、`handles.tsv:50` に i18n 専用席として正式登録されており、自リポの `.mcp.json` も 8837 を持っていた。**手元の設定ファイルを読めば分かることだった。** 取り下げた。
- **帰属を引き写しで潰した** — 上記の 270日/14通 の件。起草側の指摘で訂正。
- **流しかけた** — #156 のフレーク。条文化中の規約が無ければ「再実行で通ったから」で終わっていた。

## 明日への引き継ぎ

統合リナの順序裁定（本日）:

1. **payout B-2b の i18n format 供給計画ノート**（小・payout が現にブロックされている）
2. **#159 のサイジング**（S＝半日以内なら批准(c) の前に実装・M/L なら (c) 優先で deal は明示 park）→ 結果を hub と deal へ
3. **批准前提(c) 素振りレーン**（本命・**origin を先頭で**回す＝origin の W1 #300 再開が hold 中のため）
4. **#152**（README (ii) 採用）
5. **A1 hooks→model codemod build**
6. **D-2 横断実測**（#165・Wave 数値の再点検を同乗）

🔴 **凍結中の制約**: 新章 `06-testing-verification.md` を含む**新文書・新条文の追加は「批准前提(c) 緑 → 契約v1凍結成立」の後の最初の patch 波まで凍結**（統合リナ裁定）。新文書追加が checker の `--docs` 対象と `mustTotal` を動かし、GREEN の批准前提(a) を再オープンしかねないため。**#160〜#165 はいずれもこの凍結明け待ち**。

**出口が未定の項目は無い**（本日見つけたものはすべて issue か relay へ着地させた）。
