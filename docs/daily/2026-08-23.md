# 2026-08-23 — 承認は中継できない。中継した瞬間に、像と実態が独立にズレる

> **対象期間**: **08-22 21:40（セッション起動）〜 08-23 00:1x**。**1セッション**につき連番なし。
> 🔴 **ファイル名は終了日**（規約: 1セッション=1本・日付では割らない・セッションが記録の単位で日付はラベル）。
> 日付を越えたので `2026-08-23.md` とした。先例 = `2026-08-20.md`（08-19 深夜〜08-20 未明）。
> **中身の大半は 08-22 の作業**なので、探すときは対象期間で引くこと。
> 前セッションの日報は `docs/daily/2026-08-20.md`（08-19 深夜〜08-20 未明）。
> 本セッションは施主の `/repo-status` で始まり、hub との往復で wave3 の配布まで進んだ。

`/repo-status` で始めたつもりが、終わってみると**一晩で3回、同じ形の欠陥を踏んで、3回とも別の誰かに止められた**
一日だった。形はどれも「**装置は動いている。守っている対象が違う**」である。

そして最後に、その一般形が**承認そのもの**にも当てはまることが分かった。
hub の像は「wave3 は未許可／#287 は承認済み」、実際は**その逆**だった。どちらも hub が善意で持った像で、
**間に人が入ると宛先と対象がズレても誰も気づけない**。hub は今夜これを2回露呈させ、
自ら「**hub は承認の伝達経路になれない**」と結論して、構造の側を直すと言った。

## ヘッドライン

- 🔴 **マージ待ちの #287 に inert な穴が見つかった** — `on.push.branches: [main]` を持つのに
  notify の発火条件が `schedule || simulate` だけで、**main へマージ後の赤が誰にも通知されない**。
  **この装置が守ろうとしている状況そのものが素通りしていた**。vault の指摘を自分の木で再現して塞いだ。
- 🔴 **`nene-corpus` は、監査を配線する前に実走したら high が1件出た** — そのまま足していたら
  **ゲートを入れた瞬間に赤**。allowlist で逃がせば**逃げ道ゼロ（0件/分母15）を自艦で崩す**。**倒してから入れた。**
- 🔴 **hub 経由の「施主承認」を2回とも使わなかった** — 保留した窓の中で上記の穴が出た。
  **伝聞のまま押していたら、穴ごと main に入っていた。**
- 🟡 **自分の起動チェックが、自分の名前で決め打ちしていた** — `handoff-*fleet*tooling*` で引いたため
  `handoff-fleet-2026-08-21-...`（`tooling` を含まない）を見落とした。hub の一言が無ければ気づいていない。
- 🟢 **`.mcp.json` に野良の受信口（8837）** — `git` と `handles.tsv` の**2層で同時に不可視**だった。

## 本日の成果（時系列）

### 21:40〜22:00 — `/repo-status`

- 齟齬4件を検出し、**全件を出口へ着地**させた（報告だけで終わらせない）。
  最大のものが「`current.md` 冒頭の**いま自艦の手番にあるものは無い**が偽」で、**Issue #288** を起票。
  板の 08-22 更新で `@dev` 手番が4本（うち1本は翌日 due）に増えていた。
- 🔴 **`--limit` の値を件数として報告しない**を履行（open issue は `--limit 500 --json number -q length` で総数を実測）。
- 🔴 **自己申告**: 指示書の探索を `handoff-*fleet*tooling*` で決め打ちし、**wave3 の指示書を取りこぼしていた**。
  スキルの「探索パスを決め打ちしない」を、**自艦の名前そのもので**破っていた。

### 22:00〜22:15 — hub への挨拶で宛先を間違えた

- `ListAgents` の `docker-94` を cwd から hub だと**推定して**2通送った。**hub の実体は `期限切れのタスク対応`**
  という中身と無関係な名前だった。2通とも施主承認待ちで保留され**実害は出ていない**。
- ⇒ **保留された送信を「送った」と書かない**。承認に依存しない経路（PR コメント・`_work/handoff-*.md`）へ張り直した。

### 22:15〜22:30 — 8837 の原因特定

- ccbridge が2本立っていた原因は、hub の仮説（`seed-all.py` の二重マージ）**ではなく**、
  リポローカル `.mcp.json` の**別名エントリ `cc-nene2-i18n`（8837）**だった。
- 🔑 **2層で同時に不可視**だった: `.git/info/exclude:7` で **`git status --untracked-files=all` にも出ない**／
  `handles.tsv` に**未登録**。そして ccbridge は受けたら即 `200 ok` を返すので**送った側は成功に見える**。
- hub が施主に諮ったうえで削除（バックアップつき）。**反映は次回起動時**なので、
  **次に立ったとき `ss -ltnp` で 1本になっていることを取る**のが残タスク。

### 22:30〜22:50 — PR #287（hub 作・自艦の notify-failure）を引き受けて2回直した

hub から明示的に委譲を受けた。**Issue #289** を起こして Issue 駆動の家を与えたうえで:

1. **`simulate_failure` を checkout 直後へ移動** — 当初は9ステップ中8番目で、
   **同じファイル内の自分のコメント「Checkout の直後に置く」と食い違っていた**。
   前段が落ちると意図した `exit 1` に到達せず、**別の理由の赤で notify が上がる**。
   コメントに「**逆に後ろへ置くのも駄目**」を追加。
2. **`timeout-minutes` を3ジョブ全部に**（check 10分 / notify **5分** / publish 15分）。
   notify を短くしたのは、**ここが吊ると「赤が通知されない」に化ける**＝計器そのものが黙る場所だから。

**陽性対照**（Issue #289 の DoD）:

| # | 中身 | 取り方 | 結果 |
|---|---|---|---|
| 1 | 発火する | dispatch `simulate_failure=true` | `check`=failure / notify=**success** |
| 2 | Issue 新規作成 | 同 run | **#290** が立った |
| 3 | **dedup** | 同じ dispatch を2回目 | **同名1本のまま・コメント追記** |
| 4 | push/PR で発火しない | **捨て枝 PR #291** | `event=pull_request` / **notify=skipped** |

🔴 **項目4は構造から自明だが「観測した」と書かない**（作法 #14）ため、
`- run: exit 1` を足した捨て枝で**陰性対照を実測**した。観測後にクローズ＋枝削除、#290 も経緯を書いてクローズ。

🔴 **そのあと「陽性対照 4/4 観測で取得」という自分の書き方を撤回した。**
`schedule` 経路**そのものは一度も観測していない**——`simulate_failure` 経路で発火を見ただけで、
DoD 項目1 の定義には合っているが、**「4/4」という表現が実態より強い**。
⇒ **経路ごとに観測済み／未観測を分けた表を PR に残した**（`push` も `schedule` も未観測）。

🔑 **ここが本体である。** 塞いだことでも、対照を取ったことでもなく、
**「取れていないもの」を明示した記録を残したこと**。**「取れていない」と書いてある記録だけが、後から取り直せる。**
何も書いていなければ、次の人はそれが「取ったが書かなかった」のか「取っていない」のか区別できない。

### 22:50〜23:00 — 🔴 マージ待ちの #287 に穴が見つかった

vault → hub 経由で「**通知する event ＝ その workflow が無人で走る event を列挙する**」が届き、自分の木で測り直した:

- `check.yml` は **`on.push.branches: [main]`** を持つのに、notify は `schedule || simulate` だけ。
  ⇒ **main へマージした後に CI が赤くなっても誰にも通知されない。**
- `push` を条件に追加し、job 名・Issue 題・body を「**無人実行**」に合わせた（`github.event_name` を本文に含める）。
- 🔴 **OR に枝を足したので、陰性対照を取り直した**（前回の観測は使えない）。`pull_request` で skipped を再確認。

🔑 **これは自分が踏んだ側である。** 「参照実装の食い違いが11艦へ写る」と指摘しておきながら、
**同じ PR の中で条件を文字列として写し、trigger と突き合わせていなかった。**
hub は後に「**その inert な条件を書いたのは hub**」と申告した。**止めたのは vault と、自分の木での再現。**

同時に **`grep -c -- '--limit 100'` が `--limit 1000` に部分一致する**ことも自分の木で再現した
（境界なし=**1**／境界つき=**0**／実値=**1000**）。⇒ 配布規則として
**「境界つき grep」より「実値を抜いて数値比較」が上位**（境界つき grep は正しく書けたときだけ正しい）。hub が採用。

### 23:00〜23:15 — wave3 の配布に着手（施主から直接 GO）

**射程が指示書と3点違った**ので、着手前に hub へ返して裁定を得た:

| 発見 | 実測 | 裁定 |
|---|---|---|
| **NENE2 は wave3 完了済み** | 4点すべて充足・cron も割当どおり。**指示書より hub の日報の方が新しかった** | 対象は **5艦 → 4艦** |
| **`nene-corpus` は方式(a) の先取りになる** | backend/frontend の2本立てで composer audit は backend 側 | **(i) schedule 抜き**で進める |
| **板 L111「フリート全艦に timeout ゼロ」は偽** | NENE2 は3 workflow 全部に、nene2-js/docs.yml にもあり | hub が板を訂正（正しくは **23艦 / 分母27**） |

**`nene-corpus` は PR #380（Issue #379）まで完了**:

- **論点1** npm 監査の配線＋`notify-failure`（backend-ci / frontend-ci **両方**）
- 🔴 **論点2** **`nanoid` 3.3.17 → 3.3.18** — **配線前に実走したら high が1件出た**。
  判例44 の全数開示: `packages` **624→624**（不変）／動いたのは **nanoid 1件のみ**（昇格）／
  lockfile は frontend と tests/e2e の2本で**触ったのは frontend だけ**／監査 **1→0件**。
  🔴 `npm audit fix` ではなく **`npm update nanoid --package-lock-only`** で狙い撃ち（判例44⑧）。
- **論点3** `timeout-minutes` を5ジョブ全部に
- **陽性対照4項目を frontend / backend の両方で観測**（片方の観測を他方へ流用しない）。
  リハーサルで立った #381 / #382 は経緯を書いてクローズ。

💡 **Issue タイトルを workflow ごとに分けたのが効いた** — 同一にしていたら
**frontend の赤を backend のスレッドへ追記して、どちらが落ちたか分からなくなる**。
**dedup が正しく働くほど悪化する罠**なので、hub が配布規則に採用した。

## 今日いちばん効いた判断

**hub 経由の「施主承認」を2回とも使わなかったこと。**

1回目（#287）は、**保留した窓の中で上記の inert な穴が出た**。伝聞のまま押していたら穴ごと main に入っていた。
2回目に hub の像が「wave3 未許可／#287 承認済み」＝**実態と逆**だと判明した。
判定に使ったのは一つだけ——**その言葉を自分が直接受け取ったか**。

🔑 **縫い目は「守ると安全」ではなく「守った回数だけ事故が止まる」。**
中継の承認を断るコストは往復1回。得るのは**押す前にもう一度見る時間**で、今夜はそこで穴が出た。

### 00:0x〜00:1x — 締め際に、同じ形が4回目に出た

`git ls-remote` で #287 の枝の SHA が自分の記憶（`df5b3fd`）と違うのを見て、
**「第三者が push した」と誤警報を出した**。追いかけたら**自分のコミット**（`416c41a`・push イベントの追補）だった。

ただしその過程で、**自分の PR コメントの「最終状態」ブロックだけが古い**ことが分かった
（`HEAD = df5b3fd` は論点2 までの SHA）。本文も追補も正しく、**要約ブロックだけが腐っていた**——
今夜4回目の同じ形（#288 / #287 の inert / 参照実装の文字列写し / これ）。

🔑 **誤警報は無駄ではなかった。** 狙って見つけたのではなく、
「**自分が最後に書いた SHA**」と「**リモートの実際の SHA**」を突き合わせる動作が、副産物として要約の腐りを出した。
⇒ **締めの前に `git ls-remote` を見る**を作法候補として PR に残した。

## 📊 本日の数字

**自分で実測したもの**

| 項目 | 値 |
|---|---|
| 起票した Issue | **5本**（fleet-tooling #288 / #289 / #292・nene-corpus #379 ＋ 捨て枝 PR #291） |
| 出した PR | **2本**（引き受けた #287 ＋ nene-corpus #380） |
| #287 へのコミット | **3本**（simulate の位置 / timeout / push イベント） |
| 観測した陽性・陰性対照 | **10 run**（fleet-tooling 4・corpus 6） |
| 自己撤回・自己訂正 | **3件**（「4/4 観測」の表現／PR の「最終状態」ブロック／指示書探索の決め打ち） |
| クローズしたリハーサル残骸 | Issue **3件**（#290 / corpus #381 / #382）・捨て枝 PR **1本**・枝 **1本** |
| corpus の lockfile 差分 | `packages` **624 → 624**（不変）・動いたのは **1パッケージ**・監査 **1 → 0件** |
| `nene-corpus/tests/e2e` の監査 | **0件**（射程外だが**未計測と 0 を混ぜない**ため実走） |

**渡された数字（自分では測っていない）**

| 項目 | 値 | 出所 |
|---|---|---|
| notify-failure 未保有 | **11艦 / CI を持つ27艦**（うち13艦が片肺・`backend-ci.yml` を持つ10艦は10艦とも無通知） | hub 実測 08-22 21:40 |
| `timeout-minutes` ゼロ | **23艦 / 27艦** | hub 実測 08-22 |
| PHP 監査が週次の外 | **7艦** | hub 実測 08-22 21:31 |
| 参照実装 `nene-serve` | `origin/main` = `a6e1d53`・`ci.yml:211` が `--limit 1000` | hub 実測（**写す前に自分でも実値を確認した**） |

## 申し送り

- 🔴 **PR #287 / #286 / nene-corpus #380 はいずれもマージ待ち。** マージは施主の手番。
  **hub 経由の承認は使わない**（今夜2回それで事故が止まった）。
- 🔴 **wave3 の残り3艦（corpus-site / mcp / nene2-js）は「配線1本」では終わらない。**
  **`audit-ci` 自体が無い**ので、devDependency 追加＋lockfile 変更＋`audit-ci.jsonc` 新設になる。
  **hub 裁定 = (A) fleet 標準どおり audit-ci ＋ allowlist を配る**（**逃げ道センサスの分母 15 → 18** も承認済み）。
  **各艦で「入れた瞬間に赤」を配線前に実走で確かめ、赤なら倒してから入れる**（corpus で実際に起きた）。
- 🔴 **方式が決まっていて、次に立ったとき指示待ちが不要なもの**（施主決定・板に実装行あり）:
  **PHP 週次監査 = (c) 週次専用 `weekly-audit.yml` を1本新設 → 7艦へ**（due 08-27）／
  **Dependabot version updates = 稼働中の製品艦のみ**（due 08-27）。
- 🟡 **`.mcp.json` の 8837 削除が効いたことを、次回起動時に `ss -ltnp` で取る。**
- 🟡 **repo-status スキルの指示書探索を `handoff-*fleet*` へ広げる**（自艦の名前でも決め打ちしない）。
- 🔴 **未観測を持ち越している**: notify の `push` 経路（マージ後）と `schedule` 経路（次の月曜 01:30 UTC）。
  **「設定した」であって「発火した」ではない。**
