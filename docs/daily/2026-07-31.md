# 日報 — 2026-07-31（nene2-fleet-tooling）

> 期間注記: 本セッションは 2026-07-31 朝に `/repo-status` で着手し、終業まで走った1本。施主から**「指揮は統合リナに任せている。疑問は詳細な理由をつけて hub へ投げ、そのうえで施主判断が要るものだけ聞く。作業が終わったら hub へ完了報告・確認依頼・レビュー依頼を出す」**という運び方の指示を受け、その形で通した。マージは施主 seam を守り、**hub の報告も実測してから使う**方針を維持した（本日 hub の番号取り違えを1件検出）。

## ヘッドライン

**規約還流バッチ6レーンが条文側で完結**した（#160 / #161 / #162 を条文化・PR #207 / #209 / #212 マージ済）。あわせて hub 裁定で**「導入 ≠ 実行」ガード族を設立**し、散在していた同型の教訓7件を1つの台帳へ集約した。

**publish 非依存のレーン0（0-1 フレーク修正 / 0-2 還流 / 0-3 射程実測）を完走**し、施主 GO で **5本を束マージ**。夕方から**レーン1（tokens 1.3.0）に着手**して #132 を実装した（PR #213・open）。

技術面の収穫は、**測り方を疑って結論がひっくり返ったケースが3件**出たこと。いずれも**最初の測り方のままなら間違った結論を出していた**。

- **#156**: 「修正後4回 green」は、**未修正でも4回 green** だったので判別力ゼロだった
- **0-3 射程実測**: パスで測ると **11艦**、内容（バンドラの指紋）で測ると **1艦**
- **#211**: 原因を1つと見ていたが、起草艦の再現で**2つあり独立**と判明。**私の評価が誤っていた**

運用面では、**自分の事故を1件出した**。`gh issue comment --body "…"` にバッククォート入り本文を直接渡し、**シェルがコマンド置換として解釈して本文の一部が消えたまま投稿された**。エラーは出たが投稿は成功し、欠けたことは出力から分からない。リポの規約は relay について「本文は必ずファイル経由」と定めているのに、`gh` へ適用していなかった（#215 に条文化）。

## 本日の成果（時系列）

### `/repo-status` — 齟齬2件を出口へ

- **#169 に実測コメント2件** — `deleteBranchOnMerge: true` を実測し、**再発防止は既に成立**していると確認。残タスクを「auto-delete 有効化」から「母艦ローカルの掃除のみ」へ縮小した。`git branch -r` は14本を表示するが `git ls-remote` は2本＝**差 12 は削除済みブランチの残骸**。〔自分で実測〕
- 🔴 **訂正コメント1件** — 「消してよい置き土産 worktree」と書きかけたものが、`handles.tsv` に登録された `nene2-i18n`(:8837) の**現役の座席**だった。独断で触らず hub 裁定を仰ぎ、**座席維持**が確定。

### レーン設計 — 軸を「難易度」でなく「publish 依存の有無」に取った

hub 承認。publish は施主の縫い目＝レート制限された資源なので、順序の制約はほぼそこに集中する。難易度で並べると publish 非依存の仕事が待ちに巻き込まれて遊ぶ。

- **レーン0**（publish 非依存・待ちゼロ）／**レーン1**（tokens 1.3.0・別便）／**レーン2**（standards 2.3.0・pending publish の後）
- **#164② を unblocking 便に載せない**理由（配布物への依存追加＋cap の単位がファイル→領域へ変わる＝艦が回避策撤去と台帳再生成を同時に受ける）は、hub から「そのまま次便設計の条文にしてよい」と評価された。

### 0-1 #156 — フレーク修正と、**判別力ゼロの green**

- **PR #205（マージ済 `44a9a2b`）** — 型検査テストに明示 timeout 30秒＋lib SourceFile キャッシュ。
- 🔴 **issue の提案3が成立しないことを実測で確認**: ts ブロックは**1個**でループは1周・2テストは有効TS/壊れたTSで**別 program が必須**＝集約できない。共有できるのは program でなく **lib d.ts の読み込み**だった。
- 🔴 **クローズ条件「修正後4回 green」に判別力が無かった。** 未修正の main でも 4/4 green だったので、**修正の有無で結果が変わらない**。主張を「直った」から**「余裕が 2.0〜2.5倍 → 12〜15倍になった」**へ格下げした。〔自分で実測〕
- **クローズ（`Refs` 運用）** — 5本束マージで main に CI が5連続で走り、**1本目が修正前コミット＝天然の陰性対照**になった。issue が「これは推測」と明記していた **CI 上の余裕を実測**したところ **1.96倍**（＝ローカルで 50% 落ちたのと同水準）。5回とも timeout 0。〔自分で実測〕

### 0-2 規約還流バッチ — 3本を条文化して完結

- **#160 → PR #207（マージ済 `bd077fb`）** — closing keyword の無言の失効。起草側（NENE2）の文面はほぼそのまま採り、**fleet が本日実踏した2条を追加**した:
  - **PR 本文と commit message の両方**から外さないと自動クローズは止まらない（squash マージの本文に元コミット message が入ることは本リポの履歴 `27f5887` で実測）
  - 「バッククォートで無効化できる」に**依存しない**（依存が壊れる向きが「観測が完了したように見える」で最悪）
  - 🔴 **この PR を書いている最中に、条文化しようとしている当のものを踏んだ**（#205 の説明文で keyword を書いた）。#154 ⑤ の「対策のコードの中でもう一度踏む」の実例として §4 に記録。
- **#161 → PR #209（マージ済 `5307766`）** — serve 由来3条＋**「導入 ≠ 実行」ガード族の設立**。hub 裁定で族化が確定し、**ID 体系 `EX-x` が fleet 案として承認**された（G-x は規約文書のメタ規約で層が違う／D-x は既に衝突済み）。族メンバーは EX-1〜EX-7。
- **#162 → PR #212（マージ済 `f2db00e`）** — invoice 由来5節。**還流バッチ6レーンがこれで完結**。
  - 🔴 **正本にしかない追記を1件回収**した。起草時に fleet が落ちていて指示書へ退避されており、**#162 の issue 本文には入っていなかった**。→ 作法「**条文化のときは issue で発見し、正本で確定する**」を明文化（正本には**起草側の実装コメント**も含む）。
  - **#155 ③' との重複は層で裁った** — 設計則（override をどう書くか）と受入手順（写した後に何を回すか）は**別条文として両方置く**。

### 0-3 射程の横断実測（#165 / #164④）— **射程は 1艦**

- **測り方を切り替えたことが結果の半分**だった。パス（`dist/` `public_html/**`）で測ると **11艦**ヒットしたが、**内容（バンドラの指紋 `__toESM` / `var __create = Object.create` 等）**で測ると **nene-concierge のみ**。clear / vault の `installer.js`・contact の `embed.js` は**冒頭に人間の説明コメントがある手書き**だった。〔自分で実測・全リポ読み取りのみ〕
- **#164④ を閉じられる状態にした** — 生成物 CSS をコミットしている艦も concierge のみ＝ **Wave 数値の再点検が要る艦は他に無い**。
- 🔴 射程 n=1 の帰結を hub へ上申 → **条件節つきで条文は書く**が確定（Tier A 配布はいつでも増えうるので、n=1 だから書かないと次の艦が独力で再発見する）。
- 🔴 **副産物** — 走査中に **nene-corpus で `node_modules/.vite/deps/` が 32ファイル git 追跡**されているのを発見（`.gitignore` には書いてあるが既追跡には効かない）。corpus は停止中のため**指示書＋board** へ落とした。corpus には**一切変更していない**。

### 施主 GO — 5本束マージ

- **#204 / #205 / #207 / #209 / #212** が squash で main へ（`ddebb13` → `f2db00e`）。**マージは施主**。
- **closing keyword の設計が実地で機能したことを実測** — `Closes` 運用の4本は CLOSED、**`Refs` 運用の #156 だけが OPEN のまま**残った。auto-delete も 5/5。

### レーン1 #132 — codemod が取り残す `var(--old)` の開示

- **PR #213（open・CI SUCCESS）** — `danglingVarReferences()` を `reentrantRenames` と同型の純関数として追加。CLI に `--scan-css` を足した。**版は上げていない**（#133 と揃えて publish する想定）。
- 🔴 **これは EX-1（TC-x・トークン参照の解決性）の道具側の版**だった。製品リポに「参照が解決することまで検査せよ」と要求している当の codemod が、**「改名した」は保証するが「参照が解決する」は検査していなかった**。
- **判断が要った点**: `var(--old, fallback)` も報告対象に含めた。無い場合は宣言ごと破棄されて壊れ方が見えるが、**有る場合は黙って fallback へ切り替わる**＝見えない。ただし **serve の TC-x は fallback 付きを対象外にしている**ので、**問いによって扱いが逆になる**ことを #208 の W0b 前提群へ足した。
- 🔴 **`--scan-css` を渡さなかったら「CSS は走査していない」と明示**する（沈黙を「CSS は健全」と読ませない）。

### #133 の実測（実装は未着手）

- **表の不在が2つの意味を持っている**のが真因だった。`--text-ui` / `--animate-spin` は class rename にも `unmapped` にも出ない**完全な沈黙**。実装は「v4 が utility を生成しない namespace」の想定でそう書いているが、**「表がまだ埋まっていない」場合と区別できない**。
- 🔴 **同じ穴を fleet が過去に1回踏み、`font-weight` の1件だけ塞いでいた**（コード内コメントに記録あり）。**実例を直して失敗の型を直さなかった**形で、standards 側では #179 で是正済みの誤りが tokens 側に残っていた。
- 設計を2段で上程: **第1段**（沈黙を殺す・語彙に触れない・1.3.0）／**第2段**（`text` / `animate` を表に足す・**同名キー共存ガードが要る**・第二波裁定待ち）。

### 起票（出口処理）

- **#206** 検証規律「陰性対照を取らない N 回 green は必要条件すら満たしているか不明」— 施主定式化3パターンの**4件目**。hub 裁定で**「まだ落ちていない」を根拠にしない**が追加条文として採用された。
- **#208** 還流レーンの ID が既存 `D-1〜D-6` と衝突（**D-2 / D3 / D4 が3義**）＋ **マージ済み contact patch の事実誤り**（`05 §7` に D4 は存在しない）。**W0b の前提条件**に昇格。
- **#211** `stylingWith()` の評価時 throw（#192 と同型）。
- **#214** invoice 由来の本番反映2条（族の新メンバー候補・**本番環境が射程**なので既存章に収載先が無い＝**第3の穴**と hub 認定）。
- **#215** 本文を持つ CLI 呼び出しは本文をファイル経由で渡す（**自分の事故が一次証拠**）。

## 誤りと訂正

- 🔴 **#211 で「A は G-7 逆行」と評価したのは誤り**だった。失敗原因は2つあり独立していて（typed ブロックの path 当たり／styling 断片の評価時 throw）、前者は配布側で何をしても消えないので**独立モジュールは回避策ではなく構造要件**。**起草艦の実装コメントを読めば防げた**誤りで、#211 §6 に帰属つきで残した。起草艦（invoice）が再現と第5案（`cwd` を渡す・publish 不要）を出してくれて解決した。
- 🔴 **hub の報告1件が実測で確認できなかった** — 族メンバー候補の出典「fleet#178」は別件（(c) 素振りの red 集約）で、build に関する記述は本文にもコメントにも 0件。陽性対照つきで不在を示して訂正を求め、**hub の番号取り違え**と確定（正 = #210・payout が同じ取り違えを独立に検出していた）。**出典が確定するまで台帳に載せなかった。**
- 🔴 **自分の事故**（#215）— 上記ヘッドラインのとおり。訂正コメントを出し、条文化した。

## 📊 本日の数字

- **マージされた PR**: **5本**（#204 / #205 / #207 / #209 / #212 — **マージは施主**）〔`gh` 実測〕
- **open PR**: **1本**（#213 = #132・CI SUCCESS）〔`gh` 実測〕
- **クローズされた Issue**: **5本**（#156 / #160 / #161 / #162 / #203）〔`gh` 実測〕
- **起票した Issue**: **5本**（#206 / #208 / #211 / #214 / #215）〔`gh` 実測〕。※ 同日の #210 は payout 起票・#216 / #217 は他レーン起票で**私の分ではない**
- **open issue**: **35本**〔`gh` 実測〕
- **test**: main で `npm run check` **exit 0**・**35ファイル / 517 tests**・`AM-2 release gate: PASS`。PR #213 のブランチでは **524 tests**（+7）〔自分で実測〕
- **main の CI**: 5本束マージで **5連続 run すべて success**・**timeout 0**。§1 型検査の所要は修正前 **2555ms** / 修正後 **2178〜2520ms**〔自分で実測・ログから抽出〕
- **横断実測（0-3）**: 全 repo を読み取り専用で走査。生成物コミット型 = **1艦**（パス判定なら 11艦だった）〔自分で実測・**各リポ無変更**〕
- **relay**: **16通**送信（hub 11 / invoice 2 / serve 1 / NENE2 1 / corpus 1）。うち **3通が不達**（serve :8811・NENE2 :8793・corpus :8797 が停止中）→ **指示書3本**へ切替〔送信応答 `ok -> :port` と `Connection refused` で確認〕
- **publish**: **無し**。tokens の版は上げていない（#133 と揃えてから施主 seam へ回す想定）
- ※ **他艦の実測値は再実測していない** — serve の宙吊り参照4本・11日埋没、invoice の 24 spec 脱落・92ファイル・本番 backend の3週間差、NENE2 の PR #1625、payout / deal の build 非検証、concierge の 487件。**すべて先方の測定**であり、各 issue と条文に帰属を明記した。
