# 日報 — 2026-07-30（nene2-fleet-tooling）

> 期間注記: 本セッションは 2026-07-29 夜に `/repo-status` で着手し、07-30 未明にかけて走った1本。施主から「統合リナに指揮を任せている・疑問は理由をつけて投げる・作業ごとに完了報告とレビュー依頼を」という運び方の指示を受け、その形で通した。マージと publish は施主 seam を守り、**中継された GO は実行せず本人確認を求めた**（後述）。

## ヘッドライン

規約還流バッチが**条文側も完全クローズ**し（#172 マージ）、検査器3件の欠陥修正を **standards 2.2.0 として publish** まで通した。並行して **(c) 素振りが全7艦で一巡**し、green 3艦が出たことで検査器そのものへの疑いが晴れた。

そのうえで hub の号令を受けて origin のゲート導入に着手したが、**配布物の消費経路で摩擦を3件踏んで完遂できなかった**（#189）。うち1件は未解決で、**(c) の残作業は「導入 PR 2本」から「#189 の切り分け → 導入 PR 2本」に伸びた**。

技術面での最大の収穫は、**「台帳に登録しても効かない」という同じ形が6つの層で見つかった**こと。供給・検査・更新・参照・配布・配線のどこか一本が繋がっていないだけで、登録は無意味になる。総括した **「存在を機能の証拠として読まない」** は `_work/advice.md` に収載された。

運用面では、**hub 経由で伝達された施主 GO を実行せず、本人確認を求めた**。hub はこれを「正しい・hub の誤りだった」と撤回した。同じゲートで数時間前に hub が誤った直後だったため、同じ検証を省くのは自分のご都合主義になると判断した。

## 本日の成果（時系列）

### `/repo-status` 2巡目と、その出口

- **#169 起票＋(a)(b) 実施** — 07-29 の #153 と同型の stale が再発。ローカル3本を SHA 記録のうえ削除し、リモート15本は **`gh pr` の headRefOid と全数突合**してから削除。1本（`feat/w0a-registries`）は **stacked PR#4 の squash コミットで PR refs から復元不能**と判明したため除外し、hub 裁定でタグ `archive/feat-w0a-registries-b90b6a5` に退避してから削除した。**事後 `ls-remote` で remote heads は main のみ**。〔全て自分で実測〕
- **#162 へ invoice ①追記を収載** — 07-29 早朝に relay 不達だった分を handoff ファイルから拾い、`stylingWith()` の cwd throw を追記。

### 規約還流バッチ — records ぶんの条文化と、起草艦の検算

- **#170 → PR #172（マージ済 `f7425d5`）** — records 由来 CSP 4件＋認可⑤を条文化し、規約 v1 の収載先を割り当てた。hub の申し送り3点を全反映。
- 🔴 **records の検算で必須修正が1件出た** — 認可⑤の MUST 許容形を「match 列挙／読み取り述語」の2つに限定していたため、**論理和 `$method === 'GET' || $method === 'HEAD'` で正しく書けている実在コードが非準拠になる**状態だった（records の media 分岐・profile `:44`・vault `:55`）。許容形を3つに拡張し「禁じているのは片方だけを見ることであって構文ではない」を明記した。〔records 実測・fleet 再実測なし〕
- **指摘8件のうち3件は fleet 由来**（実装の型の帰属・56件の粒度・§4 の条件節欠落）と §8.2 に明記した。特に条件節の欠落は「親 CSP が緩ければ script は動いてしまう＝隔離が成立していない」という分岐を落としており、**MUST NOT を書いた本人が射程を狭く測れていなかった**という性質の穴だった。
- 副次的な収穫: 「健全4分岐」を構文別に割ったら**健全な分岐が同一構文でない**ことが可視化され、それをそのまま「許容形を構文で縛らない理由」として条文の根拠に昇格できた。訂正が元より強い条文を生んだ。

### 検査器の欠陥3件 — 発見から publish まで

- **#159 / PR #174（マージ済 `9e3e1cc`）** — `scoped-theme` が stylelint 合成で消費されていなかった。hub 依頼のサイジングを **S** と判定し、裁定の分岐どおり批准前提(c) の前に実装。**負の対照**（実装を stash すると新規9件中4件が落ちる）と**実 stylelint での挙動固定**で空虚合格を排した。hub 受入条件だった「スキップの可視化」にも対応し、`stylelint --print-config` に除外理由まで出るようにした。〔全て自分で実測〕
- **#178 / PR #179（マージ済 `4416e1f`）** — 素振り中に発見した gate-integrity の偽陽性。ルール**不在**（null）を `-1` に落として比較していたため、canonical が **0（明示 off）**のセルで「差異登録なき緩和」と誤検出していた。origin 実測で **red 13件 → 12件**（消えたのは当該1件のみ・他は不変）。
- **#176 / PR #180（マージ済 `64f424c`）** — deal 報告を受け、**fleet 側で独立に再現**（cap 10行/100byte に対し **202行/6908byte＝20倍超過**でも `init --check` は exit 0）。`initScan` が実測した値を `initCheck` が捨てていた。**段階的 enforce**（超過は常に表示・exit code への算入は `--enforce-legacy-size` を付けた艦だけ）を hub 裁定どおり実装。**負テスト6本を陽性対照つきで同梱**した。

### 2.2.0 の出荷

- **#186 / PR #187（マージ済 `74ea3b9`）** — bump。**#181 の base を `main` へ retarget してから #180 をマージ**し、stacked PR が CLOSED になる事故（issues#54・フリートで2回既出）を回避した。**#180 マージ後も #181 が OPEN のまま生存**することを確認済み。
- 🔴 **ローカル `npm publish` は設計どおり失敗した** — `Automatic provenance generation not supported for provider: null`。`publishConfig.provenance: true` により Trusted Publishing の CI からしか publish できない。**provenance 設定は変更しなかった**（承認されたのは publish であって設定変更ではない）。さらに `docs/publish.md` に「**publish の実行は施主（hide）。担当リナは準備と検証まで**」という明文があり、会話の GO より実行者指定のほうが具体的だと判断して検証まででいったん止めた。
- **CI dry-run success を確認したうえで施主が本番実行** — publish 完了。〔`npm view` で独立裏取り〕

### (c) 素振りレーン — 一巡

- **#178 起票（origin 初報）** — gate-integrity red 13件は**13個の欠陥ではなく単一原因**（配布 config 未導入）と切り分けた。「素振りには**ゲート導入 PR を当ててから測る**という前提工程がある」ことが初報の主要な発見。
- **全7艦を実測** — green 3艦（invoice / payout / vault）が出たことで**検査器側の疑いが晴れた**。deal は red 2件だが**導入したうえでの実在の緩和**（origin の未導入とは別種）。
- 🔴 **records で第3の状態を発見** — 依存にも `import` にも nene2 が現れるのに red 16件。切り分けると `...nene2.overrides.recordsCookieAuth` **のみ**で canonical の base を展開しておらず、standards も `^1.0.0` でメジャー1つ古い。**「依存にある」も「import している」も導入の証拠にならない**。施主裁定 07-15「records を丸ごと後送り」の適用として (c) の判定から除外したが、**除外＝隠蔽ではない**ので red は見せ続ける。

### origin ゲート導入 — 着手して、完遂できなかった

publish 完了を受けて hub から「origin / field のゲート導入 PR に着手せよ」の号令。着手したが、**配布物の消費経路で摩擦を3件踏んで止まった**（#189 起票）。

- **摩擦1（解決）**: peer に `stylelint>=16` が要る。`import nene2 from '@hideyukimori/nene2-standards'` が main entry 経由で `dist/stylelint/plugin.js` を巻き込むため、**eslint 配線だけしたい艦でも stylelint の install が必要**。`peerDependencies` に明記されているので仕様どおりだが、導入コストとして記録した。origin に追加して解決。〔自分で実測〕
- **摩擦2（縮退で回避）**: `nene2.i18n` が `jsx-a11y` を再定義し、origin の既存登録と衝突して **config の読み込み自体が落ちる**（`ConfigError: Cannot redefine plugin "jsx-a11y"`）。回避には origin の a11y 設定を canonical へ寄せる必要があり、それは**既存の a11y ルールを黙って置き換える**ことになって「測る最小構成」を超える。→ **i18n 軸を展開しない**判断（hub 承認）。〔自分で実測〕
- 🔴 **摩擦3（未解決）**: `nene2.base` が `@stylistic` ルールを **180件**持つのにプラグイン未定義で、config 読み込みが落ちる。**ただし invoice（standards 2.1.0）は同じ base・同じく `@stylistic` パッケージ不在の環境で `eslint` が exit 0 で通る**（両方とも実測）。**同じものを展開して片方だけ落ちるので、差の条件が特定できていない。**

**「invoice で通っているから配布物は正しい」とは結論しなかった。** 通っている理由が偶然かもしれないからで、これは今日ずっと追っていた「存在を機能の証拠として読まない」の逆走になるところだった。切り分けを #189 の最初のタスクに置いた。

状態: 作業ブランチ `chore/gate-introduction-report-only` は **push 済み・PR は未発行**（未完のため）・**製品コードには触れていない**（`package.json` と `eslint.config.js` のみ）。途中で止まったが壊れているものは無い。

🔴 **これで (c) の残作業が「導入 PR 2本」から「#189 の切り分け → 導入 PR 2本」に伸びた。** green だった3艦は**既に導入済み**だったのでこの壁を踏んでおらず、**新規導入の経路が実地に出たのは今回が初めて**だった。「3艦が green」は「新規導入が通る」の証拠ではなかった——今日の学びがそのまま自分に返ってきた形。

### 他艦とのやりとり

- **deal から fleet の実装欠陥2件**（#176 の cap 未 enforce・#177 の `@theme` × legacy 衝突）。報告を鵜呑みにせず再現してから issue 化した。
- **deal の C5 は 6エントリではなく 2エントリで足りる**ことを実測で回答（同一セレクタの重複は1エントリで覆え、素の `[data-theme='x']` は既定パターンで通る）。
- **cap の単位ズレを発見** — deal の `maxLines` が raw `wc -l` で、検査器の `formattedLineCount` と別物だった。deal は**渡された値をそのまま入れず自艦で再現してから** landed した。
- **#184** — deal の照会に答えるため実装を読んだところ、**gate-integrity が registries を一度も読んでいない**ことが判明。「差異登録なき緩和」という**文言が実装の約束を超えていた**。hub 裁定 (B) で「繋げない」と決め、文言を実装に合わせた（PR #185・次 minor 波）。

### 追補（同日 深夜セッション）— #189 の切り分け完了。摩擦3の真因は自分の導入 PR 側だった

上の「差の条件が特定できていない」を潰した。**3件すべて原因を特定**し、記録は #189 のコメントへ（実測・再現プローブつき）。

- 🔴 **摩擦3 の真因は配布物ではなく、私が書いた導入 PR の report-only 降格ブロック**だった。`Object.keys()` で全ルールを一律 `'warn'` にしていたため、**canonical が意図的に `off` にした `@stylistic` 180件（`eslint-config-prettier` 由来）まで severity 1 へ昇格**していた。ESLint 9.39.4 は「off の未定義プラグイン」は通すが（実測 exit 0）、severity>0 で config 読み込みを拒否する（実測 exit 2）。**invoice が通るのは降格ブロックを持たないから**で、偶然ではなく構造的理由だった。〔自分で実測〕
- **版差・環境差はいずれも原因ではなかった** — base の `@stylistic` は 2.1.0 と 2.2.0 で**完全同型**（block[5]・180件・全 off・plugins 空）、`eslint` 9.39.4 と `eslint-config-prettier` 10.1.8 も origin・invoice で一致。〔自分で実測〕
- **摩擦2 は「a11y を canonical へ寄せる」必要がなかった** — 真因は**同じパッケージの別の層を登録している参照不一致**。standards は `flatConfigs.recommended.plugins['jsx-a11y']`（`{meta, rules}`）を、製品は module default（`{meta, rules, configs, flatConfigs}`）を登録している。ESLint は**参照が違うときだけ** `Cannot redefine plugin` を投げ、同一参照なら共存する（実測）。→ どちらの層に寄せるかは全艦に効くので **hub 裁定へ回した**（私の推しは standards 側を module default へ）。
- **摩擦1 は配布物の実欠陥だった → PR #192（open・CI 緑）**。経路は `dist/index.js` →（re-export）`checks/init-scan.js` →（静的 import）`../stylelint/plugin.js` → `'stylelint'`。`peerDependenciesMeta.stylelint.optional = true` と宣言済みで、消費側 `scanLintBaselines()` も既に `await import('stylelint')` の遅延形だった＝**静的 import 1行だけが宣言と設計意図を裏切っていた**。loader hook で stylelint を解決不能にした環境での before/after は 🔴 `ERR_MODULE_NOT_FOUND` → 🟢 読み込み OK。〔自分で実測〕
- **origin の事故マージと revert を独立確認** — hub FYI を実測で裏取りし、件名の「revert 実施」が当時まだ未着地だったので差し戻した（hub が過大表記を撤回）。その後 origin PR **#466 マージ**で着地し、gate 3ファイルが事故前 `7ba7f3d` と**バイト一致**（`git diff --stat` 空）・main = `b5678ac` を確認。**導入 PR の基点はクリーンになった**。〔自分で実測〕

さらに hub 裁定（severity フィルタ採用・配布側ヘルパは次 minor 波・導入 PR は先行・origin 分は fleet 手番）を受けて origin の導入を実装したところ、**摩擦が2件増えた**。

- **摩擦5（解決）**: canonical の `parserOptions.projectService` が、**canonical より前で宣言した「型付きプログラム外ファイル」除外を上書きする**。`.storybook/*.ts` / `playwright.config.ts` / `eslint.config.js` 自身が `Parsing error: ... not found by the project service` で **4件**。canonical より後ろで除外ブロックを再宣言して **0件**。field の摩擦4（`projectService` × `project` の併存不可）とは別現象で、**canonical 同形の艦でも起きる**。〔自分で実測〕
- 🔴 **摩擦6（未決）**: canonical の `no-restricted-syntax` が**艦固有パターンを丸ごと置換**する（flat config はルール単位で後勝ち＝オプション配列ごと差し替え）。origin の自前3件が消え、うち **named-exports-only は canonical に代替が無い**ので**導入した瞬間に黙って失効**する。「導入 = 強化」の期待に反する**静かな弱化**で、#160 の「意味の違う正常値」と同型。〔自分で実測〕
- 🔴 **未決**: **origin の lint は `eslint . --max-warnings 0`** だった。**wip に私が書いた「`--max-warnings 0` でない通常の lint」は事実誤認**で、それが field への共有にも伝播していた（撤回・お詫び済み）。warn 降格では warning 386 で CI が赤くなる＝**report-only の前提が両艦で成立しない**。案 A（off へ）／D（cap を実測値へ）／F（別スクリプト）を実測つきで hub へ上げた。
- **配線が「測れる状態」を作ることは before/after で実証**: 同一コマンドで導入前 conformance **unknown 14**（gate-integrity = **unknown(crashed)**）→ 導入後 **unknown 13 / red 1**（gate-integrity = **red**・`実効 severity 1 < canonical 2`）。〔自分で実測〕
- 副産物 **#193 起票**: 未導入艦の gate-integrity が `not-installed` ではなく **`crashed`**（`@stylistic` プラグイン未発見）を返す。**「未導入」と「検査器が落ちた」が区別できない。原因は未特定と明記**し、最初のタスクを再現条件の特定に置いた。
- origin ブランチ `chore/189-gate-introduction-report-only` は **push 済み・PR 未発行**（未決2点があるため。「CI 緑でレビュー依頼」の規律）。**worktree で作業し撤去済み**・共有 checkout 不触（#464 の再発防止）。

- 🏁 **hub 裁定を受けて A 形（canonical off）へ組み替え、origin 導入 PR #477 を発行**（CI **10/10 pass**・MERGEABLE/CLEAN・3ファイル +2107/-58）。裁定は「A 採用・全艦統一」「摩擦6 は導入 PR に含める」「#193 は (c) 緑宣言の前提」「#192 技術レビュー PASS（マージは施主承認）」。〔自分で実測〕
- **摩擦5 と摩擦6 は、同じ1つの組み替えで両方消えた** — 置換が起きていたのは canonical を origin 自前ブロックの**後ろ**に置いていたからで、**前に置けば艦の既存ブロックが後勝ちで無傷**になる（写経ゼロ）。実測: `no-restricted-syntax` sev 2/opts 3・`no-restricted-imports` sev 2/opts 1 が **main と完全一致**（`--print-config` の JSON 比較）。除外ブロックの再宣言も不要になった。〔自分で実測〕
- **検査器が自力で鳴っていることも確認** — 導入後の gate-integrity は red「実効オプション数 3 < canonical 15（後勝ち全置換の疑い）」＝**艦固有が canonical を上書きしている状態を計器が検出**している。和集合（canonical ∪ 艦固有）は enforce 昇格時の宿題として台帳に残した。〔自分で実測〕

- 🏁 **(c) 素振りレーンが完了し、総括を #178 に提出**（hub 検収 → 施主上申へ）。W1 対象6艦すべて導入済み状態（records は施主裁定で対象外・負債は保持）。origin は **導入前 unknown 14 → 導入後 unknown 13 / red 1**。field も #141 マージ済み（CI 3 context SUCCESS・**field 実測**）。
- **field #141 を査読 PASS**。canonical 前置・生成器による凍結台帳・負テスト2本を確認。🔴 ただし**機構が未統一**であることを査読で申告した — origin（私の #477）は runtime フィルタで**将来 canonical に追加されるルールを黙って off に吸収**する弱い形、field は凍結台帳で**差分に出る**強い形。**私の実装のほうが弱い**ので、ヘルパの既定は field 意味論にし、origin を揃える提案を出して hub 承認を得た（「統一済み」とは記録しないよう依頼）。
- **摩擦5 の分岐条件を特定** — field 仮説（`allowJs` の差）は外れで、origin も `allowJs` 無し・root tsconfig も solution ファイルと**同条件**だった。0件だった理由は eslint config 側の既存装備2つ（`tools/**` を `ignores`・`**/*.js` + `disableTypeChecked` ブロックが前置により後勝ち）。⚠️ `'**/*.js'` は `.mjs` を捕まえない。〔自分で実測〕
- **#193 は再現せず** — 5通りの統制条件で再現しなかった（未配線 config でも計器は red を返す）ので「素振り初報が壊れた計器で読まれた」形跡は無し。原因不明のまま分類は丸めず、**crashed がどちら側で落ちたか**を出す改善＋陽性対照テストを PR #194 に。
- 🔴 **その改善が直後に本物を見つけた** — field を測ると「製品 config が配布パッケージを解決できない」。**導入済み・CI 緑でも、測定した checkout で `npm install` 未実行だと計器は crashed を返す**＝測定前提の未充足が「艦の欠陥」と同じ出力だった。`not-installed` に分類し測り直し手順を details に書く修正を #194 に同乗。**他レーンの checkout に install は打たず、field へ再測定を依頼**した（未計測を green とも red とも書かない）。
- 🔴 **数字の訂正**: canonical 4軸合算は **466**（base 459 / fsd 1 / api 4 / styling 2）で、私が #189・origin PR #477・コミットに書いた「464（4軸）」は誤り（464 は base+fsd+api のみ・実効は 78 でなく 80）。**field の数字が正**。off 386 は両枠で同じなのでフィルタの挙動と結論は不変。正誤表を総括 §6 に置いた。

- 🏁 **配布ヘルパ `nene2.reportOnly()` を実装（PR #195・CI 緑）** — hub 着手 GO を受けて。既定は **field 意味論（凍結台帳）＋ `severity: 'off'`**、`warn` で canonical の off が混ざれば **throw**（摩擦3 を静かに出荷しない）。`canonicalRuleIds()` / `reportOnlyStaleRules()` / `renderReportOnlyLedger()`（**生成器を配布側に**）/ `toolingExemption()`（既定 glob に `**/*.mjs` を含む）。テスト15本すべて陽性対照つき（生成物は**実際に import して評価**・降格ブロックを外すと武装に戻ることも確認）。`npm run check` exit 0 = 32 files / **471 tests**。〔自分で実測〕
- **実装中に地雷1件** — `stylingWith()` はエントリ実在を **cwd 基準**で確認して fail-loud に throw するので、台帳生成やルール id 問い合わせ（艦の cwd で走るとは限らない）から呼ぶと無関係に落ちる。`entryPoint` 未指定時は `composedConfig()` と同じ静的 `styling` を使う形へ修正。**fail-loud は正しいが、呼ぶ側の実行文脈を選ぶ**という教訓で、他の cwd 依存 API にも同型がありうる。〔テストで踏んで発見〕

- 🏁 **計器・配布ルールの偽陽性を3本潰した**（hub 裁定の順序どおり）。すべて CI 緑・施主承認待ち。
  - **#163 → PR #196**: 非 Tailwind 艦（concierge/serve/corpus/suite の4艦）で `better-tailwindcss/no-unknown-classes` を「差異登録なき緩和」と報告していた。**検査不能を違反として罰していた**形で #178/#179 と同類型。判定は**依存の実測**（艦の申告では判定しない＝「外せば緩和が消える」抜け道を作らない）。concierge 実測で details **2行 → 1行＋対象外注記**。残る `style-prop-css-vars-only` は断片分割が必要で、hub 裁定=**分割承認・実装は凍結明け**。
  - **#118 + #130 → PR #197**: canonical が **条文が唯一許している座席そのもの**（I18nProvider 実装・登録テーマモジュール本体）を誤検知していた。**各艦が file override で退避していた＝配布物の欠陥を艦が肩代わりしていた**。1ルール丸ごと off にせず該当セレクタ群だけ外し、互いに素も保った。座席は実測で決定（provider は4艦・テーマモジュールは**ファイル名が艦ごとに違うがディレクトリは共通**）。プローブ5本（うち陽性対照2本）。
  - **#84 クローズ**（表題 1.2.0/1.1.0 が実態 2.2.0/1.2.0 から取り残されていた。古い器は書き換えず新規起票する方針）。
- **field 報告2件を PR #194 に同乗**（配布パッケージ未 install を `not-installed` に分類／適用ファイル数0 の cwd 取り違えに測り直し手順）。**どちらも「測定前提の未充足 ≠ 艦の欠陥」の同じ束**。
- **concierge へは relay 不達**（:8795 停止）→ 規約どおり `_work/handoff-concierge-163-answer-2026-07-30.md` に切替。

- 🏁 **#164 → PR #198**（G-6 の現物）。`scan-coverage` が `index.html` を無条件 allowed にしていたため、**測定経路（`init --scan` / lint-baseline / `run` が `.css` だけを見る・3箇所）と組み合わせて「台帳にも lint にも載らないのに green」**が成立していた。issue の記述「列挙が HTML を見ない」は不正確で、**列挙は既に .html を含んでいた**（原因は測定経路側）。直し方は「登録されていない（red）／登録されているが測れない（unknown）／埋め込みなし（green）」の3分。〔自分で実測〕
- **Wave 表の誤計上を自分で再実測して確定** — 07-29 の日報に「数値は concierge 実測・**私は再実測していない**」と書いた穴を潰した。postcss-html で測って**内訳まで完全一致**: `app.css`（**生成物**）= 218（106/55/43/14）／`index.html` の埋め込み `<style>`（1ブロック・947行）= **487**（409/49/27/1/1）。→ **concierge 行 218→487・W-Layer 106→27** で確定し、`_work/issues.md` 項56 に**追記で訂正**（統合リナの台帳なので消さず追記）。行数は私 947 / concierge 949 で、差2行は数え方の違い＝**どちらも正しい**。〔自分で実測〕
- 🔴 **#164 の ③④ は残した** — ③測定経路への HTML 抽出（postcss-html）は **cap の意味論**（`maxLines` を「ファイル全体」で数えるか「`<style>` 部分」で数えるか）に決定が要り、どちらも既存台帳の意味を変えるので裁定へ回した（推しは領域単位）。④生成物 CSS コミット艦の横断再点検は D-2 同枠。**③が入るまで台帳登録済み HTML は unknown のまま**＝green で隠れていた状態より前進、という整理で報告した。

- 🏁 **tokens レーン（#135系）** — hub GO で5件に着手し、**3件着地・2件は feat として残した**。
  - **#134 + #88 → PR #199**: 実測すると**同じ根**だった。`EXTENSION_TOKEN_PATTERN` が Tailwind v4 の**複合キー**（`--text-<key>--line-height`）を受理しておらず、x- 送り済みの複合キーが「未知」と判定されて **x- が再挿入され double-x**（#88 の本体・非冪等）→ その名前はどこにも属さないので generate が refuse（#134 の症状）。**「道具が自分の生成物を拒否する」の3例目**（#49/#35 に続く）。step 6 に**「自分の検査器が拒否する名前を出さない」不変条件**も入れた。before→after: 複合キーが no-op（冪等）・不正キーは reject・round-trip が **refuse → generate OK**。〔自分で実測〕
  - **#135 → PR #200（テストのみ）**: **再現しなかった**。class 境界の前方1文字が `x-` の `-` を消費するので renamed 形の内側にマッチしない（`reentrantRenames` も空・`--check` は置換と同じ単一パス）。対応案2（run-once 明文化）も CLI の NOTE として実装済みだった。**原因不明のまま閉じない**ため、依存している不変条件をテストで固定して Closes した。〔自分で実測〕
  - 🔴 **棚卸しで stale 2件** — #88 の「根治」部分（未知 namespace の reject）は既に実装済み・#92 も実装済みに見える（受入条件の確認は別途）。**issue が実態から遅れている**のは今日3件目（#84・#163 §3・ここ）。

- 🏁 **#92 受入確認 → doc stale 棚卸し束**（hub 指定・今夜の最後）。
  - **#92 は受入条件を満たしていた**（実測）: `tailwindNamespaceOf` に fallback 無し（未知は null・`--color-accent` は 'color' ＝陽性対照）・#92 が列挙した**6トークンすべて reject**・マージ順序条件（施主 publish 2手の後）も充足済み。
  - **#90 → PR #201**: release note の「未知 namespace の x-送り済みは**再走で silent 二重送り**」が現行実装では誤り（実測: `--line-x-height-body` / `--z-x-modal` とも **reject(loud)**）。同節の「(i)reject は C part-1 で未実装」も stale だったので併せて訂正。**「実装状況を誇称しない」は、遅れて実装された事実を書き漏らさないことも含む**という整理を書き添えた。
  - **#152 → PR #202**: README の版表を**丸ごと落とした**（提案 (ii) 採用）。#55 で直して13日で再発し standards が**メジャー1つぶん**ずれていたので、手書き台帳を残す限り再発する。版は `npm view` と `fleet-baseline.json` が正・進捗も README に書かない（`current.md` と `board.txt` が正）。
  - **#98 クローズ**: payout の dead class を **class 境界つきで再実測して 4クラスすべて 0**（250ファイル）。規約 05 §9.3 の行頭が「17」のままだったので実測へ訂正（注記としては 07-18 に入っていたが**読み手が最初に見る値が stale**だった）。`check:exemplars` は訂正前後で不変（8/8・失敗0）。
  - **#86 クローズ**: 求められていたセッション2 追記は**既に landed 済み**（L87・L107）で器だけ open だった。
- 🔴 **#98 で測り方を1回間違えた** — 素の `grep '\btext-primary\b'` で 18件出て「issue と矛盾する」と読みかけたが、**`text-text-primary` の部分文字列**を拾っていた（`\b` は `-` を境界扱いする）。**#135 の部分文字列包含と同型の誤り**を、同じ晩に自分でも踏んだ。境界つきで測り直して 0 を確認。**dead class を数えるときは class 境界を付ける。**

- **#92 クローズ**（hub GO）— 受入条件①fallback 除去②列挙6トークンすべて reject③publish 順序、全部を実測で確認して閉じた。陽性対照（`--color-accent` は 'color' を返す）も置いて「常に null を返すだけの実装」で通らないようにしている。回帰は PR #199 のテストで固定済み。

**私の摩擦3 の初報は「配布物が詰まっている」という形で書かれていたが、実際は自分の消費コードの欠陥だった。** 配布物側に見つかった実欠陥は摩擦1 だけ。#189 の起票時に「invoice で通っているから配布物は正しいとは結論しない」と書いたが、正しい警戒の向きは逆でもあった——**自分の書いたものを疑う**ほうが先だった。

## 📊 本日の数字

- **マージした PR**: **5本**（#172 / #174 / #179 / #180 / #187）〔`gh` 実測〕
- **起票した Issue**: **12本**（#169 / #170 / #171 / #173 / #175 / #176 / #177 / #178 / #182 / #184 / #186 / #189）〔`gh` 実測〕
- **open PR**: **4本**（#181 / #183 / #185＝次 minor 波・#188＝本日報）／**open issue 45本**〔`gh` 実測〕
- **publish**: `@hideyukimori/nene2-standards` **2.2.0**（`npm view` → 2.2.0・`dist-tags.latest` → 2.2.0・git tag `nene2-standards-v2.2.0`・GitHub Release 生成済み。workflow run 30469735577・**施主が実行**）〔自分で独立裏取り〕
- **test**: main 統合後 `npm run check` **exit 0**・**31ファイル / 456 tests pass**・`AM-2 release gate: PASS`・**MUST 総数 248・タグ欠落 0**（批准前提(a) GREEN 時と一致＝mustTotal 不変）〔自分で実測〕
- **(c) 素振り**: **7艦**測定（green 3 / red 4）。残作業は **#189 の切り分け → origin・field の導入 PR 2本**（当初は導入 PR 2本のみと見ていたが伸びた）〔自分で実測・読み取りのみ〕
- **後片付け**: remote ブランチ **16本 → 1本**（main のみ）・タグ退避1本〔`ls-remote` 実測〕
- **relay**: **19通**送信（hub 12・deal 6・records 2・payout 1・i18n 席 1 — うち payout は不達で handoff ファイル方式へ切替）〔送信応答 `ok -> :port` で確認〕
- ※ 他艦の実測値（deal の件数・行数、records の issue 番号・行番号、invoice/contact 由来の数値）は**すべて先方の測定であり、私は再実測していない**。各 issue と条文に帰属を明記した。

追補（深夜セッション）:

- **open PR**: **5本**（#181 / #183 / #185 / #188＝本日報 / **#192＝#189 摩擦1 修正**・全部 CI 緑）〔`gh` 実測〕
- **test**: `npm run check` **exit 0**・**32ファイル / 459 tests pass**・`AM-2 release gate: PASS`（うち #192 で追加したのは3本）〔自分で実測〕
- **#189 切り分け**: 摩擦 **3件すべて原因特定**。配布物の実欠陥は **1件のみ**（摩擦1）・残る2件は消費側（摩擦3＝私の導入 PR・摩擦2＝参照層の不一致で hub 裁定待ち）〔自分で実測〕
- **独立確認**: origin の revert 着地（PR #466・main `b5678ac`・gate 3ファイルが `7ba7f3d` とバイト一致）〔自分で実測。hub 報告の裏取り〕
- **relay**: 追加 **1通**（hub へ差し戻し。本日計 20通）〔送信応答 `ok -> :8790` で確認〕

## 🔴 自分の誤りと、どう捕まえたか

- **「実装済み」を「使える」と書いた** — deal へ「C5 は2エントリの登録で前進できます」と**現在形で**書いたが、#174 は未マージ・未 publish で、deal が使うのは npm 公開版だった。**merge と publish の前提を2つ落としていた**。deal が先回りで確認して指摘。私たちが条文にしている「導入 ≠ 実行」を自分でやった形。
- **証拠の数を水増しした** — #178 で origin 未導入の根拠を3点挙げたが、3つ目（`meta.standardsVersion` が null）は**全艦で常に null**（#182）なので**無効**だった。結論は残り2点で立つが、「3つの独立した証拠が揃った」という書き方は誤り。判断より書き方の問題として訂正した。
- **負テストの前提が誤っていた** — #183 の「未導入なら null」テストにリポ内フィクスチャを使ったが、`createRequire` がリポの `node_modules` まで遡って workspace symlink を解決してしまい、**未導入を再現できていなかった**。実装ではなくテストの前提が誤り。
- **不在の証明に、不在を証明できない道具を使った（深夜）** — hub の FYI を裏取りして「origin PR #464 は存在しない」と #189 に書いたが、**#464 は issue として実在**した。`gh pr view` は issue 番号を解決しないので、あの `Could not resolve to a PullRequest` は不在の根拠にならない。hub の指摘で訂正。**測ったことは正しかったが、測る道具が問いに合っていなかった。** 「参照0件は探し方の問題」（同日 `_work/advice.md`）の同型を、その日のうちに自分で踏んだ。
- **自分が書いたコメントを根拠に扱った（深夜）** — 摩擦2・3 の初報で「`stylingWith()` が @stylistic を持つので落ちる」と wip ブランチに書いたが、実測すると **`stylingWith()` の @stylistic は 0件**。styling 軸を含めても config はロードする。**原因を特定する前に書いた推測が、翌セッションでは前提として扱われかけていた。** 撤回して #189 に記録した。
- **偽 green を1回踏んだ（深夜）** — プラグイン再定義の検証で対象ファイルを `.jsx` にしたため、**既定の lint 対象外で素通りし exit 0** になった。「両方通った＝参照は関係ない」と一度誤読した。`.js` に替えて再測し、参照不一致でのみ落ちることを確認。**exit 0 は「検査が通った」ではなく「検査が走った」の証明が要る**（G-6 を自分の probe に適用し忘れた）。
- **中継 GO を実行しなかった（誤りではなく判断）** — hub 経由の「施主 GO が出た」を実行せず本人確認を求めた。理由は ①証跡が hub の自己申告のみ ②**同じゲートで数時間前に hub が誤って撤回したばかり** ③publish は不可逆 ④施主が fleet セッションに居る、の4点。hub は「差し戻しは正しい・hub の誤りだった」と撤回し、`_work/advice.md` に収載した。

## 追補（終業後）— 施主総裁定と 15本束マージ

日報を締めた**後**に施主総裁定「全部GO」が降り、その実行までを本セッション内で終えたので追記する。

- **項25 = 契約v1凍結成立**（批准4前提 (a)(b)(c)(d) すべて充足・宣言発効）。**項22 = 15本束のマージ承認**。
- 🔴 **中継された GO をそのまま実行しなかった。** 裏取りできたのは `delete_branch_on_merge=true` への
  変更と board の記録だけで、**どちらも hub 自身の記述**だったため。施主から「群ごとに順次」という
  直接の指示を得てから実行した。今朝 publish で同じ判断をして hub が「差し戻しは正しい」と撤回した
  のと同じ形を、自分の側でもう一度通した。
- **15本を4群で全部マージ**（計器の偽陽性 #194/#196/#198/#197 → 配布物の根治 #192/#199/#200 →
  機能追加 #195/#181 → 文書 #185/#183/#191/#201/#202/#188）。**open PR は 0本**。
  マージ後の main は `npm run check` exit 0・**35 files / 517 tests**・AM-2 gate PASS〔自分で実測〕。
- **途中で3回止めて直した**（同じファイルを触る PR が多く、textual に解けても意味が壊れうるため
  1本ずつ main へ追随させて CI を確認する形で進めた）:
  1. **#198 の `index.ts` 衝突** — #196 の export と #198 の export が同じブロックに追加されていた。
     **どちらも消さず両方残す**のが正解。
  2. **#181 の2ファイル衝突** — `cli.ts` / `nene2-check.test.ts` で `--remeasure` の追加が束マージぶんと
     隣接衝突。origin/main 側が空だったので #181 の追加を残した。
  3. 🔴 **#185 で CI が落ちた。落ちたのは私が #196 で書いた陽性対照**で、判定に
     `差異登録なき緩和` という**文言そのものを pin していた**。#185 はまさにその文言を直す PR なので
     **これは正しい失敗**。違反行の本質は「実効 severity N < canonical M」の**形**なのでそちらへ変更した。
     → **#184 の教訓（検査器の文言は実装の要約であって契約ではない）が、そのまま自分のテストに
     当てはまった。** 今日 deal に対して言ったことを、自分のコードで踏んだ。
- **bump / publish は混ぜていない**（hub 指示どおり）。main には未出荷の変更が 15本ぶん入っている。
- `docs/todo/current.md` を**現況へ上書き**した（#203）。日報＝過去形・`current.md`＝現在形の役割分担どおり。

## 今日いちばんの学び

**「存在を機能の証拠として読まない」** — 一夜で7層の標本が出た。

| 層   | 断絶                                                                                   |
| ---- | -------------------------------------------------------------------------------------- |
| 供給 | 台帳に登録した → 供給されない（#159）                                                  |
| 検査 | 台帳に登録した → 検査されない（#176）                                                  |
| 更新 | 下げても記録できない（#176 → #181）                                                    |
| 参照 | 「差異登録なき」と言う → 差異登録を読まない（#184）                                    |
| 配布 | 実装した → publish されていないので艦で使えない                                        |
| 配線 | 依存にある・import している → canonical が効いていない（records）                      |
| 導入 | 導入済み3艦が green → **新規導入が通る証拠ではない**（#189・同日中に自分へ返ってきた） |

共通の根因は **「登録経路のテストはあっても、登録が効くことの負テストが無い」**。以後、検査器を足すときは陽性対照（故意に壊して FAIL することの確認）を必ず同梱する＝**1回効かせて観測してから通す**。

そして #184 を止められたのは、**deal が「この理解で合っていますか」と聞いてくれて、その一言で実装を開いたから**だった。文言は実装の要約であって約束ではない。**聞かれなければ私も踏んでいた。**

## 明日への引き継ぎ

- 🟢 **#189 の切り分けは完了**（深夜セッション・上の追補）。**初手は摩擦2 の hub 裁定待ち** — `jsx-a11y` の参照層を standards 側（module default 登録）に寄せるか、製品側に薄い層を要求するか。全艦に効くので単艦で決めない。裁定が来たら **green 3艦での参照一致を実測してから** PR を出す。
- **origin / field のゲート導入 PR 2本**（fleet 手番・号令済み）。基点は origin main `b5678ac`（revert 着地済み・クリーン）。**再着地時の修正2点**: ①降格ブロックは `off` を保持する形へ（#189 の C 案）②「styling 軸は展開しない」のコメントは誤帰属なので撤回し、styling を含めて展開する。i18n 軸のみ摩擦2 の裁定待ちで保留。**#192 のマージ・publish は待たなくてよい**（origin は stylelint devDep で回避済み＝(c) 緑化は publish にぶら下がらない）。
- **hub 新規発注（#189 の後・急がない）**: マージ直前の **CI 完了機械確認ラッパー**の設計・配布（origin #464 の D 案・hub 裁定済み）。private×無料プランで branch protection が使えないため、Stop hook pilot と同じ「人間ゲートの手前に機械ゲート1段」の型と配布経路を流用する見立て。hub は「tooling 配布であって standards 条文ではない＝mustTotal 不変・凍結に抵触しない」と理解している。**この理解でよいかは fleet 側で条文射程を確認してから着手する。**
- **次 minor 波（2.3.0 候補）に3本待機**: #181 `init --remeasure` ／ #183 provenance ／ #185 文言修正。
- **凍結は継続**: 新文書・新条文・新検査器は「(c) 緑 → 契約v1凍結成立」後の最初の patch 波まで。#160〜#165・#168・#170・#177 と、06 テスト章・07 セキュリティ章（#170 で顕在化）が待機中。
- **(c) の道筋**: #189 の切り分け → origin/field の導入 PR → (c) 緑 → 契約v1凍結成立。契約v1凍結の成立は #189 のぶん後ろへずれる。**ただし新規導入経路が初めて実地に出て摩擦を全部吐いた**ので、凍結してから踏むより今夜踏めたほうがよかった（hub 評）。
