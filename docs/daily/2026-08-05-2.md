# 2026-08-05（夜） — 届いていなかった指示書と、空き番に見えた番号

> **対象期間**: 08-05 19:0x（起動・repo-status）〜 同日夜（店じまい）。
> ⚠️ **同一日3本目**です。前セッションは 08-04 夜〜08-05 03:04（`2026-08-05.md`・PR #242 マージ済み）。
> その後 hub が店じまいを宣言し、**17:27 / 18:48 に指示書2通が発行された**ため本セッションが立ち上がりました。
> 命名は hub 裁定（08-05 夜）の**連番**形式 — ファイル名は終了日の**ラベル**であってキーではなく、
> 切れ目の探索性はこの対象期間明記が担保する。**本セッションが本裁定の最初の適用例**です（#249・規約への1行は #248）。

起動時の repo-status で、**自艦宛ての指示書2通が未受領のまま約15時間経っていた**ことが分かった。relay 8834 が不達で、hub が指示書方式へ切り替えていた分である。そこから先はほぼ全部が「配る前に測る」の作業だったが、この日いちばん学びが大きかったのは**測ったつもりで測れていなかった2件**のほうだった。艦隊の allowlist を素の `grep` で数えたら14艦すべてが該当と出て、それは jsonc のコメントを拾っていただけだった。族の新条文に `EX-8` を割り当てたら、その番号は5日前から別の issue が予約していた。どちらも**自分で気づいて自分で直した**が、どちらも「台帳を見た」「grep した」という**それらしい手続きを踏んでいた**のが共通している。**手続きを踏んだことは、正しく測ったことを意味しない。**

## ヘッドライン

- 🔴 **hub 指示書2通が未受領のまま約15時間**だった（relay 8834 不達）。起動時の repo-status で拾い、当日中に完済。
- **指示書の5項を3分割して hub 裁定を取り、A / B / C すべて着地**。A = #243 / **PR #244**（open・**hub APPROVE**）、B = **#210 にコメント**、C = #245 → `_work/fleet-muster.md` M-2。
- 🔴 **EX の採番衝突を踏みかけた** — 台帳は `EX-1`〜`EX-7` しか載せておらず `EX-8` は空きに見えたが、**#214 が 07-31 から予約**していた。**EX-9 へ退避**し、台帳に**予約行**を足して可視化（hub 裁定で確定）。
- 🔴 **素の `grep` が jsonc のコメントを拾い、mh99 を「14艦すべて該当」と誤測した**。配列だけを抽出し直して **5艦**。**撤去済みの艦ほど誤検出される**という向きの誤りだった。
- **stylelint census を board 期限（08-08）より前倒しで実測** — 実走 **5艦** / **devDep はあるが一度も走らない 2艦** / 無い 8艦。
- **current.md を現況へ上書き**（#246 / **PR #247**・open・**hub APPROVE**）。滞留5本の完済とフェーズ2の全解禁を反映。

## 本日の成果（時系列）

### 1. 起動 — 指示書2通が15時間止まっていた（repo-status）

`ls -t /home/xi/docker/_work/handoff-fleet-*.md` の mtime を見て、**前回セッション終了（03:04）より後**に発行された2通を検出した。

| 発行 | ファイル | 中身 |
| --- | --- | --- |
| **17:27** | `handoff-fleet-strict-true-2026-08-05.md` | main の `strict=true` 化（艦隊15艦）を muster 配布に載せる |
| **18:48** | `handoff-fleet-allowlist-premise-rule-2026-08-05.md` | #229 の配布物へ条文1項＋追加4件 |

**relay が不達だと指示書へ切り替わる**という運用があり、それを起動時に見る手順が repo-status スキルに入っていたので拾えた。**装置は動いていた。**

起動時の実測（本レポートの前提となる数字・すべて fleet 実測）:

| 項目 | 値 |
| --- | --- |
| `npm run check` | **exit 0**（35 files・517 tests・AM-2 PASS） |
| branch protection | `strict: true` / contexts `["check"]`（protection API） |
| open PR | **5本**・全部 CI SUCCESS・**BEHIND は0本**（`git merge-base --is-ancestor` で5本とも追試） |
| ワークツリー | clean・未追跡0・stale ブランチ0（`git ls-remote` で照合） |

### 2. 照会 — 「muster 配布」の実体が存在しなかった

strict=true の指示書は「**fleet は muster 配布に載せること**」と書いていたが、**この repo に muster という配布物が無かった**。実測では `muster` の文字列は `docs/todo/current.md` の1件（運用上の言及）のみで、`packages/` にも `scripts/` にも `_work/` にも実体は無い。

載せ先の候補3つを理由つきで hub へ返した（推し = **`_work/` に実体を新設**。理由 = strict=true は規約条文ではなく**運用手順**で、読者は「起動した艦のリナ」であって CI ではない・版も要らない）。

🟢 **hub 裁定 = 推しどおり (b) 採用**。hub が `_work/fleet-muster.md` を新設し、指示書が**実体の無い置き場を指していた**ことは hub 側の落ち度と記録された。

**同時に、5項を3分割する案も承認された**（層が3つに割れており、1PR に混ぜると論点が3つ載るため）:

| | 内容 | 行き先 |
| --- | --- | --- |
| **A** | 例外の「当てはまらない理由」は執筆時点で実測する ＋ mh99 生死確認 | #229 の配布物・standards-patch |
| **B** | ゲートの射程／build-storybook を網羅ゲートと見なさない | **#210**（対比表） |
| **C** | 配布物の数値の作法（送り手側2項） | `_work/fleet-muster.md` M-2 |

### 3. A — EX-9 の条文化（#243 / PR #244・open・hub APPROVE）

> 例外（allowlist / waiver / suppression / baseline）の「この艦では当てはまらない理由」は**執筆時点で実測する**。
> 理由が**機構の制約**か**上流の事実**かを分けて書き、上流の事実なら**実測と日付を添える**。
> 確かめずに書かれた例外は**死んだ例外**になり、マッチせず沈黙する。

**EX-3 と対**に置いた（EX-3 = 出口＝解除条件の書き方／EX-9 = 入口＝前提の真偽）。
🔴 **EX-3 だけでは今回は防げない** — vault のエントリは解除条件を「壊れているものの名前」で**正しく**書いており、**正しい出口を持つ例外が、偽の入口で作られていた**。

**写経せず測り直した**（指示書自身が写経を禁じている）:

`npm view brace-expansion time`（**fleet 実行**）— vault のエントリ執筆は 07-31 12:56 UTC:

| 版 | 公開 (UTC) | 執筆との前後 |
| --- | --- | --- |
| 2.1.3 | 07-28 10:16 | **3日前に存在** |
| 1.1.17 | 07-29 10:45 | **2日前に存在** |
| 2.1.4 | 07-30 10:15 | **1日前に存在** |
| 1.1.18 | 07-30 10:17 | **1日前に存在** |

**vault の報告値と完全一致**した。**引用ではなく追認である。**

### 4. 🔴 誤測1件目 — 素の `grep` が jsonc のコメントを拾った

mh99 の生死確認を配るにあたり、**名指ししている艦の射程**を測ろうとした。最初の測り方:

```
git show origin/main:frontend/audit-ci.jsonc | grep -oE 'GHSA-[a-z0-9]+-...'
→ 14艦すべて YES
```

**14艦すべてというのは、この波が6隻着地した後の数字としてありえない。** そこで測り方を疑い、`census.py` の `strip_jsonc` を借りて **`allowlist` 配列だけ**を抽出し直した:

| | 素の grep | 配列のみ |
| --- | --- | --- |
| mh99 を名指しする艦 | **14** | **5**（contact / deal / origin / profile / records） |

原因は **jsonc のコメント中の「なぜ外したか」という記述まで拾っていた**こと。
🔴 **向きが最悪だった** — **きちんと撤去して理由をコメントに残した艦ほど、誤って「まだ持っている」と判定される**。

陽性対照（qwww が13艦で検出）と陰性対照（corpus は空配列・ファイルは読めている）を添えて確定値とした。
**#226 で自分が書いた「道具の単位を間違えると正しい側まで壊す」の再演**なので、条文の §4.2 に実例として収めた。

### 5. 🔴 誤測2件目 — 空き番に見えた `EX-8` は5日前から予約済みだった

条文に **`EX-8`** を割り当ててコミットまで済ませた。根拠は族台帳（`2026-07-29-serve-conventions.md` §1.3）に **`EX-1`〜`EX-7` しか載っていない**こと。

その後 current.md を読んでいて気づいた:

```
docs/todo/current.md:32  3. **#214 の族台帳収載**（EX-8）。条文化は W0b の章立て判断まで止める（hub 裁定）。
```

**#214 が 07-31 の時点で名乗っていた**（#214 タスク1「族への編入と ID（EX-8 か）の裁定 — hub」）。**hub 裁定待ちで台帳に反映されていなかっただけ**で、予約は先に存在した。

対処（PR #244 に同梱）:
1. #243 を **`EX-9`** へ退避
2. 台帳 §1.3 に **`EX-8` の予約行**（🔒 未確定・#214・hub 裁定待ち）を追加して可視化
3. 経緯を台帳・条文の誠実性注記・#214 のコメントの3箇所に記録

> **空き番の判定は「台帳を見る」では足りない。未確定の予約は台帳に載らないからである。**

**§1.3 が「D-2 / D3 / D4 が3義」を理由に `D-x` 続番を退けたのと、同じ型が同じ名前空間で再発しかけた**（#208 と同族）。これ自体が EX 族の形をしている——**採番したことは記録されたが、台帳へ反映されたことは検査されていなかった**。

🟢 **hub 裁定（同日夜）**: **先着が勝つ・入れ替え不要**（#214 = EX-8 / #243 = EX-9）。
併せて **5日滞留していた #214 の族編入も承認**された（「マージされている ≠ 本番に入っている」は族の形そのもの）。

### 6. B — #210 へ（コメント投函）＋ stylelint census の前倒し

項2（ゲートを足す前に射程を負テストで測る）・項3（build-storybook を網羅ゲートと見なさない）を対比表へ送った。**vault の 71/78 と records の 573/600 は引用せず**「各艦で測れ」の形にした（指示書の但し書き）。

**hub から同じ層の新ネタ（records #1052）が来たので、board 期限 08-08 の census を前倒しで回した。**

`origin/main` 固定・フロントを持つ **15艦**（fleet-tooling は frontend 無しにつき対象外）:

| 状態 | 艦数 | 艦 |
| --- | --- | --- |
| 🟢 `check` で実走する | **5** | NENE2 / deal / invoice / payout / vault |
| 🔴 **devDep にあるが config も script も無く一度も走らない** | **2** | **field / origin** |
| ⚪ そもそも無い | **8** | clear / concierge / contact / corpus / profile / records / serve / suite |

🔴 **field / origin が「導入 ≠ 実行」の教科書的実例だった。** 両艦とも `"stylelint": "^16.26.1"` を devDep に持ちながら、**設定ファイルがリポ全体に1つも無く**（陽性対照: 同じ検索が vault / deal では各1件ヒット）、**scripts にも `check` にも居ない**。

> **依存表だけを見る監査は「stylelint 導入済み 7艦」と報告する。実走は 5艦。**

配布形が2種に割れる（#229 の artifact A / B と同じ構造）。**α（8艦）= 新規導入 / β（2艦）= 依存は既にある・config と配線だけ。**
🔴 **「導入しましょう」の号令を流すと、β の2艦は「もう入っています」と答えて終わる。**

### 7. C — #245 起票 → `_work/fleet-muster.md` M-2

**M-2-a**（配布物の数値には測定時点を添える）／**M-2-b**（承認本数のうち同じ指示書が既に例外扱いした枝を送り手が明記する）。hub が予定地を作っておいてくれた場所を埋めた。

fleet 側から実例を1つ足した。同じ計測を18時間差で取ると:

| | 08-05 **00:52**（基準線 JSON） | 08-05 **19:08**（fleet 再実測） |
| --- | --- | --- |
| 艦隊の allowlist 合計 | **23** | **20** |
| mh99 を名指し | **8艦** | **5艦** |

→ ① は「hub が気をつける」話ではなく**装置の問題**である。`escape-hatch-census` は `measured_at` を JSON に持っているのに、**人が数字を引き写す瞬間に測定時点が落ちる**。M-2 はその落下点に置く歯止め。

### 8. current.md の現況上書き（#246 / PR #247・open・hub APPROVE）

**滞留5本の完済**（#213 / #219 / #223 / #228 / #242 — いずれも本日 hub がマージ）と、**vault #352 完了によるフェーズ2の全解禁**、**strict=true**、**muster 新設**を反映した。

「測るときの作法」に本日実踏の3件（道具の単位／空き番の判定／自分が配る数字の測定時点）を追加。

🔴 **日報 `2026-08-05.md` は書き換えていない。** 申し送りの「vault #352 が5レーンをブロック」は**執筆時点では正しかった記録**であり、**過去形の記録は追記も修正もしない**。現況の更新先は current.md である。

## 🔴 踏みとどまった判断（実行しなかったこと）

- **日報を書かずに hub へ照会した** — 「店じまい宣言＝セッション境界」を今日の自分に当てると本セッションは新セッションだが、`2026-08-05.md` が既にあり、**同一日3本目の命名に規約の答えが無かった**。連番を推しとして理由つきで投げ、裁定（(b) 採用）を待ってから書いた。
- **board を自分で書き換えなかった** — board は hub 管轄という理解なので、A の着地と census 完了を報告して判断を仰いだ（hub が2行に反映）。
- **「5艦に同じ誤りがある」と書かなかった** — 名指ししている ≠ 誤り。生死は各艦が自艦の tree で決める。
- **stylelint の「素通り」を実証したと書かなかった** — 測ったのは**配線の実在**まで。負テストは撃っていない。素通りの実証は records #1052 と vault の負テスト表＝**いずれも伝聞**であることを #210 のコメントに明記した。
- **`check` 以外の経路を見ていないことを開示した** — CI 単独ステップや pre-commit で走っている艦があれば census は過小評価になる。
- **CLAUDE.md の連番1行を今日入れなかった** — PR #247 が同じ箇所を編集中・**承認済みマージ待ち**で、衝突させると承認済み PR を触り直させる。**消えないように #248 として出口を作った**（検出したが出口を作らなかったために消えた事例＝ repo-status スキル §G の教訓）。

## 他艦へ効く一般形

| 一般形 | 出どころ |
| --- | --- |
| **例外の「当てはまらない理由」は執筆時点で実測する**（機構の制約 / 上流の事実を分ける） | vault #353 / clear（EX-9・#243） |
| **空き番の判定は台帳を見るでは足りない** — 未確定の予約は台帳に載らない | 本日の EX-8 衝突（#244） |
| **道具の単位を、測りたいものの単位に合わせる** — 素の grep は構文を知らない | 本日の mh99 誤測（#243 §4.2） |
| **依存表だけを見る監査は「導入済み」を過大に数える** | stylelint census の field / origin（#210） |
| **配布物の数値には測定時点を添える／承認本数の内訳は送り手が書く** | hub 自認＋vault パイロット（#245・muster M-2） |
| **指示書が指す置き場が実在するか確認してから着手する** | muster の実体が無かった件（照会①） |

## 📊 本日の数字

**fleet が自分で実測したもの**（測定日 2026-08-05・ref は `origin/main` 固定）

| 項目 | 実測値 |
| --- | --- |
| `npm run check` | **exit 0**（35 files / 517 tests・AM-2 PASS）— 起動時・PR 前・計3回 |
| branch protection | `strict: true` / contexts `["check"]`（protection API） |
| 起票 | **fleet-tooling 5本**（#243 / #245 / #246 / #248 / #249） |
| PR | **2本 open**（#244 / #247・どちらも CI SUCCESS・CLEAN・**hub APPROVE**・**マージは施主待ち**） |
| コメント投函 | **2件**（#210 = 分割 B / #214 = EX-8 予約の記録） |
| 艦隊の allowlist | **20エントリ / 14艦**（陽性対照 qwww 13艦・陰性対照 corpus 空配列） |
| mh99 を名指しする艦 | **5**（素の grep では 14 と誤測 → 配列のみ抽出で確定） |
| 18時間の差分 | allowlist **23 → 20** / mh99 **8艦 → 5艦** |
| `brace-expansion` の patch 公開時刻 | 2.1.3=07-28 / 1.1.17=07-29 / 2.1.4=07-30 / 1.1.18=07-30（vault 値と一致） |
| stylelint census | フロント **15艦**中 実走 **5** / devDep のみ **2** / 無し **8** |
| open issue | **54本**（起動時 56 → 本日 +5 起票・他は hub / 施主側で消化） |
| 本日この repo でマージされた PR | **5本**（#213 / #219 / #223 / #228 / #242 — **すべて hub が実施**・fleet は関与していない） |

**他艦・hub から渡された数字**（fleet では再実測していない）

| 出所 | 数字 |
| --- | --- |
| vault | 負テストの rc 表（不正 CSS で build rc=0 / 解決不能 import で build rc=1）・story 到達不能 **71/78（91%）** |
| records | story 到達不能 **573/600（95.5%）**・prettier 正規形の不正 CSS が全ゲート素通り（#1052） |
| clear | mh99 の 2.x は 2.1.3 が当初から存在＝エントリの前提が最初から誤り |
| hub | strict=true の適用15艦・payout の Dependabot 8本が「5 CLEAN → 7 BEHIND」へ変化・vault パイロットの承認14本 vs 実削除12本 |

## 申し送り

**次の入口は #239 の清書 → doc PR**（board due **08-12**）。横断規律「テスト環境と本番環境の差異は、差異そのものを検査対象にする」。**参照実装が2層そろっている**（DDL 層 = clear #404 / SQL 層 = clear #417・PR #422）。

次いで **#232 / #234 の実装設計**（フェーズ2・vault #352 完了により**解禁済み**）、**#231 / #225 の doc PR**、**#229 の結果回収**、**#237 の2回目**（#229 の波が着地してから）。

**stylelint census の残 = α / β の配布形設計**（board 行・due 08-08・fleet 持ち）。β（field / origin）は
**「導入しましょう」では動かない**ことを号令の文面に必ず入れる。

**施主手番**: **PR #244 / #247 のマージ**（どちらも hub APPROVE 済み）／ **tokens 1.3.0 の publish**（#213 マージで材料がそろった・#133 と束ねてから）／ **nene2-standards 2.3.0 の publish 判断**（#221）。

**hub 手番**: EX-8 の予約行を 🔒 → ✅ へ昇格（#214 の条文化 PR と同時でよい・急がない）。

出口の決まっていない齟齬は無い。
