# 2026-08-19 — 渡された数字が2つ外れていて、どちらも実測で止まった

> **対象期間**: 08-19 夜（施主が `/repo-status` で起動）〜 08-19 深夜。本日は **1セッション**につき連番なし
> （粒度規約・hub 裁定 2026-08-05 / #241 / #249）。前セッションは `docs/daily/2026-08-15.md`。
> **08-16〜18 は自艦の稼働なし**（3日空き）。

現況把握で起動し、hub から期限切れの3玉（`composer.lock` 追跡 / gitleaks 8.24.3 検証 / Dependabot 棚卸し）を
受けて全部片づけた一日だった。ただし本日いちばん効いたのは玉を進めたことではなく、**着手前に測ったこと**である。
玉①では「先頭艦の次は field、理由は field だけ実害が出ているから」と渡されたが、**その実害は再現しなかった**——
制約が `^1.11` である以上、申し送りにあった `v1.8.2` は**構造的に入りようがない**。玉③では「121件（high 50）」を
渡されたが、**判断の単位で数え直すと 35** だった。どちらも hub が裏取りのうえ撤回・訂正している。

**装置は正しく動いていた。外れていたのは、装置を通っていない数字のほうだった。**
玉①の6艦という母集団も、実測すると `type: library` の艦と PHP コードが0本の艦が混ざっており、**3艦**が正しかった。

## ヘッドライン

- **`composer.lock` 追跡を3艦へ配布**〔自分で実測〕。**PR 4本・全 CI 緑・いずれも施主承認待ち（マージされていない）**:
  concierge #230 / field #164 / profile #135 / serve #215。**4艦とも `composer audit --locked` = 0件・exit 0**。
- 🔴 **配る前に母集団の齟齬を3件見つけ、6艦 → 3艦に確定した**〔自分で実測・hub が裏取りのうえ board を訂正〕。
  ①field の「実害」は再現せず ②`records-commercial` は **`type: library`**＝是正対象でない ③`trace` は **`.php` 0本・lock も無い**。
- 🔴 **gitleaks の罠2つは、性質が違った**〔自分で実測・8.24.3 と 8.30.1 の両方で〕。
  罠1（`[[allowlists]]` 複数形）は **8.24.3 固有**、罠2（1 allowlist 内の条件が OR）は **版非依存の恒久の意味論**。
  ⇒ 「新しい gitleaks なら安全」は成立しない。**第3の罠**（既定ルールセットの版差）も発見（#282）。
- **Dependabot は 121 → 35 判断まで潰れた**〔自分で実測〕。**最大リポ `records-site` の 44件は 22×2 の完全な二重計上**。
  正本 = `_work/reports/2026-08-19-dependabot-alerts-triage.md`。
- 🔴 **`COMPOSER_NO_AUDIT: 1` が4艦で再現**し、**同一 run で「暗黙は死んでいるが明示は生きている」を CI 上で確定**（#281）。

## 本日の成果（時系列）

### 1. `/repo-status` — 齟齬2件と、その出口

- 🔴 **README §状態に可変な進捗が残っていた**（「残るのは (c) 素振りレーン」）。hub 裁定 08-15 では **board が正**で、
  そもそも README は進捗を書かない方針（#55 / #152）。**#55 / #152 の族3件目**として **#280 を起票**。
- **マージ済み PR のローカル残骸ブランチ3本**（upstream `gone`）。
  ⚠️ `git merge-base --is-ancestor` は3本とも「main に未収録」と出るが、これは **squash マージだから**で、
  対応 PR（#275 / #277 / #279）は全部 MERGED。**施主 GO を得て削除**。
- リモートは `git ls-remote` で **`main` 1本のみ**＝クリーン。

### 2. 玉① `composer.lock` 追跡 — 先頭艦 concierge（#229 / PR #230）

手順⑥までを1艦で通した。**ゲートを足す前**に測った値:

| 測ったこと | 結果 |
|---|---|
| 既存 lock と `composer.json` の整合 | `composer install --dry-run` = `Nothing to install`（**再生成不要**） |
| `composer audit --locked` | **0件・exit 0**・**0.59〜0.60 秒**（3回） |
| 陽性対照A（他艦） | `NeNe` が smarty **2件を検出** |
| 陽性対照B（**自リポの lock に `symfony/yaml v5.4.0` を注入**） | **3件検出** |
| ゲート健全性 | 検出あり=**exit 1** / clean=**exit 0**（low でも落ちる） |

🔴 **path 型の懸念を実験で潰した。** backend CI は毎回 NENE2 を `git clone --depth=1` するので、
**lock に焼かれた reference と CI の clone は必ずずれる**。再現したところ、**clone `17c8187` / lock `98869b1e` の
ずれたまま `composer install` は exit 0 で完走し、lock も書き換わらなかった**。
⇒ **lock を書き戻すのは `update` であって `install` ではない。**
参照実装3艦（payout / records / suite）が**3艦とも別々の sha を焼いた lock を追跡している**ことも確認した。

### 3. 玉① — 配る前に止まった（母集団の齟齬3件）

残り5艦を先に測ったところ、**board 行が玉の根拠にしていた「6艦とも `type: project`」が成立していなかった**。

| 艦 | 実測 | 出口 |
|---|---|---|
| `field` | 制約 `^1.11`・ローカル lock も `vendor` も **v1.11.0**・`composer check` **exit 0** | 🔴 **実害は再現せず**。`^1.11` は v1.8.2 を解決できない＝**構造的に不可能** |
| `records-commercial` | 🔴 **`type: library`**・nene2 は **require-dev のみ** | **除外**（ライブラリが lock を追跡しないのは Composer 公式の推奨どおり） |
| `trace` | 🔴 **`.php` 0本・`composer.lock` が存在しない**・`^1.5` の化石 | **待機**（生死判定が施主手番・`due:2026-08-21`） |

hub が裏取りのうえ **3件とも受け入れ、board を 6艦 → 3艦へ訂正**〔hub 申告〕。
field の実害については **hub が転写の鎖を4段さかのぼり、どこでも実測されていなかったことを確認**している〔hub 申告〕。

**自己訂正1件**: `records-commercial` の `.php` を最初 `find` で数えて **123本**と出したが、
**うち 115本は `.phpstan.cache` の中身**だった。**追跡下の実数は 8本**で board が正しい。**送る前に潰した。**

### 4. 玉① — 3艦へ配布（PR 4本・すべて未マージ）

| 艦 | Issue / PR | 中身 |
|---|---|---|
| concierge | #229 / **PR #230** | lock 追跡 ＋ audit 独立ステップ |
| field | #163 / **PR #164** | **lock 追跡のみ**（audit は分離） |
| profile | #134 / **PR #135** | **lock 追跡のみ**（audit は分離） |
| serve | #214 / **PR #215** | lock 追跡 ＋ audit 独立ステップ ＋ schedule コメント更新 |

**4艦とも 0件・exit 0 / 0.58〜0.60 秒 / 86 packages**（concierge のみ 89）。**4艦それぞれで陽性対照**を取った。
**どの艦も版を選び直していない**（4艦とも `--dry-run` が整合だったので既存 lock を1バイトも変えず採用）。

🔴 **audit を分けた艦がある理由**: field / profile は `schedule` が `frontend-ci.yml`、PHP が `backend-ci.yml` にあり、
backend へ足すと**週次再検査に乗らない**。`frontend-ci.yml` は Node 専用なので PHP 監査を足すのは構成として正しくない。
⇒ **lock 追跡だけ先行**させ、理由を **PR 本文に書いた**（board 行の中だけに理由が残ると流れるため）。
**serve だけは単一 `ci.yml` に schedule と PHP が同居**しており、**穴を作らずに完結できた**。

hub 側の独立実測（A群6艦＝schedule と audit が別 workflow / B群5艦＝schedule 自体が無い・計 **11艦**）と
**別経路で一致**した〔hub 申告〕。

### 5. 玉② gitleaks 8.24.3 の検証（#282）

8.24.3 のバイナリを取得し、**8.30.1 と両方**で同一の複製リポに通した。
複製には**両版が確実に検出する標準形の stripe 鍵**を新コミットで足し、**例外が広がれば本物の鍵が素通りする**形にした。

| config | 8.24.3（CI固定） | 8.30.1（手元） |
|---|---|---|
| 陰性対照: allowlist 無し | 2件 | 2件 |
| **罠1**: `[[allowlists]]`（複数形） | 🔴 **2件＝効かない** | 1件＝正しく効く |
| **罠2**: `commits` ＋ `paths` | 🔴 **0件＝本物の鍵が素通り** | 🔴 **0件＝同じく素通り** |

⇒ **罠1は版固有／罠2は版非依存の恒久の意味論**。board は両方を「8.24.3 と現行の差」として括っていたが、
**罠2について不正確**だった。hub が条文を「1つの allowlist に条件は1つだけ」へ書き換え、
`_work/reports/2026-08-19-gitleaks-allowlist-rules.md` へ恒久化した〔hub 申告〕。

🆕 **第3の罠**: `stripe-access-token` の許容長が版で違い、**8.24.3 だけが本体41字を検出する**。
**罠1と向きが逆**なので、「手元は甘い/辛い」と一方向に覚えると両方外す。
⇒ 正しい言い方は **「手元と CI は別の検出器であり、どちらが厳しいかはルールごとに違う」**。

**自己訂正1件**: この第3の罠を最初に踏んだとき「8.30.1 が秘密を見逃す」と読みかけた。**誤り**で、原因は
①**gitleaks が cwd の `.gitleaks.toml` を自動で読む**ため cwd 依存で結果が変わる ②**テスト鍵が版差の影響を受ける長さだった**、
の2点。両版が検出する鍵に差し替え `--config` を明示して測り直した。**送る前に潰した。**

### 6. 玉③ Dependabot の棚卸し（分類まで・是正しない）

**数え方が3つある**ことを先に確定させた。

| 数え方 | 値 |
|---|---|
| ① 延べ alert（GitHub 画面の数） | **98** |
| ② (リポ, advisory) の実数＝直す箇所 | **66** |
| ③ advisory の実数＝**判断の数** | **35** |

渡された「121件（high 50）」は**①で、かつ5日前の値**だった。
🔴 **`nene-records-site` の 44件は `lp/` と `docs/` の lockfile 2本による完全な二重計上**。
🔴 **未計測が 22 リポある**（HTTP 403 =「Dependabot alerts are disabled」）。**0件ではない。**

**到達可能性**は `scope` を鵜呑みにせず現物の `package.json` を全数走査した。
**11パッケージ中7つは直接宣言なし＝推移的**、`vite` / `postcss` は **devDependencies のみ**、
**直接依存は `astro` と `dompurify` の2つだけ**で、**ブラウザへ配られうるのは `dompurify`（articles）1つ**。
**修正版が存在しない advisory は 0件**。

### 7. `COMPOSER_NO_AUDIT` の確定（#281）

concierge PR #230 と serve PR #215 の run ログの env に **`COMPOSER_NO_AUDIT: 1`** がそのまま出ており、
**同じ run で明示ステップは正常に走っていた**。sakura FT・clear を含め **4艦で再現**。
⇒ **「暗黙は死んでいるが明示は生きている」は仮説ではなくなった。** ローカルの推定ではなく **CI 上で確定**。

## 📊 本日の数字

| 項目 | 値 | 出所 |
|---|---|---|
| 出した PR | **4本**（concierge #230 / field #164 / profile #135 / serve #215） | 自分で実測 |
| うちマージ済み | **0本**（**全部が施主承認待ち**） | 自分で実測 |
| 起票した Issue | **7本**（自艦 #280 / #281 / #282 / #283、他艦 concierge #229 / field #163 / profile #134 / serve #214） | 自分で実測 |
| `composer audit --locked` | **4艦とも 0件・exit 0**・0.58〜0.60 秒 | 自分で実測 |
| 陽性対照 | **4艦それぞれで 3件検出・exit 1** | 自分で実測 |
| lock 追跡の対象艦 | **6艦 → 3艦**（+ 先頭艦 concierge） | 自分で実測・hub が裏取り |
| gitleaks の罠 | **2つ → 3つ**（うち1つは版非依存と判明） | 自分で実測 |
| Dependabot | 延べ **98** / 直す箇所 **66** / **判断 35** | 自分で実測 |
| Dependabot の未計測リポ | **22**（alerts 無効） | 自分で実測 |
| `COMPOSER_NO_AUDIT` の再現艦数 | **4** | 2艦は自分で実測・2艦は既報 |
| 削除した残骸ブランチ | **3本**（施主 GO 済み） | 自分で実測 |
| 渡された数字のうち外れていたもの | **2件**（field の実害／121件の数え方） | 自分で実測・hub が撤回・訂正 |
| 自己訂正 | **2件**（`.php` 123→8本／「8.30.1 が見逃す」の誤読）。**どちらも送る前に潰した** | 自分で実測 |
