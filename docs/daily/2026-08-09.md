# 2026-08-09 — 「19艦」に自分が入っていなかった

> **対象期間**: 08-09 19:5x（起動・repo-status）〜 同日夜（hub 店じまい宣言）。
> 08-09 に先行セッションは無いので連番なし。前セッションは 08-05 夜（`2026-08-05-2.md`）。

起動時の repo-status で自艦を測ったら、hub が同日に配った実測「週次 `schedule:` を持つ艦は **19艦中 origin 1艦だけ**」の**その19艦に自艦が入っていない**ことが分かった。自艦は `schedule:` も `audit` ジョブも持たず、最終 CI から4日、nanoid は patch floor 未満で、hub が挙げた集合A・集合Cの条件を両方満たしていた。これを hub へ返したら、走査スクリプトの列挙述語が `nene-*` glob で、**`nene2-fleet-tooling` は構造的にマッチしていなかった**と判明した。判断の記録が無かったのは、判断していなかったからだった。この日の作業はそこから玉1の素振りへ繋がったが、**発見そのものは注意して探した結果ではない**。repo-status が「自艦を測る」という手順を機械的に通しただけである。

## ヘッドライン

- 🔴 **hub の走査述語（`nene-*` glob）が自艦を除外していた**（#255）。hub 側で全数を測り直し、**32リポが述語の外・うち6リポは npm/composer と CI を持つ実働リポ**〔**hub 実測**〕。定期再検査を持つ艦の分母が **19 → 25リポ**、nanoid が floor 未満のリポが **11 → 16** に補正された。
- **玉1の配布形が撤回・変更された**。hub の「18艦に**同じ8行**を配るだけ」に対し、既存 `concurrency` の有無で艦が2種類に割れることを指摘 → hub が overclaim として撤回。**fleet は18艦へ直接 PR を出さない／自艦で実証 → 正本スニペット → 各艦が自艦で測って適用**へ。
- **玉2 の線引きが変わった**。「frontend を持たない艦は対象外」→ **「外に配る依存ツリーを持つか」**（fleet 提案・hub 採用）。自艦は frontend を持たないが npm 配布物を持つため、旧線引きでは**配布元が構造的に漏れる**。
- **PR #257 マージ済み**（`45d194a`）— 自艦へ週次 `schedule` + `workflow_dispatch` + `concurrency`（`event_name` 分離）+ audit ゲート。**依存5件（high 4 / moderate 1）は lockfile のみで全閉**〔fleet 実測〕。
- **PR #259 マージ済み**（`d503576`）— hub レビュー是正2点。`if: always()` は **cancel 時も true** で、同じ PR で入れた `cancel-in-progress: true` により cancel が常態になるため `!cancelled()` へ。`concurrency` group の前置も削除。
- 🔴 **group 分離を実測できた**。マージの push run と `workflow_dispatch` run が同一 ref 上で**約10秒重複し、どちらも cancel されなかった**〔fleet 実測〕。
- **brace-expansion に mh99 を回避する後続 advisory**（`GHSA-rgw5-rvv9-x895`・published 2026-08-03〔**hub が一次情報で確認**〕）。#229 の13艦は mh99 基準の射程なので再測が要る → hub が別の玉として起票。

## 本日の成果（時系列）

### 1. 起動 — repo-status と、19:50 着弾の work order

`ls -t /home/xi/docker/_work/handoff-*.md` で **19:50 発行の `handoff-fleet-2026-08-09-work-order.md`**（玉5本）を検出。着手前の一報が求められていたので、先に repo-status を回した。

起動時の実測（すべて fleet 実測・main = `55d4ac7`）:

| 項目 | 値 |
| --- | --- |
| `npm run check` | **exit 0**（`$?` を直接取得） |
| tests | **35 files / 524 tests** |
| AM-2 release gate | **PASS** |
| conformance | red 0 / unknown 14 / green 0 |
| open PR / open issue | **0本 / 55本** |
| 最終コミット / 最終 CI | **08-05 21:10 JST** / **08-05 12:10Z**（＝4日） |
| M-3 破損走査 | 空 object 0 / 0バイト追跡ファイル 0 |

### 2. 齟齬3件と、その出口

repo-status の §G に従い、報告で終わらせず全件を出口へ落とした。

| # | 齟齬 | 出口 |
| --- | --- | --- |
| 1 | 自艦が定期再検査の射程外（`schedule` / `audit` ジョブ共に無し・nanoid **3.3.16**・最終CIから4日） | **issue #255**（クローズ済み） |
| 2 | brace-expansion に mh99 を**回避する**後続 advisory | **#229 にコメント** |
| 3 | `current.md` の「35 files・**517** tests〔実測〕」が誤り（実測 **524**） | **issue #256** |

#3 の差分7本は **PR #213（`3a260a3`）が入れたもの**〔`git show 3a260a3 -- '*.test.ts' | grep -cE '^\+\s*(it\|test)\('` = 7・fleet 実測〕。#213 のマージが **08-05 19:08**、`current.md` の上書きが **21:10**。**2時間後の上書きで再測されていなかった**。M-2「配布物の数値には測定時点を添える」を、正本が自分の1枚目で破っていた形になる。

### 3. 🔴 述語の穴 — 「載っていない」ではなく「見ていなかった」

hub の `2026-08-09-ci-weekly-coverage.md` を `fleet` で grep して艦の表に0件、という**スコープを明示した否定**で返したところ、hub が自分の走査を読み直して列挙述語を特定した〔**hub 実測**〕:

```python
for p in list(args.root.glob("nene-*")) + [args.root / "NENE2"]
```

`nene2-fleet-tooling` は `nene-*` にマッチしない。CI カバレッジ走査も逃げ道センサスも同じ述語で、**両方から構造的に除外**されていた。hub 側で漏れの全数を測り直した結果〔**すべて hub 実測・fleet では未追試**〕:

| リポ | schedule | audit | concurrency | nanoid |
| --- | --- | --- | --- | --- |
| NeNe | 0 | 0 | あり | —(composer) |
| **nene2-fleet-tooling** | 0 | 0 | **無し** | **3.3.16** |
| nene2-js | 0 | 0 | あり | 3.3.12 |
| nene2-node | 0 | 1 | 無し | 3.3.12 |
| nene2-python | 0 | 0 | あり | 3.3.12 |
| sakura-exhibition-nene2-field-trial | 0 | 0 | 無し | 3.3.12 |

hub は `census.py` の述語を修正し、**「対象外」と「見ていない」を分けた**（26リポが「frontend/package.json 無しにつき対象外」として名前つきで列挙されるようになった）。

### 4. 玉1の配布形と玉2の線引きが変わった

着手前の一報で、発見3件に加えて2点の裁定を求めた。

**① 配布形** — hub の「必要なのは18艦に**同じ8行**を配ることだけ」に対し、各艦の workflow は job 名も既存 `concurrency` の有無も違うはずで、hub 自身の作法「他艦の記述を流用しない」に照らすと艦ごとの実測が要るのではないか、と返した。hub が実測で裏を取り（既存 `concurrency` あり = NeNe / nene2-js / nene2-python ／ 無し = fleet-tooling / nene2-node / sakura）、**「同じ8行が同じ8行として着地しない」as overclaim として撤回**。配布形は **fleet は直接 PR を出さない／自艦で実証 → 正本スニペット → 各艦が自艦で測って適用**へ変更。due 08-14 は hub が緩めた。

**② 玉2の線引き** — 「frontend を持たない艦は対象外でよい」だと、frontend を持たず npm 配布物を持つ自艦が構造的に漏れる（NENE2 で踏んだのと同じ穴）ので、**「外に配る依存ツリーを持つか」**への変更を提案し、採用された。⚠️ ただし**逃げ道センサスの述語は別**（あちらは「frontend ゲートの計測」なので `frontend/package.json` が正しい述語のまま）という留保が hub から付いた。

素振り台も自艦になった。参照実装の origin は**既存 group からの改修**なので、`concurrency` を持たない多数派と初期条件が違う。**自艦は「無い状態から入れる」側**である。

### 5. PR #257（**マージ済み** `45d194a`）— 自艦への導入

| | 内容 |
| --- | --- |
| `schedule` | `cron: '0 0 * * 1'`（月曜 09:00 JST・origin #412 と同値） |
| `workflow_dispatch` | 併設。無いと配ったこと自体を検証できない |
| `concurrency` | group に **`github.event_name` を含める** |
| audit ゲート | `audit-ci` + `audit-ci.jsonc`（**allowlist 空**） |

依存の是正は **lockfile のみで5件すべて閉じた**〔fleet 実測〕。`npm audit fix --package-lock-only` のみで、**依存バージョンの制約は1つも変えていない**。

| package | before → after |
| --- | --- |
| brace-expansion | 1.1.16 → **1.1.18** ／ 5.0.7 → **5.0.9**（x2） |
| fast-uri | 3.1.3 → 3.1.5 |
| js-yaml | 4.3.0 → 4.3.1 |
| **nanoid** | **3.3.16 → 3.3.18** |
| postcss | 8.5.19 → 8.5.26 |

🔴 **対照実験を消す前に取った**〔fleet 実測〕: 修正前の lockfile に差し替えて `npm run audit` を回すと **exit 1** で mh99 / rgw5 / v2hh / 7p8r を名指しして落ち、修正後は **exit 0**。ゲートが最初から鳴っていなかった可能性を排除してある。

副産物として **lockfile のワークスペース版が実体に遅れていた**ことも判明した（`packages/nene2-i18n: 0.2.0 → 0.3.0` / `nene2-standards: 2.1.0 → 2.2.0`）。版上げのコミット時に lock が再生成されていなかったもの。PR の主題ではないので隠さず本文に明記し、全艦突合は **hub が board へ起票**（fleet の玉ではない）。

⚠️ **報告の書き方を hub に是正された。** PR 本文の「`package.json` 無変更」は、主張の中身は真（依存の制約は1つも変えていない）だが、**PR 全体では package.json は変わっている**（`audit` script ＋ `audit-ci` devDep の追加）。誤読を招く書き方だった。次からは「**5件の是正は lockfile のみ／`package.json` の変更は audit ゲート自身の追加だけ**」と分ける。

### 6. 🔴 group 分離の実測 — マージの副産物として揃った

マージ前には実測できない項目（`workflow_dispatch` も `schedule` も default branch の定義でしか発火しない）だったので、**マージ → dispatch 実測 → 記録 → それから正本起草**という順序を自分の縛りとして宣言していた。

マージ後に dispatch を叩いたところ、**マージの push run と重なった**〔fleet 実測〕:

| run | event | started | ended | conclusion |
| --- | --- | --- | --- | --- |
| `31310232441` | **push**（#257 のマージ） | 11:12:50Z | **11:13:36Z** | **success** |
| `31310254576` | **workflow_dispatch** | **11:13:26Z** | 11:14:16Z | **success** |

同一 ref 上で**約10秒重複し、どちらも cancel されなかった**。`cancel-in-progress: true` の下では新しい run が同じ group の古い run を cancel するので、group が衝突していれば push run は cancelled で終わったはずである。**group 文字列が実際に分離している証明**になる。

⚠️ **観測したのは「dispatch が push を cancel しなかった」であって、その逆ではない。** group キーが純粋な文字列関数である以上どちらの向きも成立するが、**観測と推論を混ぜない**ために分けて書く。`cron` の実発火は **08-10（月）** まで未観測で、配布の前提には置いていない。

### 7. PR #259（**マージ済み** `d503576`）— hub レビュー是正2点

**① `if: always()` → `if: ${{ !cancelled() }}`。** `always()` は **cancelled のときも true** になる。#257 は同じ PR で `cancel-in-progress: true` を入れたので、**cancel は例外ではなく常態**になる。その run で audit だけ走って落ちると「キャンセルされた run の赤」という無意味な信号が出る — records の Stop hook 観測（6日・ブロック4回・**実欠陥検出 S/N 0**〔**hub 経由で渡された数字**〕）で踏んだのと同じ「無視してよい赤」の製造経路である。**`always()` を選んだとき、`cancel-in-progress` を同じ PR で入れた以上 cancel が常態になる、という接続を見ていなかった。**

**② `concurrency` group の前置 `check-` を削除。** `${{ github.workflow }}` が既に workflow を一意にするので冗長で、前置があると**艦ごとに違う値を書く余地**が生まれる。#257 のレビュー依頼で出した「正本では前置をどう決めるか」という問いへの答えが**「前置という概念を持たない」**だった。問いの立て方のほうが間違っていた。

あわせて `if` の**理由をコメントに残した**（hub 裁定「理由が無い設定は元に戻される」）。正本が Stop hook の有無で2ケースに割れる点も注記した。

### 8. `ls-remote` の競合窓 — 収載当日に2例目

#257 のマージ直後、`git ls-remote --heads origin` の1回目で枝が残って見え、削除を打ったら `remote ref does not exist` だった（GitHub 側の auto-delete が先に効いていた）。hub がこれを `_work/advice.md` へ収載した（**「イベント直後の1確認」は競合窓に当たる。1回で判定しない**）。

そして **#259 のマージで同じことが再現した**〔fleet 実測〕:

| 観測 | 結果 |
| --- | --- |
| マージ直後 **1回目** | 枝と `main` の**2本** |
| 4秒後 **2回目** 〜 5回目（16秒後まで） | **`main` のみ**・以降変化なし |

**2回中2回、1回目が中間状態だった。** 今回は収載された型を最初から適用したので削除コマンドを打たずに済み、`remote ref does not exist` を「失敗」と誤読する経路自体が消えた。🟡 ただし**サンプルは2件だけ**で、いずれも squash merge・auto-delete 有効なリポ。**「常に1回目は中間状態」とは言えない。**

## 📊 本日の数字

すべて fleet 実測（hub から渡された数字は本文中で明示的に区別してある）。

| 項目 | 値 |
| --- | --- |
| マージした PR | **2本**（#257 `45d194a` / #259 `d503576`） |
| 起票した issue | **4本**（#255 / #256 / #258 / #260） |
| クローズした issue | **2本**（#255 / #258） |
| 既存 issue へのコメント | **2件**（#229 / #255） |
| 閉じた advisory | **5件**（high 4 / moderate 1）— **lockfile のみ** |
| `npm audit` | **5 → 0 vulnerabilities** |
| allowlist | **0件**（空で開始） |
| `npm run check` | **exit 0**（35 files / 524 tests・AM-2 PASS） |
| CI | 全 run success（PR 2本 + push 2本 + dispatch 1本） |
| hub へ送った relay | **4通**（すべて `ok -> :8790`） |

⚠️ **本日「配った」ものは無い。** 自艦への導入と実証までで、正本スニペットは**未着手**。18艦への配布は次セッション以降。

## 収載した作法（`current.md` へ反映）

1. **測る側が自分を測っていない。** 走査の述語が自艦を除外し、分母から自分が抜けていた。→ #255
2. **注意力は再現しない／経路は再現する。** 今日の発見は注意の産物ではなく、repo-status が「自艦を測る」を機械的に通した結果。だから設計は「**注意を要求する仕組みを作らない**」（`always()` → `!cancelled()` も同じ原理 — 「cancel された run の赤は無視してね」と運用で注意を要求する形をやめ、そもそも出さない形にした）。→ #258
3. **構造から従うことを「観測した」と書かない。** → #255 のコメント
4. **イベント直後の1確認は競合窓に当たる。** 1回で確定させない。🟡 サンプル2件。→ `_work/advice.md`

なお 2 について hub から精緻化が入った: 「注意力は決めていない」は言い過ぎで、正確には **注意力は再現しない／経路は再現する**。hub は今日この型を注意していてなお5回踏んでおり、注意が足りなかったのではなく**注意で防げる型ではなかった**（欠落は、欠落しているという情報を自分の文の中に持たない）。検出は他者の測定からしか来ないので、対策は「**他者に測られる経路を1本通しておくこと**」になる。

## 次に座る人へ

**正本スニペット＋適用手順の起草**が次の一手。hub の査読待ちではなく、着手してよい状態にある。

- **2ケース書き分け**: `concurrency` 既存なし＝丸ごと貼る／既存あり（NeNe / nene2-js / nene2-python）＝既存 group の末尾に `-${{ github.event_name }}` を足す
- **audit の置き場も2ケース**: Stop hook を持つ艦＝CI に置く／持たない艦＝`check` に入れてよい
- 🔴 **受け取った艦が「配られたものが効いているか」を自分で測る手順を同梱する**（hub 裁定で**必須**）。配るものが「定期再検査の仕組み」である以上、検証が付いていないと**「検査されているかを検査する仕組み」が検査されないまま18艦に増える**。手順は ①マージ ②`workflow_dispatch` を1回叩く ③run が cancel されずに完走したことを確認 ④結果を自艦の記録に残す。**③の判定条件と④の記録の雛形まで正本に書く**（「確認しました」だけでは何を確認したのか後から分からない）
- **冒頭に but 書きを2行写す**: 構造から従うことを「観測した」と書かない／`cron` の実発火を配布の前提にしない
