# 2026-08-14（2本目） — 止めた判断が3件、名義の誤読が1件

> **対象期間**: 08-14 17:5x（施主が `/repo-status` で起動）〜 08-14 夜（#267 / #268 の PR）。
> 本日1本目は `docs/daily/2026-08-14.md`（対象期間 = 08-13 23:5x 〜 08-14 早朝・依存監査レーンの前半）。
> hub の店じまいを挟んで再開したのでセッション境界が立っており、**連番の2本目**として書く
> （粒度規約・hub 裁定 2026-08-05 / #241 / #249）。

現況把握のつもりで起動したら、`current.md` が**17時間ぶん古かった**。前夜に撃った他艦への PR 10本のうち **7本が既にマージされていた**のに、ファイルは「全部未マージ」と書いていた。ここまでは想定内で、`current.md` を上書きすれば済む。**想定外だったのは、その7本を誰がマージしたかをあたしが誤ったこと**である。`gh pr view --json mergedBy` は `hideyukiMORI` を返す。だからそう書いた。実際にマージしたのは hub だった——**`gh` はどのリナのセッションでも施主の資格情報で動くので、GitHub 上の名義は常に施主に見える**。hub の指摘は「誰が検収したかの記録が狂う」で、これは正しい。次に同種の PR を出すとき「施主が見た前例がある」という誤った前提を積むところだった。

その後 hub から玉が3つ降りてきて、そのうち**1つは「撃たない」で終わった**。NeNe に依存脆弱性ゲートを足す玉で、安全弁（足す前に `composer audit` が 0件であることを実測する）に引っかかったからである。ただし**止めた時点で報告して終わらせなかった**のが今日の分かれ目だった。「何があれば 0件になるか」を測ったら、**緑の dependabot PR が5日間待っていた**。hub がそれを通し、あたしが測り直して 0件を確認し、それからゲートを足した。

## ヘッドライン

- 🔴 **マージ実行者を「施主」と誤記して hub に訂正された**。`gh` の実行者名義（`mergedBy`）は**全リナ共通の資格情報**であって行為者ではない。issue #267 の本文とメモリを訂正した。
- **【PHP-AUDIT】が 13艦中 12艦で着地**。最後の1艦 `NeNe` に **PR #792（マージ済み・hub が検収）**。残りは `nene-origin` #646 のみで、**Actions の課金停止でジョブが起動しない**ためブロック中。
- 🔴 **NeNe には安全弁が働いた**。`origin/main` の lock で `composer audit --locked` → **medium 2件・exit 1**〔fleet 実測〕。**足していたら main が即赤**になっていた。→ **足さずに報告**（hub の指示どおり）。
- 🟢 **そのブロッカーは既に解除可能だった** — dependabot **#790**（smarty v5.8.0→**v5.8.4**）の head lock で同じ検査を回すと **0件・exit 0**、パッケージ **83→83**、動いた2つとも昇格〔fleet 実測〕。hub が #790 をマージ → **測り直して 0件**〔fleet 実測・main `6e2c55e`〕→ #792 を撃った。
- 🔑 **ステップが実際に走った証跡を run ログから取った** — `unit / composer audit / No security vulnerability advisories found.`。**`composer check` に埋めていたらログに残らず、「監査が無い」と「監査したが何も出なかった」を外から区別できなかった。**
- **玉2（rgw5 再測）**: advisory を **GitHub Advisory GraphQL API の一次情報**で読み直し、**25リポの `origin/main` の lockfile を全数実測**。**未閉じは `NENE2`（#1647 で解消）と `nene2-js`（#136・08-15）の2つだけ**。「不在」の5リポは**陽性対照つき**で確認した。
- 🔴 **新発見: composer ゲートと npm ゲートは厳しさが非対称**。`composer audit` は素で全 severity を落とし、**`--audit-level` というオプションは存在しない**〔実測〕。npm 側（`audit-ci.jsonc`）は medium 以下を無視する。→ hub 裁定4点（現状維持・緩めるなら個別例外・**非対称は意図ではなく経緯**）。
- **齟齬の出口を4件**: 自艦 issue **#266 / #267 / #268**、`NeNe` issue **#791**。うち #791 は同日に PR #792 まで到達。

## 本日の成果（時系列）

### 1. `/repo-status` — 現況ファイルが17時間ぶん古かった

- `current.md`（08-14 01:12）の「撃ってあって、まだマージされていない PR **10本**」表に対し、実測は **7本 MERGED**（08-14 08:37–08:38Z）。
- ついでに **判例43（マージ後のブランチ残存）を消化**した。マージ済み7本の head を `gh api repos/<slug>/git/ref/heads/<branch>` で叩き、**7本とも 404＝削除済み**〔実測〕。**残存ゼロ**。
- `git branch -r` は6本を出すが `git ls-remote` は2本。**ローカルのキャッシュが4本古いだけ**なので、リモートの実態としては報告しなかった（skill の注意どおり）。
- 出口: **#266**（`main = <sha>` も自己陳腐化クラス）・**#267**（current.md の上書き）を起票。

### 2. 🔴 マージ実行者の誤記 → hub が訂正

- `mergedBy` が `hideyukiMORI` を返したので「施主がマージ」と報告・#267 の本文にもそう書いた。
- hub 訂正: **実行したのは hub**。`gh` が施主の資格情報で動くだけ。→ #267 の本文を訂正し、メモリ（`gh-actor-is-always-hide`）に収めた。

### 3. 玉1（NeNe）— 安全弁で止めて、解除条件を測って、撃った

| 段 | 実測 |
| --- | --- |
| 母集団述語の追試 | `composer.lock` が `origin/main` に **210,594 バイト**・workflow は `tests.yml` / `docs.yml` の2本 → **述語は真**（hub の申告と一致） |
| 安全弁（追加前） | `composer audit --locked` → **medium 2件・exit 1**（smarty CVE-2026-62996 / 62992） → **足さずに報告** |
| 解除条件の測定 | **#790 の head lock で 0件・exit 0**・パッケージ **83→83**・smarty v5.8.0→**v5.8.4**（↑）／ polyfill-mbstring 1.37.0→**1.38.2**（↑）・**降格0**・CI 2/2 SUCCESS |
| hub が #790 をマージ | → **測り直し**: main `6e2c55e` の lock で **0件・exit 0**〔fleet 実測〕 |
| 載せる前の機械検証 | YAML パース通過・`jobs` は2本健在・`unit` に `composer audit` が**1つだけ**追加・**消えた step 0**・`runtime-smoke` **11→11**・差分は**7行追加／削除0** |
| **PR #792** | `unit` **SUCCESS** ／ `HTTP runtime smoke` **SUCCESS** → **hub が検収してマージ** |

- 撃ち方は前夜と同じく **GitHub API のみ**（NeNe の作業ツリーには一切触れていない）。
- 出口: `NeNe` issue **#791**（測定値・参照実装・順序を本文に固定）。

### 4. 玉2（rgw5 再測）— 一次情報 → 全数実測 → 陽性対照

- advisory（GraphQL API）: high・published 2026-08-03・**`<1.1.18` / `>=2.0.0,<2.1.4` / `>=3.0.0,<3.0.6` / `>=4.0.0,<5.0.9`**。**hub の申告と完全一致**（＝渡された数字を追試して一致した例）。
- 25リポの `origin/main` lockfile を contents API で直読み。**未閉じ 2**（`NENE2` frontend / `nene2-js`）・**充足 17**・**brace-expansion 不在 5**・**lockfile 自体が無い 2**（`NeNe` / `nene-trace`）。
- 「不在」5リポは**陽性対照つき**: 全て lockfileVersion=3・文字列出現 0回、**同じ器械が clear では 8回拾う**。
- おまけ: **`nene-corpus-site` の `origin/main` の lock は 406 パッケージのまま**＝前夜の書き潰しは**スクラッチの写しだけで push されていない**ことを確定〔実測〕。

### 5. NENE2 #1647 — 本文を整備（マージは hub）

- 独立再実測: パッケージ **761→761**・動いたのは **5名前 / 7 lockfile エントリ**・**全部昇格・降格0**。
- hub 裁定（受ける・作り直さない）と理由3点、判例44② の教訓を本文へ。→ hub が `update-branch` して**マージ済み**。

### 6. 🔴 ゲート非対称の発見と裁定

- `composer audit`: 素で全 severity を落とす。**`--audit-level` は存在しない**〔陽性対照つき実測: `The "--audit-level" option does not exist.`〕。
- `audit-ci.jsonc`: `moderate: false`＝medium 以下を無視。
- **hub 裁定**: ①`--ignore-severity` で一律に黙らせない ②赤くなったら是正 or 期限つき個別例外で受ける（第1号は sakura FT の `symfony/yaml` low 3件）③npm 側を引き上げるのは今はやらない ④**非対称は「意図」ではなく「経緯」**として board 起票＋施主上申（hub の手番）。

### 7. 締め — #265 の順序整理と current.md の上書き

- 自艦 **#265**（#254 の条文化）が CI 緑のまま丸一日空いていた。**#267 が同じブロックを触る**ため、先に入れてもらった（hub がマージ）。
- hub の追加要求で、**#266 を #267 に同梱**。条文は個別禁止の列挙ではなく一般形にした:
  **「自艦の可変な計数（open PR・open issue・main の SHA）は書かず、取り方＝コマンドを書く」**。
  🔴 **但し書き**: 禁止は**自艦の計数だけ**。他艦の状態は残す（線を引かないと current.md が「何も書いていない紙」になる）。
  判定の軸は hub の言語化を採った ——**「その記述は、*自分の行動*によって古くなるか」**。

## 📊 本日の数字

| 項目 | 値 | 出所 |
| --- | --- | --- |
| 出した PR | **2本**（`NeNe` #792・自艦の本 PR） | 実測 |
| **マージされた**自艦発 PR | **9本**（うち本セッション中に hub がマージしたのは #1647 / #792 の2本） | 実測 |
| 起票した issue | **4本**（自艦 #266 / #267 / #268・`NeNe` #791） | 実測 |
| 全数実測したリポ | **25**（npm lockfile・`origin/main` 直読み） | 実測 |
| rgw5 未閉じ | **2 → 1**（#1647 マージで NENE2 が閉じ、残は `nene2-js`） | 実測 |
| 【PHP-AUDIT】 | **11艦 → 12艦 / 13**（残 `nene-origin`・課金ブロック） | 実測（hub も13艦を全数再測） |
| 撃たずに止めた玉 | **1**（NeNe への追加を安全弁で保留 → 解除条件を測って同日に解除） | — |
| 自分の誤り | **1**（マージ実行者の名義誤読） | hub が訂正 |
| フリートの allowlist | **7**（不変・玉3 は 08-15） | 実測（前夜と同値） |

## 明日（08-15）へ

1. **玉3 = react-router 7艦**（lockfile bump ＋ allowlist 撤去を1PR で・射程は実測済み）
2. **nene2-js #136 の作り直し**（brace-expansion の3件だけ・降格を持ち込まない）
3. **玉4（L3 作業デー・金曜）= 10艦ぶんの `registries.jsonc`**（順序を逆にしない）
4. **母集団述語の実装**（`(repo, ecosystem)` の組・**両向き**の回帰テスト）
5. 照会1〜4 の実行（corpus-site の測り直し／`vitepress` 鎖の材料／astro は別束／sakura FT の composer 側）
