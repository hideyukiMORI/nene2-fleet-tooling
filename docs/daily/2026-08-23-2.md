# 2026-08-23（2本目）— 部品を26本積んで、一度も画面で使っていない

> **対象期間**: **08-23 12:12（施主の `/repo-status` で起動）〜 14:2x**。**同日2本目**につき連番。
> 1本目 = `docs/daily/2026-08-23.md`（**08-22 21:40〜08-23 00:1x**・PR #293 で着地待ち）。
> 先例 = `docs/daily/2026-08-14-2.md`。
> 本セッションは `/repo-status` で始まり、hub との往復14回を挟んで `nene2-ui` の構築へ移った。

`/repo-status` を回したら、**ワークツリーに未コミットの新パッケージが59ファイル乗っていた**。
hub が同日に実装したもので、Issue も PR も無い。そこから受け取り・PR 化・欠陥の修正と進み、
気付けば**部品26本・PR 7本の積み上げ**になっていた。そして日の終わりに hub が止めた——
**26本のうち、実際の画面で使われた回数は 0**。

**今日いちばん学んだのは、自分が書いたばかりのコードこそ測る、だった。** `Card` に
`overflow-hidden` があるものとして PR 本文を書き、hub に「無い」と指摘された。30分前に自分で
書いたファイルだった。

## ヘッドライン

- 🔴 **`packages/nene2-ui/` が未コミットで乗っていた**（59ファイル・Issue も PR も無し）。hub の玉と判明 → **#294 起票**して家を与えた
- 🔴 **`npm run check` が lock の非同期を検出できない**（#296）。#295 の CI 赤で露見。`node_modules` に張られた環境では見えず、**ワークスペースを1本足すと必ず「ローカル緑・CI 赤」になる**構造だった
- 🔴 **`.tsx` の穴が3つ**（`format:check` の glob・`check` の `npm ci` 不在・`vitest` の include）。**`.tsx` を持つパッケージが無い間はどれも見えない**
- 🔴 **キットが自分の README に反していた** — 「三状態は set で出荷する」と書いて `LoadingState` を欠いていた
- 🔴 **プリミティブが disabled も focus も1つも持っていなかった** — 画面側の39件は逸脱ではなく**キットの穴埋め**だった
- 🔴 **`FormField` が `aria-describedby` を散文で委譲していた** — vault は `aria-invalid` 3件に対しリンク **0件**、payout は 26 対 **0**
- 🟡 **`hint` は同じ名前で描画位置が2通り**（invoice はラベル横・vault / profile はコントロール下）
- **積み上げ7段で hub が凍結**。W1 が1艦通るまで新しい部品を作らない

## 本日の成果（時系列）

### 1. `/repo-status` と齟齬の出口（12:1x〜12:5x）

- **#294 起票** — 未コミットの `packages/nene2-ui/`（59ファイル）に Issue 駆動の家を与えた。hub へ照会して出自を確定
- **npm バッジの二重の誤り** — 未 publish でリンク先404、かつ**兄弟3パッケージは元からバッジ0本**。hub が恒久削除
- **PR #286 をクローズ**（施主 GO）。`docs/daily/2026-08-20.md` が**両枝でバイト一致**、`current.md` は #293 が63行追加の上位版であることを実測してから畳んだ
- **枝の掃除**（施主 GO）— `git remote prune` で1本、**未マージの `docs/285-daily-and-current` も中身の保全を実測してから削除**。リモート4本・ローカル4本・`branch -r` が一致
- **Issue #285 の孤児化を検出** — #286 を閉じたことで親を失っていた。#293 の本文に `Closes #285` と包含の実測表を追記（既存54行は無改変）
- 記録は **#169** へ

### 2. `nene2-ui` の受け取りと CI の赤（12:5x〜13:1x）

- **PR #295** を開いた直後、check が **`npm ci` で赤**。`package-lock.json` にワークスペース登録が無かった
- 🔴 **ローカルの `npm run check` は hub 側でも自艦でも全7項目 PASS していた**
- lock の差分は **14行で全量を目視**（+4 link / +9 workspace / **−1 `react` の `devOptional` 削除・版は 19.2.7 のまま**）。**`npm audit fix` は使っていない**
- **#296 起票 → PR #297** — `check` の先頭に `npm ci --dry-run --ignore-scripts --offline`（**実測 1.0 秒**）。hub の「無条件で軽く回る形があるならそちらが上」に従った
- 🔴 **素の npm を直接呼ばずラッパを噛ませた** — `--offline` だと `ENOTCACHED` が**回線障害に見える**ため。言い直さないと「無視してよい赤」に化ける

### 3. W0.5 レイアウト部品（13:1x〜13:2x・**#298 / PR #299**）

- hub の生データ（`_work/reports/2026-08-23-w05-layout-primitives-rawdata.md`）から**計算して**9段スケールを決めた
- 候補ごとのスナップ時のズレを実測: 7段 92.1% ／ 8段 96.3% ／ **9段 98.2%** ／ 10段 98.3%（≤2px・計944件）
- 🔑 **勝ち筋は段数を減らすことではなく異なりを潰すこと** — 艦は944件を **153種**の異なりクラスで書いている
- 既存5トークンは **`var()` 別名**として残した（値は1文字も変えていない）⇒ **58箇所の remap が段階的にできる**
- 🔴 **`vitest.config.ts` の include に `.tsx` が無く、レンダテスト12本が1本も走っていなかった**

### 4. プリミティブの状態表現（13:3x〜13:4x・**#300 / #302 / PR #301**）

- 実測: `Button` / `Input` / `Select` とも **`disabled` 0・`focus` 0**
- ⇒ 画面の39件は**逸脱ではなくキットの穴埋め**。先に lint を打つと**押せないが押せないことが見えないボタン**が本番に出る
- `focus` ではなく **`focus-visible`**（艦が両方書いていた理由の復元）、`ring` ではなく **`outline`**
- **`Textarea` 新設**（艦に5実装）、**`aria-describedby` の委譲を廃止**して `FieldContext` で配線

### 5. W0.6 / W0.7（13:5x〜14:1x・**#304 / #306 / PR #305 / #307**）

- **`LoadingState`** — README が「set で出荷する」と書いて欠いていた
- `Modal`（native `<dialog>`・**jsdom 25.0.1 は `showModal` 未実装**なので `open` へ縮退）／`ConfirmDialog`（cancel を先に）／`Badge` / `InlineAlert`（**tone が role も決める**）
- `Checkbox`（ラベル内包）／`Radio`（**`name` 必須**）／`Switch`（`role="switch"`）／`DataTable`（`scope` と `caption` 必須）／`Pagination`（現在位置をテキストで）
- **README のインベントリを検査可能にした**（`src/readme.test.ts`）— 前 PR で「生成する」と書いたが、**書いただけでは何も保証しない**

### 6. `FormField` の拡張と数字の訂正（14:1x〜・**#308 / PR #309**）

- 配布ソースに**訂正前の数字**が入っていた。書き込む前に取り直し、**分類の合計が総数と一致するまで**詰めた
- 🔴 **hub の訂正値もずれていた**（生 grep は 16 でなく 14・内訳の刻みも違う）。**payout は「26 対 1」ではなく「26 対 0」**（唯一のヒットはコメント行）
- **`hint` の描画位置が2通り**あったので `hint` / `labelAdornment` に分割
- **3艦とも `hint` を `aria-describedby` に繋いでいない**（見えるが読まれない）⇒ 両方載せる（error が先）

## 🔴 訂正したこと

**`Card` に `overflow-hidden` があるものとして PR 本文と doc コメントを書いた。** キット全体で
`overflow` の出現は **0件**。hub の指摘で気づいた。**30分前に自分で書いたファイル**だった。

結論（`ring` ではなく `outline`）は変わらず、根拠を実測へ差し替えた——切るのはキットではなく
**艦の既存コンテナ**（records 12 / vault 2 / deal 2）。

🔑 **正しい結論は、正しい根拠の証拠にならない。** 今回は結論が同じだったので実害は無かったが、
「Card が切る」を前提に `Card` 側を触っていたら無関係な変更を入れていた。

## 📊 本日の数字（**すべて自艦で実測・2026-08-23 14:19 時点**）

|                              |                                                                                  |
| ---------------------------- | -------------------------------------------------------------------------------: |
| 起票した Issue               |          **9本**（#294 / #296 / #298 / #300 / #302 / #303 / #304 / #306 / #308） |
| 開いた PR                    |                        **7本**（#295 / #297 / #299 / #301 / #305 / #307 / #309） |
| **マージした PR**            |                                                    **0本**（マージは施主の手番） |
| クローズした PR              |                                                          1本（#286・マージせず） |
| キットの部品                 |                                                      **26本**（v0.1 の10 → +16） |
| **部品が画面で使われた回数** |                                                               **0**（W1 未着手） |
| テスト                       |                          **43 files / 627 tests**（本セッション開始時 36 / 527） |
| 陽性対照                     | **23種**（#297 の2 ／ #299 の3 ／ #301 の5 ／ #305 の4 ／ #307 の6 ／ #309 の3） |
| open PR                      |                                                  **9本**（積み上げ6段＋独立3本） |
| open issue                   |                                                            **73本**（開始時 64） |
| hub との往復                 |                                                                         **14回** |

⚠️ **「起票9・PR 6」は成果ではなく在庫**。マージが0なので、**フリートに届いたものは今日ゼロ**。

## 次に座る人へ

- 🔴 **積み上げは #309 で凍結**（hub 指示）。**W1 が1艦通るまで新しい部品を作らない**
- **#293 → #295 → #299 → #301 → #305 → #307 → #309** の順。**#295 が入れば以降は rebase して整理できる**
- #297（lock ゲート）と #287（notify-failure）は**独立にいつでも**
- `Toast` と `Icon` は**未着手**（`Icon` はアイコンセットの施主判断待ち）
- W1 リファレンス = `_work/reports/2026-08-23-nene2-ui-w1-reference.md`（206行・全部実測）

---

## 数字を書くときにやったこと（今日の型）

**表に書いた数字を、書いた直後にもう一度別の方法で数え直した。** 初稿では
「開いた PR **6本**」「陽性対照 **18種**」と書いていたが、実測すると **7本** と **23種** だった。
どちらも**記憶から書いた**もので、今日 `Card` で踏んだのと同じ形。

🔑 **日報は「その日の記録」なので、書いた瞬間に検算しないと、翌日から誰も検算しない。**
