# 日報 — 2026-07-16（nene2-fleet-tooling）

> **検査器が、自分が強制するはずの条文を破っていた日。** `check:exemplars` は「準拠判定の正は origin/main」と定める規約 02 A-10 に違反してローカル作業ツリーを読んでおり、その A-10 自身が「stale な計測で誤った準拠主張をした事故」への再発防止として制定された条文だった。是正の過程で **npm への空パッケージ publish**・**未コミット規約からの批准前提生成**・**npm に存在しない版への patch release** を publish/マージ前に止めた。**PR 6本マージ・1本レビュー待ち・Issue 7本起票・全て CI 緑。** 本日が本リポ初の日報（`docs/daily` の慣習は records のみ — 実測）。

## ヘッドライン

**`check:exemplars` が規約 A-10 に違反していた（#37 / PR #38）。しかも A-10 は、この罠を踏んだ事故への再発防止として作られた条文だった。**

実装は `--root` 配下の**作業ツリー**を `readFileSync` していた。fleet 各リポが作業ブランチ・origin/main から最大4 commit 遅れのまま測られ、**実態 4/8 を 1/8 と誤報**した。規約 02:169 A-10（MUST `[P]`・会議R3④決定）はこう定める:

> 準拠状態の主張・監査・実測は origin/main ＋ CI の check 出力を正とし、記載には commit SHA を併記する。ローカルワークツリー grep は「その枝の事実」＝参考値
> 【**根拠事故: 現状分析 §7「transport import 0/15」が stale 計測と確定（R3 記録の訂正1）**】

**＝ 条文が守らせたい当のものを、条文の執行器が破っていた。** この罠は本日判明分で4回踏まれている（現状分析の筆者 → 前任統合リナ → 統合リナ → **自分**）。**自分も踏んだ側**であり、統合リナに「1/8 だ、board が誤りだ」と報告して撤回している。

是正は「origin/main を `git show` 経由で読む・fetch 不能は unknown・**リポごとの commit SHA を出力に載せる**（A-10 の SHA 併記 MUST は従来まったく未実装だった）」。回帰テストは **bare origin にアンカーを植え、ローカル作業ブランチで消した fixture を git で組み**、`gitRefSource` = green / `worktreeSource` = red を**対にして固定**した。罠そのものをテストにした。

---

## 本日の成果（時系列）

### A. #37 / PR #38 — check:exemplars を A-10 準拠へ（**マージ済** 16:00:52）

- 既定の読み取り源を **origin/main** に。作業ツリーに一切触れない → ローカルがどのブランチ・どれだけ dirty でも結果が動かない
- **リポごとの commit SHA を report に載せた**（A-10 の SHA 併記 MUST — 未実装だった）
- 検査不能を `repo-unavailable` として failures と**別腕**に。検査できていないことを red と言うと「アンカーが無い」という**嘘の主張**になるため
- レビュー指摘（統合リナ）で**設計ミスが1件**: `--no-fetch` で `authoritative: true` を返していた。`authoritative: false` を出力に出すだけでは**区別が読み手任せ**で、exit code は green→0 なので参考値モードを CI で回せば**塞いだはずの穴と同じ型**が残る。**G-6「green は『検査が走り正の証拠を得た』場合のみ」**を根拠に、**参考値の源では green を出さず unknown** という一本の規則にまとめ、`--worktree` と `--no-fetch` を同じ扱いにした（機械強制になる）
- **典拠の誤りを是正**: 「検査不能は unknown」は CF-1 ではなく **G-6**（05:22）。統合リナの発言を**原文に当たらず転写**していた。CF-1（05:1027）は適合ベクトル出力の条文で別物

### B. #40 / PR #41 — 批准前提の red リストを A-10 準拠で再生成（**マージ済** 17:06:50）

- 旧版は**批准前提の正本でありながら、批准前提の条文に違反した方法で作られていた**（stale 計測・SHA 併記なし・記録した規約6文書 sha256 が現物と全件ずれ）
- **A-10 の SHA 併記 MUST を3層に適用**: (1) 規約文書 sha256 (2) **入力文書の出所コミット**（xi-workspace）(3) 対象リポ commit
- (2) を足した理由 🔴 — **生成の実行中に規約 `04-i18n.md` が未コミットのまま18行書き換わっているのを検出して中断した**。未コミットの文書の sha256 は**どのコミットにも存在しない版**を指し、誰も再現できない。実際、同じ検査を同日2回回して parity-test 参照が `:408` → `:426` に動いた。**「#38 で『作業ツリーを読むな』を検査器に入れた当日に、その検査器の入力で同じことをする」わけにはいかない**
- 前版との数値差は**「差の出所」つきの表**に。特に **resolved 0 → 4 は「4本植えた」のではなく「元から植わっていたものが見えていなかった」**と明記。**是正されたのは検査器であって fleet の実態ではない**
- 旧版は削除せず **supersede バナー**で保存（事故の記録）

### C. #42 / PR #43 — @import の layer() 二重指定（**マージ済** 17:06:54・severity=error）

- 規約が `[S:no-double-layer-import]` を **3箇所**（03:192・468・747）でタグ参照しているのに **config にルールが無い**状態だった（`check:standards-doc` が不存在 rule-id として検出）
- **error で入れられた**: origin/main 全リポ実測で実プロダクトの違反は **vault の1行のみ**。vault リナと「**vault 先 → lint 後**」で合意し、vault#214 が先に着地 → **warning 先行が不要**（KU-1「後で error に上げ忘れる＝恒久偽 green」を自作せずに済んだ）
- vault リナの質問から **url() の穴**を発見: `@import url("./x.css") layer(components)` が素通り。判別を「url() か」→「パスが相対か」に寄せた。**ただし素朴にパスだけだと `url('../../node_modules/katex/...')` ＝相対だが第三者 CSS の正当な vendor を落とす**（ブランチの既存テストが検出）→ node_modules 除外

### D. #44 / PR #45・#46 — nene2-i18n を publish 可能に（**両方マージ済** 17:06:58 / 17:40:21）

🔴 **`private: true` を外すだけで publish すると、空パッケージが npm に恒久的に残るところだった。**

|        | `npm pack`（素の checkout から実測）                              |
| ------ | ----------------------------------------------------------------- |
| 修正前 | **2 files / 1.6 kB** — README と package.json のみ・**dist 無し** |
| 修正後 | **11 files / 5.8 kB** — dist 9ファイル入り                        |

- 原因は `prepack` の欠落。他2パッケージは #13（`92040a4`）で得ているのに、**i18n だけ `private: true` で publish 経路に乗らず、この是正から漏れていた**。#13 のコミットメッセージが同じ事故を記録している（「hide の初回 publish 実測で発覚・6 files/8.7kB」）。**`npm publish` は取り消せない**
- 欠けていたのは prepack だけではない: **`publishConfig.access: public`**（無いと scoped は restricted 扱い＝「public にする」が成立しない）・`repository`・**publish workflow の選択肢に i18n が無い**（「対象外」とコメント付き）。**「private を外す」の1行では、どの経路でも public にできない状態**だった
- **AM-12 の「結合の実体」を実測**（統合リナが未検証と申告した箇所）: **`fleet-baseline.json` は nene2-standards の配布物に同梱されていない**（`files: ["dist","registries"]`・npm pack 85 files に不在。**リポのルート＝package ディレクトリの外なので npm は構造上入れられない**）→ **bump に再 publish は不要**。ただし規約 02 A-3 は「**nene2-standards 配布の** fleet-baseline.json」と書いており**食い違う**（統合リナへ引き渡し）
- 版は **0.1.0**（施主裁定）。`fleet-baseline.json` の座席 `null` → `^0.1.0`

### E. #47 / PR #48 — red リストの生成器の批准前提記号（**マージ済** 17:40:25）

- `RAT-3(a)` は `check:standards-doc` の前提、`check:exemplars` は **`RAT-3(b)`**（minutes:809 全文・README:116 で裏取り。発見は Payout リナ・統合リナ）
- 統合リナの推し（「RAT-3(b) に直す」）は**採らなかった** — それだと**2節の軸が違うまま**残る（standards-doc = 出所 RAT-1/RAT-2 ／ exemplars = 前提）。**彼女自身が指摘していた構造**。両節に「出所」と「満たす批准前提」を**両方**書く形へ
- **生成物も同時に再生成**。「.md を手で直しても次の再生成で戻る」の逆も真で、**手で .md だけ直すとどのコマンドでも再現できない hybrid** になり #40 で塞いだ問題が再来する

### F. #49 / PR #50 — 拡張トークン検査（**レビュー待ち・未マージ**）

- **道具が自分の生成物を自分の検査器で拒否する**: 生成側 `--font-weight-x-medium`（#35 で是正）→ 検査側 `false`
- 🔴 **統合リナの「publish 済み 1.0.2 のバグ」は誤り**（実測: `npm view … versions → [1.0.0, 1.0.1]`・`npm pack …@1.0.2 → notarget`）。npm から実物を取って測った **1.0.1 は自己矛盾していない**（両側とも旧形で一貫）。**npm に壊れた publish は無く patch release は不要**。急ぐ理由は「npm が壊れている」ではなく「**壊れたまま publish しかけている**」
- 原因は `contract.ts` が namespace 表を見ない**「3枚目」**。`codemod.ts:175-177` が「x- 送りと class 翻訳が**同じ1枚を見る**ことが不変条件」と明記しているのに、契約検査だけが独自 regex を持っていた ＝ **#35 が塞いだ当の穴の3箇所目**
- **⚠️ この PR は不十分**（後述）

### G. #39 — vendor CSS の行き先が機械強制されていない（**open**）

- vault リナが「実測はしていない＝憶測」と断って挙げた懸念が、調べたら**条文と一致**していた（03:254(b)・199 が vendor を `@import url(…) layer(vendor)` 一択に縛るのに、強制するルールが無い）
- 当初「実測0件＝仮想の穴」で起票 → 統合リナが**別の角度**（url() の絶対 URL・layer 無し）で **nene-serve `serve.css:7` の実例**を発見。**単一の角度で0件を確認して「穴は無い」と結論するのが誤りだった**、という記録として残した

---

## 📊 本日の数字（すべて自分で実測。渡された数字は明示）

|                                |                                                                                                          |
| ------------------------------ | -------------------------------------------------------------------------------------------------------- |
| PR **マージ**                  | **6**（#38 / #41 / #43 / #45 / #46 / #48）                                                               |
| PR **レビュー待ち**            | **1**（#50 — マージ可・ただし W1 は撃てない）                                                            |
| Issue 起票                     | **7**（#37 #39 #40 #42 #44 #47 #49 — うち **closed 5** / **open 2**）                                    |
| main                           | `d870c1f` ・ **test 328 passed (22 files)** ・CI success                                                 |
| 批准前提 `check:exemplars`     | **red** — [X] 8件中 resolved **4**・検査不能 0（源 origin/main・authoritative）                          |
| 批准前提 `check:standards-doc` | **red** — タグ欠落 MUST **215** / 不存在 rule-id **15**（#43 で 18→15）/ [P] 列挙外 **0**                |
| nene2-i18n の tarball          | **11 files / 5.8 kB / unpacked 16.1 kB**（merged main から・dist 同梱・src 混入なし・秘密なし）          |
| フリートの stylelint 配線      | **0 リポ**（css を持つ実プロダクト20本・css 227ファイル。痕跡は config を書いている fleet-tooling のみ） |
| 二重 layer 指定                | 実プロダクト **0件**（vault#214 着地後・fixture 1件は #43 で同梱修正）                                   |

> **渡された数字**（自分で測っていない）: 統合リナの誤り回数（30）・止めた回数（fleet-tooling 12 / payout 11 / vault 6）・フリート全体のマージ 11本。**vault#214 の `npm run check` 結果と byte diff も vault リナ報告であり未検証。**

---

## 🔴 自分の誤りと、どう捕まえたか

**1. A-10 の罠を自分も踏んだ。** `--root` でローカル作業ツリーを測り「resolved 1/8、board.txt:53 の 4/8 は誤り」と統合リナに報告した。**board が正しかった。** 指摘を受けて全項目を測り直し撤回。**自発ではない。**

**2. 伝聞を原文に当たらず転写した（CF-1 → G-6）。** 統合リナが「CF-1」と呼ぶのをそのままコード・Issue・PR へ書いた。正しくは G-6。**「もう一度確かめてください」と言われるまで原文に当たらなかった。**

**3〜5. 測定コマンド自体が嘘をついた（3回）。**

|     | 何をした                                                                                                                                                 | どう捕まえた                                      |
| --- | -------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------- |
| ①   | `node … \| tail -4` の後に `$?` を読み、**tail の終了コード**を検査結果として「exit=0」と表示。そのまま進めば「standards-doc green」と報告していた       | パイプの構造に気づいて測り直し                    |
| ②   | 変異検査で python の置換が `bad escape \s` で**失敗**していたのに vitest を回し「36 passed」＝**旧実装に戻っていないまま「旧実装でも通る」と読める出力** | 置換の成否を assert して再実行                    |
| ③   | 対照実験で `npx tsc --build` が**ルートに tsconfig.json が無くて失敗**。dist は是正版のままで、旧実装で回したつもりが是正版で回り**両方 0 errors**       | `dist` の `new RegExp` の**件数を数えて**気づいた |

**3件とも「効いていない変更を、効いた前提で読む」型。** 対処は「**変異を入れたことを assert してから測る**」に統一した。

**6. 🔴 人の診断を実測だと思い込み、施主に誤った判断材料を渡した（本日最大の落ち度）。**

統合リナの診断「`--font-size-body` の silent は **fallback 後の先頭セグメントが表にあるかどうか**の差」を**確かめずに**選択肢へ組み込み、施主裁定を仰いだ。施主は (i)「表に無い namespace は reject」を選択。**実装して実測したら成立しなかった**:

- **塞ぎたかった silent が塞げない** — `--font-size-body` は **fallback していない**。`tailwindNamespaceOf` の表ループで `--font-` に**直接ヒット**して `font` を返す（`font` は表にある）。(i) を入れても `--font-x-size-body ✅受理` は**変化なし**
- **origin を壊す** — `--z-dropdown` / `--z-modal` は `z` が表に無いので reject され**テスト2件が落ちる**。`codemod-map.ts:48-49` が「未知 namespace（`--z-*` 等）は先頭セグメントを返す＝**従来挙動を保存する**」と明記＝**fallback は意図的な機構**
- **根本** — `--font-sans`（正当な v4 font-family トークン）と `--font-size-body`（発明された非 v4 名）は**どちらも `--font-<key>`**。**名前の形からは判別できない**

実装を戻し、統合リナへ再送。**#50 に壊れた実装は push していない**（作業ツリー clean・push 済みと一致）。今日ずっと「伝聞を原文で確かめる」をやりながら、**選択肢の前提そのものを確かめなかった**。

---

## 統合リナを止めた件（止めなければ何が起きていたか）

| 止めたもの                                            | 起きていたこと                                                                                                                                                                                                              |
| ----------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **未コミット規約からの生成**                          | `sha256=3aaa…` という**どのコミットにも存在しない版**を批准前提の正本として記録。誰も再現できない                                                                                                                           |
| **空パッケージの publish**                            | 指示書どおり「private を外して publish」→ **2 files/1.6 kB の空パッケージが npm に恒久的に残る**。取り消せない                                                                                                              |
| **npm に無い版への patch release**                    | 「publish 済み 1.0.2 のバグ」を前提に 1.0.3 を立て、**存在しない前提の上に PR を積む**                                                                                                                                      |
| **(c) を引用で埋める**                                | 「oracle 分は R5 の6系統実測が先行実施済み」＝ **6ペルソナの CI コスト・ベンチマーク**を6リポ素振りとして引用 → **実行していない検査を green と主張**（G-6 違反）。実測は oracle 2/6・validate:themes 0/6 で **(c) は未達** |
| **CF-1 / EXCLUDED_NAMESPACES / transition kind の器** | 誤った条文番号・誤った器（`EXCLUDED_NAMESPACES` は「テーマ差し替え対象外」であって「未契約」ではない）の上に設計                                                                                                            |

**ただし逆も同じだけある。** 彼女の hermetic 指摘（fetch 内蔵が AM-12 と衝突しないか）で 05 §5.2 を確認し「#18 は fleet-tooling CI・#17 ratchet が既に同じ型」と裏を取れた。**1/8 を撤回できたのも彼女の指摘。** 最後の1件（(i) の前提）は**彼女の診断を確かめなかった自分の落ち度**であって、彼女の誤りではない。

---

## ★ 本日フリートで10回踏まれた型 — この視点から

> **手元にあるものを、名指ししたものだと思い込む**

自分の踏んだ分は **`--root` を origin/main**（#1）・**失敗した tsc の dist を旧実装**（#5）・**人の診断を実測**（#6）の3件。

**この型に対する規約の答えは既にあった。** 02 A-10 は現状分析 §7 の stale 計測事故への再発防止として制定されている。**それでも今日10回踏まれた。** 条文があることと、条文が守られることは別で、**機械強制がなければ条文は読み手の善意に依存する**——それが #38 で「参考値では green を出さない」を**機械強制**にした理由であり、#48 で「.md を手で直さず生成器を直す」にした理由でもある。

**一方で、条文の執行器自身がこの型を踏んでいた**（#37）。**執行器を誰も検査していなかった。**

---

## 明日への引き継ぎ

### 待機中の球

- **PR #50** — レビュー待ち。**マージ可・ただし W1 は撃てない**（検査側の半分のみ。誤りではなく不十分）
- **#49 の生成側は振り出し** 🔴 — 施主裁定 (i) は**実測で成立しないと判明**（上記「自分の誤り 6」）。**明日の指示書が (i) 前提で書かれると同じ壁に当たる。** 統合リナへ再送済みだが、**行き違いの可能性がある**
- **nene2-i18n 0.1.0 の初回 publish** — 施主のローカル操作待ち（Trusted Publisher は既存パッケージにしか設定できないため初回のみローカル・`--provenance=false`）。判断材料は #46 に全数目視つき
- **批准前提 (c) 節の設計** — 施主判断待ち。**器だけ先に作ると「全部 unknown と表示する節」ができる**（G-6 に忠実で嘘はつかないが批准を進める仕事はゼロ）。素振りの実行が先

### 明日の出発点（**未実測＝憶測**・確かめてから施主に出すこと）

- fallback は残す（`--z-*` の従来挙動保存は意図的機構）
- silent の対象は「**v4 に実在しない typography 名が v4 namespace に prefix 一致する**」ケースに限定（`--font-size-*` / `--line-height-*`）
- 器は**個別写像表**（`ORIGIN_TABLE` 等は pre-contract 語彙をキーに持つ既存の器）で明示 reject が board:59「typography は第二波・施主裁定07-15=C 現状維持・due 08-21」と整合するかもしれない

### 統合リナへ引き渡し済み（本リポでは扱えない／横断レーン）

- **規約 02 A-3 の食い違い** — 「nene2-standards 配布の fleet-baseline.json」が**構造上配布できない**・`check:fleet-baseline` も未実装（`unknown(not-installed)`）＝**条文が実在しない配布物を正本と呼び、検査機構も無い**
- **#39** — vendor の行き先（実例 `nene-serve serve.css:7`）
- **フリートの stylelint 配線 0 リポ** — `[S:…]` タグは現在1つも実行されていない。配線は W0b。**ただし conformance は `unknown(not-installed)` で出しており空虚合格は作っていない**
- **vault の PHP `@conformance`** — 「15件を baseline/ignore で抑止したまま green」。**管轄外**（composer `hideyukimori/nene2`）だが、フロント規約 CF-2 なら `frozen(15)` と出るべき形。judge せずルーティングのみ

### 注意

- **`npm run check` はローカルの `.claude/worktrees/`（git 管理外）を lint して落ちる。** CI 対象外。`--ignore-pattern '.claude/**'` で exit 0 を確認済み
- **`npx tsc --build` はルートに tsconfig.json が無く失敗する。** 正しくは `npm run type-check`（= `tsc --build packages/*`）
