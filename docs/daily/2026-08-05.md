# 2026-08-05 — フェーズ2 発令と、配る前に測った一日

> **対象期間**: 08-04 深夜（hub の1回目の店じまい確定の直後）〜 08-05（最終の店じまい）。
> ⚠️ **同一セッションの2本目**です。セッションは 08-04 夜に始まり日付をまたぎました。途中で hub が
> 店じまいを宣言したので `2026-08-04.md`（PR #228・未マージ）を書きましたが、その後も依頼が続きました。
> 規約「1セッション = 1本」への例外なので、両方の冒頭に期間を明記して切れ目を示します（#241・粒度は hub 裁定待ち）。

3週間ふさがっていたフロント・セキュリティ堅牢化フェーズ2が、施主5点裁定で発進した。fleet の担当は P2-1（L1 共有 lint）・P2-2（L2 実走検証）・P2-3（L3 夜間ハーネス）の3本。同時に npm audit の新規 advisory 2件が全艦の CI を赤にしうる状態で来たので、そちらを先に艦隊へ配った。この日いちばん効いたのは、どの仕事でも**配る前に測ったこと**だった。work order も board も「残り3艦」としていた横断調査は実測すると1艦で、L2 の「統一UTが走っているか」は走っているか以前に配布されておらず、自分で取った基準線は時点が混在していて破棄した。hub の誤りを7件止め、自分の誤りを4件訂正した。どれも相手を疑ったからではなく、その数字を自分の設計の前提にするから読んだ結果だった。**今日この repo でマージされた PR は無い。**

## ヘッドライン

- **フェーズ2 発令**（施主5点裁定）。fleet 担当3本のうち **P2-1 / P2-2 の設計が hub 検収 PASS**、P2-3 は受け皿を作成。
- 🔴 **P2-2 は「走っているか」以前だった** — L2 の統一UTは `files` の外にあり、**艦の node_modules に test 0件**。完了条件が充足不能だったので正本を書き換えてもらった（#232）。
- 🔴 **配布前の実測で対象が減り続けた** — #229 の横断調査は3艦→**1艦**、audit 配布は suite が完了済みで対象外、L1 lint は全14艦とも0の更地。
- 🔴 **自分で取った基準線を破棄した** — 計測時点が艦ごとに混在（corpus が作業ブランチ＋未コミット21件）。**ref 固定のツールを実装して取り直したら、3つの数字すべてが動いた**（#237）。
- **#169 の出口を実行** — stale ブランチ15本を削除し、ローカル / remote-tracking / origin の**3者一致**を検証。

## 本日の成果（時系列）

### 1. npm audit 横断（#229）— 配布対象が実測で減り続けた

新規 advisory 2件（`GHSA-rgw5-rvv9-x895` brace-expansion / `GHSA-7p8r-x3mc-p8w7` fast-uri）で全艦の frontend CI が赤の公算。deal が検出し、fleet が (B) 横断を担当した。

**配布前に測った結果**（fleet 実測・各艦の package.json / package-lock.json を直読）:

- **brace-expansion を override で名指しする艦 = 13艦**。ただし **field と contact は完全固定**（`1.1.16` / `2.1.3`）で **lockfile 更新では上がらない**。他11艦は caret なので lockfile 更新で足りる → **artifact を2種類に分けた**
- **fast-uri を持たない艦が5隻**（clear / concierge / suite / profile / contact）→ この5隻に fast-uri の指示を出すと空振りなので書き分けた
- **vault / records は fast-uri が既に 3.1.5** → **bump 指示を出さなかった**
- **suite は完了済み**（PR #417 の内容が反映済み）→ 配布対象から外した

**配布**: vault#353 / clear#407 / payout#277 / concierge#211 / origin#563 / profile#128 / **field#148**（編集1箇所）/ **contact#573**（編集2箇所・艦隊で唯一）/ records#1050 / deal#201（allowlist の置換依頼）＝ **10艦**。

途中で clear が「**mh99 は死んだ例外だった**」（allowlist エントリがどの advisory にもマッチせず沈黙していた）を実測したので、**配布9艦すべてに生死確認の1項を追加**した。追加前に「13艦すべてが mh99 の記載を持つ」ことを測り、**空振り配布がゼロ**であることを確認してから入れた。

### 2. フェーズ2の設計3本

**P2-2（#232・L2 実走検証）**: 完了条件は「adopt 12艦で統一UTが CI 実行されていること」だったが、**統一UTは配布されていなかった**。`@hideyukimori/nene2-client` の `files` は `["dist","README.md","LICENSE"]` で、tests はその外。**vault / payout の node_modules を直読して test 0件**（陽性対照として `dist/index.js` は存在）。保証されているのは「パッケージ自身の健全性」であって「各艦の統合が契約どおりか」ではない。

設計は `./testing` subpath で契約テストを配る案（hub 採用）。**44ケース全部は配らない** — 大半はパッケージの内部実装の検査で、艦が何をしようが壊れないため。判定基準を「**艦が供給する値によって壊れうるか**」に置き、**5群12項目**へ絞った。

**P2-1（#234・L1 共有 lint）**: 実測で**全14艦＋共有 config とも 0**（完全な更地）。ただし3種のうち **sourcemap・秘密検査だけ ESLint ではなくビルド成果物の検査**なので、**本番 build が走らない艦では原理的に実行できない**。完了条件の段階分けを提案し採用された。

**P2-3（#236）**: 設計本体は後だが、確定制約（vault #352 完了が前提・check 集約形の推奨）が出たので**受け皿だけ先に作った**。置き場が無い要件は消えるため。

### 3. #237 — 基準線を取り、破棄し、取り直した

hub 発注の「逃げ道」棚卸し。手作業で基準線を取ったが、**hub が「実測にも いつの実測か がある」を advice に収載した**のを受けて自分の計測を遡って確認したところ、**corpus が作業ブランチ＋未コミット21件・field が対応中ブランチ**だった。**全体の84%を占める数字がその状態の値**だったので、hub 裁定で**破棄**。

`_work/tools/escape-hatch-census/` に **ref 固定のツールを実装**（`git show <ref>:<path>` のみ・既定ブランチを自動解決・時刻とSHAを刻む・陽性対照つき）。取り直した結果、**3つの数字すべてが動いた**。

### 4. #169 の出口 — stale ブランチの削除

施主承認を受けて実行。**archive タグ退避 → PR state 確認 → SHA 一致検証**の順で、**15本削除・20本 prune**。**実行直前に対象集合を取り直した**ところ、セッション中に2本増えていた（今回は対象外）。

### 5. 条文の起草・還流

- **#231** remedy の名指しは期限を持つ（serve 起草・実例3件で劣化の3形態を整理）
- **#239** テスト環境と本番環境の差異は、差異そのものを検査対象にする（**board due 08-12**・草案完成）
- **#233** 成果物境界の語彙（実測3艦・条文化は EX-6 の後という順序条件つき）
- **#230** 決定木に shared 層の hook を受ける行が無い
- **#240** フリート規約のローカル写しが中央に追随しない（**実測11艦**が写しを保有）

## 🔴 ミスと再発条件

**成果と同じ密度で書く**（concierge の「ミスの記録は成果の記録より価値が長持ちする」に倣う）。

| # | ミス | 再発条件 | 止まった理由 |
| --- | --- | --- | --- |
| 1 | **#76 を「未実装の証拠」に使った** — 実際は PR #131 で15日前に実装・出荷・消費済み（issue が閉じ忘れで open） | **open な issue を「まだ無い」の根拠に使う**とき | 棚卸しの1本目で自分の引用が崩れた |
| 2 | **設計を実装で確かめずに書いた** — `runTransportContract({client})` は transport が生成時に fetch を束縛する（`transport.ts:205-217`）ので成立しない | **API 形を推測で決める**とき | 実装レーンの**負テスト**が2回直した |
| 3 | **#65 の要約（board 行の一文）だけで矛盾と判断** — 実際は「スキーマが per-repo」の話で矛盾していなかった | **要約を根拠として使う**とき | 実読 |
| 4 | **基準線の計測時点を記録しなかった** — 13艦中2艦が別ブランチ。**測ったこと自体は正しいが、何を測ったかを記録していなかった** | **「実測した」と書けてしまう**とき（前3件より始末が悪い） | hub の同型事故の報告を自分に当てた |
| 5 | **自作ツールのコメント除去が URL を壊した** — `re.sub(r"//[^\n]*", ...)` が `"https://…"` を食う | **正規表現で構文を扱う**とき | ツールが 0 でなく **UNMEASURABLE** を出した（設計が救った） |
| 6 | **その修正として入れた末尾カンマ除去が、文字列内の `,]` を壊した** | **直した勢いで同じ種類のバグを入れる** | **先に書いた陰性対照**が数秒で検出 |

**5と6の一般形**: 素朴な正規表現が構文を無視して文字列の中身を壊す。**#226 で自分が書いた「道具の単位を間違えると正しい側まで壊す」の再演**だった。

## 🔴 踏みとどまった判断（実行しなかったこと）

- **#76 を閉じなかった** — 3段のうち段1・段2は完了していたが**段3が未検証**。閉じると「closed だが実は未対応」を自分で作る
- **タグを push しなかった** — 全件 MERGED を確認済みで内容は main にあるため。ただし**手順の緩和ではなく条件付きの例外**だと #169 に明記した
- **「増えた / 減った」を書かなかった** — #237 は1点計測なので差分が無い
- **推定のまま条文化しない、と自分の提案に釘を刺した** — #240 の「艦固有の補足がある」は自艦名の出現回数からの推定で、**中身を読んでいない**
- **backend 条文をこのリポに置かなかった** — ここはフロント統一規約の正本。#233 で自分が言った「混ぜない」の自己適用

## 他艦へ効く一般形

| 一般形 | 出どころ |
| --- | --- |
| **remedy の名指しは期限を持つ**（生存確認 → 再読の2段） | serve / deal / clear の3形態 |
| **テスト環境と本番環境の差異は、差異そのものを検査対象にする** | #235（dev サーバ）＋ clear #419（SQLite） |
| **逃げ道のほうが安いゲート**（ゲートは動いているが、逃げ道が正しい対処より安い） | concierge #219 |
| **不在そのものではなく、不在が把握されていないことが問題** | #238（waiver との対比） |
| **ローカル写しは、組織的に用意された要約** | field #153 |
| **手順を書くより、手順が要らない形にするほうが強い**（check 集約なら required 依頼が消える） | vault #352 の直し方 |
| **負テストは設計の検査器** | nene2-js PR #126 |

## 📊 本日の数字

**fleet が自分で実測したもの**（測定日 2026-08-05・ref は各リポの既定ブランチ）

| 項目 | 実測値 |
| --- | --- |
| `npm run check` | **exit 0**（35 files / 517 tests・AM-2 PASS） |
| 起票 | **fleet-tooling 8本**（#232〜#241 のうち）＋ **他艦 10本** |
| PR | **0本**（本日この repo で**マージされた PR も 0本**） |
| 逃げ道の基準線 | allowlist **23** / conformance **51** / knip **26** / suppressions **3,166** / **15艦**（waiver **0**） |
| 本番 build が check にも CI にも無い艦 | **1隻**（vault・3艦実測） |
| L1 lint 3種を持つ艦 | **0 / 14艦**（共有 config も 0） |
| 統一UT が届いている艦 | **0**（`files` の外・node_modules に test 0件） |
| フリート規約のローカル写しを持つ艦 | **11艦** |
| stale ブランチ削除 | **15本**（＋ remote-tracking **20本** prune・3者一致を検証） |

**他艦・hub から渡された数字**（fleet では再実測していない）

| 出所 | 数字 |
| --- | --- |
| clear | mh99 は死んだ例外・react-router の first patched が 8.2.1→8.3.0 へ移動・PDO bool は SQLite だけ通る |
| serve | 5.0.8 が11日で脆弱側に入った・#175 は日程未定 |
| records | A1 codemod の穴3件（うち出ていく参照は8ファイル監査で繰り上げ1件・同階層のままが正 3件） |
| nene2-js | 契約12項目のうち **seam 依存5件 / 不要8件** |

## 申し送り

明朝の入口は **#239 の清書 → doc PR**（board due 08-12・hub が「一晩置け」）。次いで #232 / #234 の実装設計、#231 / #225 の doc PR、#229 の結果回収、#237 の2回目（#229 の波が着地してから）。

🔴 **vault #352 がフェーズ2の単一最大のクリティカルパス**で、L1 / L2 / 成果物境界 / E2E / L3 パイロットの**5レーンをブロック**している。vault は #353（audit）→ #352 の順で走る。

**施主手番**: マージ4本（#213 / #219 / #223 / #228・すべて CI SUCCESS。**#213 が tokens 1.3.0 の publish 準備を塞いでいる**）／ **nene2-standards 2.3.0 の publish 判断**（#221）。

出口の決まっていない齟齬は無い。
