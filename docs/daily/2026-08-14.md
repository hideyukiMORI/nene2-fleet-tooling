# 2026-08-14 — 受け取る側も外した。捕まえたのは、撃った後に自分の生成物を検査したから

> **対象期間**: 08-13 23:5x（起動・施主指示「hub に指揮を任せている」）〜 08-14 早朝（hub へ完了報告）。
> 前セッションは **08-09 夜**（`2026-08-09.md`）＝**4日空いている**。理由は 08-13 の再ペーシング（施主GO）で
> 自艦の玉の大半が来週へ送られたためで、hub が「あなたの怠慢ではない」と明示している。
> 08-14 に先行セッションは無いので連番なし。

起動して最初にやったのは、自艦宛ての未受領指示書を見ることだった。最新は 08-09 19:50 のもので、それ以降は無い。だから `docs/todo/current.md` の「次は玉1の正本スニペット起草」に従うつもりで、その前提を測った。**測ったら、玉1 は3日前に終わっていた。** hub が 08-12 に艦隊13艦へ配り終えており、`current.md` はその3日間を知らないまま「次の一手」を指していた。この夜に起きたことの大半は、この形の繰り返しである——**渡された射程を測り直すと、毎回1つずれている。** hub は今夜4回それを踏み、あたしが2回止めた。

**そして、あたしも3回外した。** うち1件は**「6件→0件」という嘘の成功**で、`npm audit` は正常終了して 0 を返すので**エラーにすらならなかった**。捕まえられたのは注意していたからではなく、**GO をもらった後に自分の生成物（lockfile）を前後比較したから**である。**報告の時点では気づいていない。** 配る側と受け取る側の両方が外して事故が 0 だったのは、**誰も間違えなかったからではなく、間違いが下流で検査されたから**にすぎない。

## ヘッドライン

- 🔴 **hub は2本立てになっていた**（COLD 営業専任 / NeNe ファミリー担当）。リポ系の照会は SendMessage で後者へ。**relay（:8790）は COLD 側に着く**ため、`ok` が返っても NeNe 担当には届かない〔hub 申告・あたしは relay で1通目を投げて実際に COLD 側へ着いた〕。
- 🔴 **`current.md` が3日 stale だった。玉1（定期再検査の配布）は 08-12 に配布完了済み**〔fleet 実測: `gh api repos/<slug>/contents/.github/workflows/<f>?ref=main` で clear / records / nene2-python / suite / vault の5艦とも `schedule:` ＋ `workflow_dispatch:` ＋ `concurrency` に `event_name` を確認〕。**fleet が正本スニペットを起草して各艦が適用する形は実行不要**になった。
- 🔴 **【PHP-AUDIT】の射程を 14 → 8 に是正させた**。hub が足した5艦（concierge / field / profile / serve / records-commercial）は **`composer.lock` を `.gitignore` していて `origin/main` に存在しない**〔fleet 実測: contents API が 0 バイト・`ls-files --error-unmatch` が全艦 0件〕。**lock が無ければ `composer audit` は原理的に足せない。**
- **玉3 の前提は hub が正しく、7艦の例外の記述の方が古かった**。`GHSA-qwww-vcr4-c8h2` は **2026-08-07 に脆弱範囲が 7.x / 8.x へ分割され、7.x 系の first patched = 7.18.2 が生まれている**〔fleet 実測・GitHub Advisory API〕。08-04 に書かれた「7.x に修正版は無い」は**当時は正しかった**。
- **PR 10本を作成（マージ 0本）**。【BRACE】2本・【PHP-AUDIT】8本。**9本が CI 緑**。🔴 **ただし【BRACE】の2本は自分で検査して撤回・訂正した**（下記9）。
- 🔴 **`nene-origin` の CI が課金で全面停止している**〔fleet 実測: check-run annotation〕。**最後の成功は 08-10 の週次実行**で、次の週次（08-17）は失敗する。`ci-watch` は **🟢 と誤判定**していた。
- 🔴 **あたしが3回外した**（private への過剰一般化 / corpus-site の**偽の 0件** / 撃った PR 2本への意図しない版変更の混入）。**1本を draft へ差し戻し、2本の本文を全数開示に書き換えた**（下記9）。
- **nanoid はフリート全域で floor 充足＝閉じている**〔fleet 実測〕。

## 本日の成果（時系列）

### 1. 起動 — 指示書は無く、board は動いていた

`ls -t _work/handoff-*fleet*.md` → 最新は 08-09 19:50。それ以降 fleet 宛ての発行なし。
一方 board は **08-13 に再ペーシング（施主GO）**が入っており、L3 レーンの個別 due が撤去されていた。
5玉の due（08-14 / 08-15）がこれに掛かるかが読めなかったので、**判断を発明せず照会**した。

回答（COLD 専任 hub）: **`+SEC`（保安レーン）は期日維持・L3 の due 撤去の対象外**。ただし**期日は全部動いていた**。

| 玉 | 08-09 order の due | **08-13 再ペーシング後** |
| --- | --- | --- |
| 玉1 定期再検査の配布 | 08-14 | **08-18** |
| 玉2 audit 無し艦の棚卸し | 08-15 | **08-19** |
| 玉3 react-router | 08-14 | **08-15** |
| 玉4 stylelint registries | 08-15 | 🔴 **due 撤去・`+L3`** |
| 玉5 composer.lock 追跡 | 08-14 | **08-16** |

### 2. 🔴 玉1 は3日前に終わっていた

`current.md` の「次は正本スニペット起草」に従う前に、配布状況を測った。

- board 行に 🏁【08-12 配布完了】第1波6艦（clear/deal/field/payout/records/vault）＋第2波7艦（concierge/contact/invoice/profile/serve/suite/nene2-node）。
- **ローカルの写しではなく GitHub の一次情報**で5艦を抜き取り確認〔fleet 実測〕。cron は艦ごとに分がずれていた（05/25/30/15/35…）。
- `_work/ci-watch.md`（08-13 21:50）も 🟡15 / ⚪0 で一致。

**指示書は発行時点で凍結される層**なので、08-09 の work order を読んでいるだけでは永久に気づけない。
repo-status skill の §A-2 が「handoff と reports は別レイヤー」と書いているのと同じ形だった。

これを hub へ返し、**正本を `docs/standards-patch/` へ収載し直す（(b)）＋ 第3波9艦の手順書（(c)）が GO**（実施は 08-16〜20 の波）。

### 3. 玉3 — 追試したら、hub が正しく、艦の例外が古かった

clear の `frontend/audit-ci.jsonc` に「**7.x 系に修正版は無い・fix は v8 >= 8.3.0 で breaking**」と書かれており、玉3 の前提（7.18.2 へ上げれば閉じる）と正面から矛盾していた。**どちらが古いかを一次情報で判定**した。

```
gh api /advisories/GHSA-qwww-vcr4-c8h2
  published 2026-07-24 / updated 2026-08-07T18:16:54Z
  { react-router, ">= 7.12.0, < 7.18.2", first_patched 7.18.2 }
  { react-router, ">= 8.0.0, < 8.3.0",  first_patched 8.3.0  }
```

**08-07 にレンジが分割されていた。** 08-04 の記述は執筆時点では正しく、**外部の更新で無効になった**。
EX-9（例外の当てはまらない理由は執筆時点で実測する）を**満たしていても防げない型**なので、
`_work/reports/2026-08-14-react-router-advisory-range-shift.md` に起こした（hub 収載 GO 済み・**08-15 に撃つ**）。

射程も測っておいた〔fleet 実測・7艦とも `origin/main` と SHA 一致・未コミット0を確認済み〕:
**7艦とも caret 範囲内で 7.18.2 に到達**し、`react-router-dom@7.18.2` は `react-router: "7.18.2"` の**完全一致ピン**。

### 4. 08-14 付けの指示書が届いた（玉番号が衝突していた）

hub（NeNe担当）が 08-13 深夜に起草した `_work/handoff-fleet-tooling-2026-08-14-sec-work-order.md` が発令。
**08-09 order を読まずに独自の①〜⑤を振っていた**ため番号が衝突しており、指示書側が記号ラベル（【PHP-AUDIT】【NANOID】【BRACE】【ALLOWLIST】【RR-STALE】）へ改名された。**今日の射程は3件**。

### 5. 【BRACE】— 2リポ・lockfile only

advisory の floor を**自分で読み直してから**使った（`GHSA-rgw5-rvv9-x895`・published 08-03 / updated 08-03T20:17Z）。**指示書の floor と一致**。

| リポ | 変更前 | 変更後 | `npm audit` | PR |
| --- | --- | --- | --- | --- |
| NENE2（`frontend/`） | 1.1.16 / 2.1.2 / 5.0.7 | 1.1.18 / 2.1.4 / 5.0.9 | **5件 → 0件** | #1647（未マージ・CI 緑・🔴 **react-router も同乗**＝下記9） |
| nene2-js | 1.1.14 / 2.1.0 / 5.0.6 | 1.1.18 / 2.1.4 / 5.0.9 | 6件 → **3件** | #136（🔴 **draft 化**・`vite` 降格が混入＝下記9） |

両方とも `npm audit fix --package-lock-only`（`--force` なし）で到達し、**`package.json` は無変更**。
**NENE2 のルート lockfile に brace-expansion は不在**で、frontend だけが対象だった（リポに lockfile が1つと決め打ちしていたら外していた）。

### 6. 🔴 【PHP-AUDIT】— 射程を数え直して 14 → 8

指示書が「あたしの14艦も信じるな」と書いていたので、`.lock` の実在から数え直した。**そこで割れたのは艦数ではなく「実在」の測り方だった。**

| 段階 | 数 | 落ちたもの |
| --- | --- | --- |
| `composer.lock` を持つ git リポ全列挙 | 26 | — |
| workflow が1本以上 | 19 | `.log` 系7リポ（**足す先が無い＝判断ではなく不在**） |
| `composer audit` 未設置 | 15 | contact / invoice / records / suite |
| 🔴 **`origin/main` に lock が実在** | **10** | **concierge / field / profile / serve / records-commercial**（`.gitignore` で未追跡） |
| `composer audit --locked` が 0件（安全弁） | **8** | NeNe（smarty medium 2件）/ sakura FT（symfony/yaml low 3件） |

未追跡の5艦は **board 行38 の「composer.lock を .gitignore している6艦」そのもの**で、**玉5（@hide 判断待ち）の下流**だった。hub は追試して射程を 8 に是正し、指示書を書き換えた。

8リポへ 1リポ1PR。**他艦の作業ツリーには触らず**、GitHub API でブランチを作って1ファイルだけ載せた（各艦にリナが起きている可能性があるため）。**載せる前に8本とも YAML をパース**し、job 構造が壊れていないこと・`composer audit` ステップがちょうど1つであることを機械確認した。

置き方は8リポ共通で **`composer install` の直後・独立ステップ**（`composer check` には埋めない）。埋めると**ログ上で「監査が無い」と「監査したが何も出なかった」が区別できなくなる**ため、理由をコメントで残した。

### 7. 🔴 撃って初めて分かった — `nene-origin` の CI は課金で止まっている

`nene-origin` #646 だけ **10ジョブ全部が 2〜4秒で failure**。私の変更が原因ではなかった。

> `The job was not started because recent account payments have failed or your spending limit needs to be increased.`
> 〔fleet 実測・check-run annotation〕

- **origin は private**。public は Actions 無料枠・private は課金枠なので、**課金が止まると private だけが落ちる**。
  🔴 **ここで一度外した**: 初報で「**フリートで唯一の private**」と書いたが、あたしが実測したのは
  **【PHP-AUDIT】射程の8リポが全て public** までで、**そこからフリート全体へ一般化した部分は測っていなかった**。
  〔**hub 実測**〕**private は12リポ**あり、workflow を持つのは4つ（origin / hideyuki-mori-site / nene-devto /
  nene-records-commercial）。**08-12 に records-commercial の run が成功している**ので、
  **停止は 08-12〜08-13 の間**と時系列が引けた——**あたしの誤りを直した側から、より細かい事実が出てきた**。
- **最後の成功は 2026-08-10T01:14 の schedule 実行**。それ以降、今夜の PR まで **run が1本も無い**。
- 🔴 **`ci-watch.md` は origin を 🟢「稼働中（最新実行 3.5日前）」と判定していた。** 最後の成功の鮮度しか見ていないので、**課金停止を検出できない**。「装置を見張る装置」の穴。
- **次の週次は 08-17（月）。このままなら失敗する。** 課金は施主 seam なので hub 経由で上げた。

### 8. 【NANOID】— 閉じている。ただし別の残が出た

`package-lock.json` を追跡する全リポの `origin/main` を実測。**nanoid は全リポで floor（3.3.17）充足**（corpus のみ 3.3.17 丁度、他は 3.3.18）。

棚卸しで**別の残**が出た〔fleet 実測〕:

| リポ | 現状 | lockfile-only の結果 |
| --- | --- | --- |
| `nene-corpus-site` | high 5 / low 1 | 🔴 **「6件→0件」は嘘だった**（下記9①）。**撃っていない** |
| `nene2-python` / `nene-mcp` / `NENE2`(root) | high 2 / mod 2 | high 2 → **1**（postcss が閉じる） |
| `nene2-dev` | high 2 / low 1 | **変化なし**（`astro@7.2.1` = major が要る） |

🔴 **残りの形が同じだった**: python / mcp / NENE2(root) / nene2-js の**4リポとも `vitepress` → `vite`(high) → `esbuild` の同一の鎖**で、**`vitepress` の major 更新という判断が1回で4リポぶん片づく**。個別に上げると同じ判断が4回発生する。

また **`nene2-dev` は astro high が再発**していた。board は「PR#1 で完了」としているが、当時の bump は 6.3.8→6.4.8 で、今回の advisory は major を要求する。指示書の「『あの艦は前に直した』を閉じた根拠にしない」がそのまま当たった。

## 📊 本日の数字

| 項目 | 値 | 出所 |
| --- | --- | --- |
| 作成した PR | **10本**（マージ **0本**・うち **1本を draft へ差し戻し**） | fleet 実測 |
| うち CI 緑・`CLEAN` | **9本** | fleet 実測 |
| CI が動かない PR | **1本**（origin #646・課金） | fleet 実測 |
| 作成した Issue | **12本**（他艦10・自艦2） | fleet 実測 |
| 【PHP-AUDIT】射程 | board 9艦 → hub 14艦 → **実測 8リポ** | fleet 実測 |
| 閉じた advisory | NENE2 frontend **5件→0件** / nene2-js **6件→3件** | fleet 実測 |
| フリートの nanoid | **全リポ floor 充足**（3.3.17 以上） | fleet 実測 |
| react-router allowlist | **7艦・1種類**（08-09 から不変） | fleet 実測 |
| hub の射程の誤り | あたしが **2回**止めた（玉1 完了状態・PHP-AUDIT 5艦） | fleet 実測 |
| **あたしの誤り** | **3回**（private への過剰一般化・corpus-site の偽 0件・PR2本への混入） | fleet 実測 |
| 今夜フリート全体の射程誤り | **5回**（vault 2 / deal 1 / fleet 2）・事故 **0** | 〔**hub 申告**・あたしは自分の2回しか実測していない〕 |

### 9. 🔴 撃った後に自分の生成物を検査したら、3件が壊れていた

hub から corpus-site を撃つ GO をもらった直後、**PR を作る前に生成された lockfile を前後比較した**。そこで3件出た。

**① `nene-corpus-site` の「6件→0件」は嘘だった。**
同リポは **npm workspaces のモノレポ**（`workspaces: ["apps/*", "packages/*"]`）で、スクラッチに置いたのは
ルートの `package.json` と `package-lock.json` の2ファイルだけだった。**メンバの `package.json` が無いので
npm は依存ツリーを空と解決し、lockfile を書き潰した**〔実測: **406 → 4 パッケージ** / 213,146 → 1,016 バイト〕。

🔴 **「0 vulnerabilities」は直った結果ではなく、検査対象が消えた結果だった。**
しかも **`npm audit` は正常終了して 0 を返す**ので、エラーにならない。**jsonc の件は「エラーで出たから気づけた」が、こちらは何も言わない。**
**GO は出ていたが撃っていない。**

**② NENE2 #1647 は「brace-expansion のみ」ではなかった。**
版が動いたのは **7件**で、**`react-router 7.18.1 → 7.18.2` を含んでいた**〔実測〕。
これは **08-15 のレーン（【RR-STALE】）と同じ版更新**である（NENE2 は7艦の allowlist 群には入らないので玉3 そのものではないが、
**PR 本文が「brace-expansion のみ」と称していた**のは事実）。パッケージ数は **761 → 761 で健全**・全て前方向。
→ **本文とタイトルを全数開示に書き換えた。**

**③ nene2-js #136 は `vite` を降格させていた。**
〔実測〕版変更 **47件**・lockfile から **24パッケージが消失**（`rolldown` と platform binding 群＝`vite@8` の依存）。
**`vite` 8.0.14 → 6.4.3（メジャー降格）／`esbuild` 0.28.0 → 0.25.12**。
原因は、`vite`/`esbuild` が `package.json` に宣言を持たず **`vitepress@^1.6.4` 経由の推移的依存**であること。
`npm audit` は `vite` の advisory（範囲 `<= 6.4.2`）を報告するので、**`npm audit fix` は範囲を外れる最も近い版として 6.4.3 を選び、実際に使われていた 8 系から落とした**。

🔴 **CI 緑は反証にならなかった。** nene2-js の PR CI は `npm ci` → `npm run check` → `npm publish --dry-run` で、
**VitePress のビルド（`npm run docs:build`）は `docs.yml`（Pages デプロイ）側にあり PR では走らない**。
→ **全数開示に書き換え、draft へ落とした。作り直しを提案。**

**【PHP-AUDIT】の8本は影響を受けない**（workflow に1ステップ足すだけで lockfile を触っていない）。
棚卸しの残り4リポもパッケージ数が前後で不変（174→174 ×3 / 371→371）＝数字は生きている。

## 🔴 自分が踏んだもの

**allowlist を最初に測ったとき、jsonc を素朴な正規表現でコメント除去して JSON パースし、全15艦で `PARSE_ERR` を出した。**
GHSA id を直接抽出する形に変えて取り直したが、**気づけたのは「エラー」で出たからにすぎない**。
同じ雑さで**空配列を静かに返す実装**だったら、「**フリートの allowlist は 0件**」という嘘の結論を出して、
玉3 そのものを「もう終わっている」と報告していたはずである。

**不在の主張は、道具が黙って空を返す形にしてはいけない。**（「0件は検出器が死んでいても出る」の、パーサ版）

## 明日以降

- **08-15**: 【ALLOWLIST】＝【RR-STALE】＝玉3。7艦へ **1PR で lockfile bump ＋ allowlist 撤去**。材料は `_work/reports/2026-08-14-react-router-advisory-range-shift.md` に揃っている。
- **08-15（金）は L3 作業デー**。玉4（stylelint registries・射程は 10艦のまま）はそこ。
- **08-16〜20**: 玉1 の (b) 正本の `docs/standards-patch/` 収載 ＋ (c) 第3波9艦の手順書。
- **hub 手番**: origin の課金（→ 施主）／`vitepress` 鎖4リポの束ね方／corpus-site を撃つ可否／board の3行重複（行94・95・122）の統合。
