# standards patch — nene-records 由来の規約還流（5件: CSP 4件＋認可 GET/HEAD）

- **Issue**: #170 ／ **出典**: `_work/handoff-records-fleet-conventions-draft-2026-07-29.md`（本体・CSP 4件・hub 査読 PASS）＋同 `-addendum.md`（別便・認可⑤）。いずれも Records リナ 起草。
- **経緯**: board `(A) 2026-07-29 hub手番: fleet規約還流の引き渡し`（due 2026-07-31）。引き渡し = 統合リナ 2026-07-29 深夜便（申し送り3点つき）。これで還流バッチ（mori-site #154 / contact #155 / invoice #162 / records 本件）の全レーンが fleet に着地。
- **対となる patch**: `2026-07-29-mori-site-conventions.md`（#154 / PR #157）・`2026-07-29-contact-conventions.md`（#155 / PR #158）。論点が違うため PR を分けている（同運用）。
- **分担**: 起草側（records）が「実戦で踏んだ具体を落とさないこと」に責任を持ち、**条文としての整形・章番号の割り当てが本リポの責任**。
- **この文書の地位**: 規約文書への収載は **W0b の作業**（新条文の追加は「批准前提(c) 緑 → 契約v1凍結成立」後の最初の patch 波まで凍結 — 統合リナ裁定 2026-07-29）。本ファイルは収載先の確定と条文の文面凍結までを担い、**条文の正本**となる。
- **帰属**: 本文書の実測値（違反56件・issue/PR/commit 番号・横断 grep の3箇所等）はすべて **records リナまたは統合リナの測定**であり、fleet では再実測していない。

---

## 0. 収載先マトリクス（結論）

| | 条文 | 収載先（主） | 収載先（副・要相互参照） | 機械強制 |
|---|---|---|---|---|
| ① | CSP は「必要な指令を・必要なページだけ」広げる／nonce は使う場面でだけ生成 | **🔴 セキュリティ章が無い**（§6 参照）・暫定 **`02 §9.6`（応答セキュリティヘッダ・新設）** | **`05 §6` の「機能追加時のチェックリスト」**（contact ②' が新設した枠。届け先＝機能を足す人 — mori-site ⑥の適用） | 🟡 ②の pin テストが間接強制 |
| ② CSP 出力の byte-for-byte pin | ① と同じ `02 §9.6` | `05 §5`（CI ゲート一覧） | 🟢 可（**既定ポリシー定数の台帳化を優先** — hub 申し送り(b)。§7 参照） |
| ③ CSP は本番でしか壊れない | ① と同じ `02 §9.6` | **🔴 本来は横断テスト章**（#154/#155 と同じ穴・3レーン目）— 一般則化は `06-testing-verification.md`（方向承認済み・凍結明け）へ送る（hub 申し送り(c)・fleet 判断＝**送る**） | ❌ 検証規律（記録様式のみ様式化可） |
| ④ `srcdoc` は親 CSP を継承・sandbox は CSP の代替でない | ① と同じ `02 §9.6`（CSP 継承の意味論なので同居） | — | 🟡 静的検出可（`srcDoc` × `sandbox="allow-scripts"` の共起） |
| ⑤ 読み取り認可は GET と HEAD を列挙で書く | 暫定 **`02 §3`（認証トークン・認証状態）に「§3.x 認可解決」追補** | `05 §6`（resolver 雛形を配るなら scaffold 側にも） | 🟢 静的判定可（検査器は凍結明け・§7 参照） |

**凡例** — 🟢 検査器で強制しうる／🟡 一部のみ機械判定できる／❌ 人間の検証・レビュー規律。

🔴 **①〜④の主たる収載先が「暫定」なのは、規約 v1 にセキュリティ横断章が無いため**（CSP への言及は現行 01〜05 に 0 件 — 起草時 grep 実測）。#154/#155 の「テスト章が無い」と**同型の第2の穴**。W0b の収載時に「06 テスト章」と併せて「07 セキュリティ章」の要否を判断する（§6）。

---

## 1. ① CSP は「必要な指令を・必要なページだけ」広げる。nonce は使う場面でだけ生成する

### 条文（`02 §9.6` 新設・冒頭条文）

> CSP を緩めるときは、**緩める指令を最小に、緩める応答面も最小に**する（MUST）。
> 機能Aのために `script-src` が要るなら `script-src` だけを足し、無関係な指令（`img-src`・`connect-src` 等）は触らない。HTML 応答が困っていても API 応答の CSP は巻き込まない。
>
> **nonce は「その応答が実際に nonce 付き `<script>` を出すとき」にだけ生成**し（MUST）、出さない応答の CSP は従来と**バイト単位で同一**に保つ（→ ②が機械で固定する）。
>
> 「とりあえず全ページで nonce を出す」「まとめて `'unsafe-inline'` にする」は MUST NOT。前者は nonce の意味（この応答が意図した1本だけを許可する）を薄め、後者は**その指令に対する CSP の防御を丸ごと捨てる**。

### 根拠（records 実測の要点）

- 出発点（records #557 / PR #558）: `default-src 'self'` が公開SPAの inline `<style>`・`data:` フォントをブロック＝**本番実機で56件の違反**として発現（dev では露見しない — ③の現物）。ここで `style-src`/`font-src`/`img-src` だけを緩め **`script-src` は `'self'` のまま**・公開HTML応答にだけ付与。
- 分岐点（records #982 / PR #995）: 分析と無関係な極小 inline script の需要が発生。「分析も一緒に有効化」「`'unsafe-inline'` を足す」のどちらにも倒れず、**nonce の生成条件を「実際に script を描く応答」に絞り、組み立て側は「nonce があれば script-src にだけ付ける」**へ拡張。**対象外ページの CSP は1バイトも変わらない。**
- 同型の再演（records #802 / PR #803）: embed allowlist は `script-src`/`connect-src`/`frame-src` の3指令にだけ追加・allowlist 空なら出力は従来と byte-for-byte 同一（→②）。

### 実装の型（写せる形・records `PublicHtmlCsp` 由来）

```php
// 生成側: この応答が実際に nonce 付き <script> を出すときだけ生成する
$nonce = ($analytics->isEnabled() || $fabNeedsScript) ? bin2hex(random_bytes(16)) : '';

// 組み立て側: 無効なら既定定数をそのまま返す（= byte-for-byte 同一）
if (!$analytics->isEnabled() && !$hasNonce) {
    return self::POLICY;
}
```

### 届け先（起草側の明示要求 — mori-site ⑥の適用）

①を「CSP を書く章」にだけ置くと **CSP を書く人しか読まない**。実際に判断を誤るのは**機能を足す人**（分析・CTA・埋め込みの実装者）。よって `05 §6` の「機能追加時のチェックリスト」（contact ②' 枠）に**「外部/インラインスクリプトを足すときは ① を読む」の1行を必ず併載**する（W0b 収載時の指定・相互参照 MUST）。

---

## 2. ② CSP 出力を変える変更は「機能無効時の出力 byte-for-byte 同一」を回帰で pin する

### 条文（`02 §9.6`）

> CSP の組み立てに分岐を足す変更（新指令・新オリジン・nonce 条件の変更）では、**その機能を使っていない場合の出力が変更前と完全に一致する**ことをテストで固定する（MUST）。「だいたい同じ」「必要な指令は入っている」では不足で、**文字列として同一**（`assertSame` 相当）を assert する。
>
> 理由: CSP は**壊れても機能が動いてしまう**。緩めすぎの間違いは症状が出ず、**攻撃面だけが静かに広がる**。テストが落ちる方向に倒しておかない限り、最後の砦が CSP 文字列の目視 diff になる（指令が5つを超えたあたりで人間の精度は落ちる — records 起草者の実感値）。
>
> 厳格側の性質も pin してよい（SHOULD）: 例「`style-src` より前に `'unsafe-inline'` が現れない」＝「必要な指令にだけ付いている」を位置関係で機械化した形（records `PublicHtmlCspTest.php:70`）。

### 根拠

records #802 / #982 はどちらも**全公開ページが通る関数**への分岐追加で、「空 allowlist なら byte-for-byte 同一」を PR 受入条件に明記（commit `d4eaa33`）し `assertSame(PublicHtmlCsp::POLICY, $csp)` で固定（`PublicHtmlCspTest.php:18`）。

### 検査器の方向（hub 申し送り(b) の裁定を反映）

起草側の提案は2案（「全フラグ OFF == 既定定数」テストの存在検査／既定ポリシー定数の台帳化）。**採るのは後者=台帳化を優先**（hub 申し送り(b): テスト存在の静的判定は困難）。各製品の `PublicHtmlCsp::POLICY` 相当を registries に台帳化すれば、フリート横断で「どの製品がどこまで緩めているか」が一覧になり、差分＝レビュー対象になる。**registries への kind 追加は新検査器に接続するため凍結明けの patch 波**（§7）。

---

## 3. ③ CSP は本番でしか壊れない。dev の緑を「動いた」の根拠にしない

### 条文（`02 §9.6`）

> CSP に関わる変更の検証は、**本番と同じ CSP ヘッダの下で、実ブラウザ**で行う（MUST）。dev サーバは CSP が無い/緩いことが多く、**dev で動くことは何の証拠にもならない**。ユニットテストが証明できるのは「意図した文字列を出した」ことまでで、**その文字列の下でスクリプトが実際に走るか**は別問題。
>
> 検証は「コンソールに CSP 違反が出ないこと」の確認までを含める（MUST）。CSP 違反は**例外を投げず**、機能が静かに無効化されるだけなので、**症状を探しに行かないと見つからない**。

### 根拠

- records #557 の56件は**本番の単一オリジン実機検証で初めて**出た。それまでの開発とテストは全部緑。
- records #857（④）は**本番相当 CSP を再現したヘッドレス Chromium** で初めて `SCRIPT_BLOCKED` を捕捉。

### 一般則への昇格（hub 申し送り(c)・fleet 判断）

③は CSP 固有ではない。07-29 にフリートで確認された「ゲート実行の緑を判定材料にしない」（serve の brace-expansion）・「検査しない対象の green」（G-6・#164）と**同じ型** — 「**緑は『壊れていない証明』ではなく『そのコードパスを通っていないだけ』かもしれない**」。fleet 判断: **一般則としては `06-testing-verification.md`（方向承認済み・凍結明け）の柱の1本に送り**、本条は CSP 文脈の適用形として `02 §9.6` に置く（一般則と適用形の両置き・相互参照）。⑤の締め文「**叩かれていないメソッドは、動いていないのではなく、確かめていないだけ**」も同じ柱に併載する。

---

## 4. ④ `srcdoc` の iframe は親ページの CSP を継承する。sandbox は CSP の代わりにならない

### 条文（`02 §9.6`）

> `<iframe sandbox="allow-scripts" srcdoc="...">` に任意の HTML/JS を流し込む設計を、「隔離したので安全に実行できる」根拠で採ってはならない（MUST NOT）。`srcdoc` の iframe には**親ページの CSP がそのまま適用される**（ブラウザ仕様）。`sandbox` は DOM/オリジンの隔離であって、**CSP の適用範囲を切り替えるものではない**。この設計は**安全になるのではなく、動かなくなる**。
>
> 任意 JS を伴うコンテンツを配信するなら、**別オリジンに実 URL で置き `src=` で読む**（SHOULD）。CSP はオリジン単位で当てられるので、そこで初めて「そのコンテンツ用の CSP」を設計できる。**ただし別オリジン実 URL 配信は配信基盤（オリジン・デプロイ経路・キャッシュ）を1式持つコストを伴う**（records #857 の LP 配信モデル参照 — hub 申し送り(a)）。コストを払えない場合の答えは「では srcdoc で」**ではなく**「任意 JS 実行という要件自体を落とす」。

### 根拠

records #311 の `SandboxedBundle.tsx` は "scripts execute but cannot reach the parent DOM" とコメントしていたが、**実際には scripts execute しない**（`script-src` が `default-src 'self'` から継承され inline script は実行拒否）。**設計前提そのものが偽**。現在は単発パッチで潰さず別オリジン配信モデル（#857）へ統合して解く方針 — **間違った前提のまま小さく直さない**判断も教訓として併記する。「ユーザ提供 HTML を安全に表示する」要求はどの製品にも出るし、`srcdoc`+`sandbox` は**一番手軽に見えるが要件を満たさない**ので、条文で先回りして潰す価値がある。

---

## 5. ⑤ 読み取りに能力を要求するときは、GET と HEAD を必ず同じ扱いにする

### 条文（暫定 `02 §3` 追補「認可解決」）

> 認可（どの操作にどの権限が要るか）をパスとメソッドから解決するとき、**読み取りメソッドを単一の等値比較で書かない**（MUST NOT）。`$method === 'GET'` は **HEAD を認可段から抜け落とし**、同じリソースへの認可がメソッドで効いたり効かなかったりする状態を作る。
>
> 読み取りは**列挙で書く**（MUST）: `match ($method) { 'GET', 'HEAD' => Capability::…, … }`、または読み取り述語を1つ定義して全分岐がそれを使う。
>
> HEAD は本文を返さないので「漏れない」と考えたくなるが、**ステータスコードは返る**。`404` と `200` の差はそのまま**リソースの存在オラクル**であり、`Content-Length` が付く構成なら**規模の推定**まで通る。

### 根拠

- records #1023: `CapabilityResolver::resolve()` の1分岐だけが `$method === 'GET'` で、認証済み低権限ユーザの HEAD が認可段を素通り（`null` = 能力不要と解釈）。**全分岐を GET/HEAD で突き合わせた実測で、「列挙で書けているか」がそのまま健全性の判別条件だった**（列挙4分岐=全部無事・単一等値1分岐=素通り）。変更系（`isMutationMethod()`）は全部健全＝壊れるのは「読みに権限を要求する」側だけ。
- レビューで見つけにくい理由: `$method === 'GET'` は読んで違和感がなく、**正しい分岐のすぐ隣**でも起きる。HEAD を叩くテストを書く習慣も無い。
- 横断（統合リナ grep・2026-07-29）: concierge 2箇所（`CapabilityResolver.php:34,39`）・payout 1箇所（同 `:58`）に**同型の書き方**がある。🔴 **横断ぶんは grep 一致のみで周辺ロジック未読** — 現時点で言えるのは「同じ書き方が3箇所ある」までであり、**バグと断定しない**（hub 申し送り・各艦の実測待ち）。

### 検査器の方向

起草側の2案のうち、fleet の見立ては**案2（代表パス × {GET, HEAD} のテーブル駆動テストを各製品に配る）優先**。案1（`=== 'GET'` の静的検出）は誤検知抑制の理由コメント規約まで含めると重く、案2は records で実際に3パスの不一致を即座に出した実績がある。いずれも**新検査器 = 凍結明け**（§7）。

---

## 6. 🔴 規約 v1 の構造的な穴（3レーン目の独立要求）— W0b への申し送り

- **テスト章の不在**: mori-site（#154）・contact（#155）に続き、records ③も横断テスト章を要求する3レーン目。`06-testing-verification.md` は方向承認済み・**凍結明け**。
- **セキュリティ章の不在（新規・本 patch で顕在化)**: CSP への言及は現行規約に 0 件。①〜④は `02 §9.6` へ暫定収載するが、docs/security 系（フロントリファクタ計画 C トラック）と併せ、**「07 セキュリティ章」の要否を W0b 収載時に判断**する。判断まで本ファイルが条文の正本。

## 7. 検査器・台帳の追加はすべて凍結明けへ（列挙）

②の既定ポリシー定数台帳化（registries kind 追加）・④の `srcDoc`×`allow-scripts` 共起検出・⑤のテーブル駆動テスト配布は、いずれも**新検査器/新台帳＝「批准前提(c) 緑 → 契約v1凍結成立」後の最初の patch 波**（統合リナ裁定 2026-07-29・#159 台帳系と同じ整理）。本 patch では**条文の文面凍結まで**を行い、検査器 issue の起票は凍結明けに行う。

## 8. 規範の強さを原文から変えた箇所（起草艦へ確認する列挙 — #154/#155 の作法）

| # | 原文 | 本 patch | 変更 |
|---|---|---|---|
| 1 | ①「〜はしない」（平叙） | MUST NOT | 強度の明示化 |
| 2 | ②「テストで固定する」（平叙） | MUST | 強度の明示化 |
| 3 | ③「実ブラウザで行う」「〜までを含める」（平叙） | MUST ×2 | 強度の明示化 |
| 4 | ④「成立しない」（事実記述） | 「この根拠で採ってはならない」MUST NOT | **事実→規範への転換**（例外の余地なし: ブラウザ仕様由来のため mori-site ①型の例外条項は不要と判断） |
| 5 | ④「別オリジンに実 URL で置き `src=` で読む」（平叙） | SHOULD＋コスト注記＋「コストを払えないなら要件を落とす」 | **SHOULD 化＋fleet 追記**（hub 申し送り(a) の反映。最後の一文は fleet の追加 — 要確認） |
| 6 | ⑤「単一の等値比較で書かない」「列挙で書く」（平叙） | MUST NOT ／ MUST | 強度の明示化 |

## 9. 誠実性

- 本文書の実測値・issue/PR/commit 番号はすべて records リナ（#557/#558/#583/#802/#803/#857/#982/#995/#311/#1021/#1023/`d4eaa33`）または統合リナ（横断 grep 3箇所）の測定。**fleet では再実測していない**。
- fleet が自ら測ったのは「現行規約 01〜05 に CSP 言及 0 件」（grep）のみ。
- §8 の列挙は起草艦（records）へ relay で確認を取る（本 PR のレビュー期間内・回答が変更を要求したら反映してからマージ）。
