# standards patch — nene-serve 由来の規約還流（3条）＋「導入 ≠ 実行」ガード族の設立

- **Issue**: #161 ／ **出典**: `_work/handoff-serve-fleet-conventions-draft-2026-07-29.md`（serve リナ 起草・hub 採用済み）。参照実装 = serve PR #197（TC-x）／PR #196（deal #175 の写し）／PR #198（DA-x）／serve #194（DR-x の着地先）。
- **経緯**: 還流バッチの1レーン。#161 本文には「**凍結明けまで着手しない**」とあったが、**2026-07-30 の施主裁定で契約v1凍結が成立**したため着手条件が外れた。
- **hub 裁定（2026-07-31）**: serve からの明示の問い「W0b に同乗収載するか、『導入 ≠ 実行』ガード族として括るか」に対し、**族として括る**で確定。族名は仮に「**導入 ≠ 実行」ガード族**、ID 体系は **fleet 案 → hub レビュー**の順。本ファイル §1 がその案。
- **対となる patch**: `2026-07-29-mori-site-conventions.md`（#154）・`-contact-conventions.md`（#155）・`-records-conventions.md`（#170）・`-nene2-conventions.md`（#160 / PR #207）。
- **分担**: 起草側（serve）が「実戦で踏んだ具体を落とさないこと」に責任を持ち、**条文としての整形・章番号・ID の割り当てが本リポの責任**。
- **この文書の地位**: 規約文書への収載は **W0b の作業**。本ファイルは収載先の確定と条文の文面凍結までを担い、**条文の正本**となる。
- **帰属**: §2〜§4 の実測（serve の宙吊り参照4本・9箇所・11日埋没・4経路中3経路 throw 等）は**すべて serve リナの測定**であり fleet では再実測していない。**§1（族の構成・ID 体系）と §5（メンバー台帳の検証）は fleet が本日実測した**（§6 で区別）。

---

## 0. 収載先マトリクス（結論）

| | 条文 | 収載先（主） | 収載先（副・要相互参照） | 機械強制 |
|---|---|---|---|---|
| **族** | 「導入 ≠ 実行」ガード族の設立（§1） | **`05` に新節（`第0章` G-6/G-7 の直後を推す）** | 各条文の収載先から族へ逆参照 | ❌ 族そのものは規律 |
| ① TC-x | トークン参照の解決性 | **`03-styling-theming.md`**（テーマ契約の章） | `05 §5`（CI ゲート一覧）・族 | 🟢 可（参照実装あり・serve PR #197） |
| ② DR-x | 未着地の出口（記録は出口ではない） | **新章 `06-testing-verification.md`**（方向承認済み）／暫定 `05 §5` | 族・#206（検証規律） | ❌ 人の運用（様式のみ様式化可） |
| ③ DA-x | 時限つき例外の解除条件は「壊れているもの」で書く | **`05 §5.6`**（#155 ③ が既に据えた節に隣接） | 族・#155 ③' | 🟡 一部（解除条件の**存在**は機械判定可・**正しさ**は不可） |

**凡例** — 🟢 検査器で強制しうる／🟡 一部のみ機械判定できる／❌ 人間の検証・レビュー規律。

---

## 1. 族の設立 — 「導入 ≠ 実行」ガード族

### 1.1 族の定義（条文）

> **「導入したこと」を検査するゲートは、「効いていること」を検査していない**（原則）。
> 導入を検査するゲートを置いたら、**実行時に効いていることを検査する別のゲート**を対で置く（MUST）。
> 前者の green は後者の証拠にならない。

G-6（検査器は fail-closed・検査不能は unknown）が**検査器の側**から言っていることを、**被検査対象の側**から言い直したもの。G-6 は「検査が走らなかったのに green を出すな」、本族は「**検査は走ったが、それは導入の検査であって実行の検査ではない**」。

### 1.2 なぜ族として立てるか（起草側の主張＋fleet の追認）

serve の主張をそのまま採る:

> 規約としては「導入の検査」と「実行の検査」を別立てで要求するのが正だと考える。

fleet の追認理由: **族として立てないと、同じ主張が章をまたいで別名で散る**。実測すると、同じ型の実例が**すでに5件、4つの章にまたがって**存在している（§5 の台帳）。1件ずつ各章に収載すると、6件目を踏んだ人が「これは既知の型だ」と気づく手段が無くなる。

### 1.3 🔴 ID 体系（fleet 案・hub レビュー待ち）

**新プレフィクス `EX-x`（EXecution）を立てる**ことを推す。

**根拠 — 既存名前空間の実測（2026-07-31・fleet）**:

| プレフィクス | 意味 | 出典 | 空きか |
|---|---|---|---|
| `AM-1〜` (678箇所) | 会議決定（Agreed Minutes） | 議事録・全章 | ❌ |
| `G-1〜G-7` | **第0章 メタ規約（強制の原理）** | `05 第0章` | ❌ |
| `D-1〜D-6` | **依存・配布の基準線** | `05 §1.1` | ❌ |
| `ST-x` / `U-x` / `REG-x` / `C-x` | 各章の条文 | 02 / 01 / registries 等 | ❌ |
| **`EX-x`** | — | — | ✅ **未使用** |

- **G-x への追加（G-8, G-9…）は採らない。** G-x は「規約文書自身の強制の原理」（G-1 タグなき MUST は存在しない・G-4 規約文書自身が CI を持つ）で、**規約文書のメタ規約**である。本族は**製品リポ側のガードの設計則**なので層が違う。
- **`D-x` の続番は採らない。** D-x は依存・配布の基準線で意味が違ううえ、🔴 **すでに衝突が発生している**（#208）: 還流レーンが「D-2（生成物ドリフト）」「D3（daily-lint）」「D4（fake timers）」を採番しているが、`05` の D-2/D-3/D-4 は全く別の条文である。**同じ轍を踏まない。**
- **serve の仮 ID（TC-x / DR-x / DA-x）は既存と衝突していない**ので、そのまま個別プレフィクスとして残す案も成立する。ただし族の成長（現時点で5件・今後も増える見込み）を考えると、**族が1系列を持つほうが「6件目」を置く場所が自明**になる。

**案（fleet 推し）**: 族 = `EX-x` の1系列。起草側の仮 ID は**別名として台帳に残す**（トレーサビリティのため）。

| 族 ID | 条文 | 起草側の仮 ID | 由来 |
|---|---|---|---|
| **EX-1** | トークン参照の解決性 | TC-x | serve #156 / PR #197 |
| **EX-2** | 未着地の出口（記録は出口ではない） | DR-x | serve #194 |
| **EX-3** | 時限つき例外の解除条件は「壊れているもの」で書く | DA-x | serve PR #198 |
| **EX-4** | 依存 override は API 互換を実測する（恒久ガードテスト） | — | deal PR #175 |
| **EX-5** | 生成物コミット型リポはクリーンビルド後の差分ゼロを検査する | — | #165（射程実測が先） |

**未確定**: `EX-` という綴りそのものは仮。hub レビューで別綴りに変えても構造は変わらない。

---

## 2. ① TC-x（→ EX-1）トークン参照の解決性

### 条文（`03-styling-theming.md` に収載）

> 契約テーマファイルに宣言された CSS カスタムプロパティと、プロダクトソース内の
> **fallback を持たない** `var(--x)` 参照を突合し、未宣言トークンを指す参照が
> 1件でもあれば CI を落とす検査を各リポに置く（MUST）。
>
> - 検査対象: テーマ CSS ＋ `src/` 配下の `.css` / `.ts` / `.tsx`（テストは除外）
> - **fallback 付き参照は対象外**（`var(--pct, 0%)` のように実行時にインライン style や埋め込み親が値を供給する設計。**fallback がその契約**であって欠落ではない）
> - 失敗時は `file → var(--token)` の形で**どこの何が**外れたかを出す
> - 「宣言済みトークンが 0 件」でテストが空振り成功しないよう、**パーサ自体のガード**を持つ（G-6 の自己適用）

### 根拠（serve 実測・fleet では再実測していない）

CSS の `var(--x)` は `--x` が未宣言でも**エラーにならない**。宣言ごと破棄され、要素は継承値で描画される。**型検査・lint・テスト・Storybook ビルド、どれも通る。**

| 参照 | 実際に起きていたこと | 開いていた期間 |
|---|---|---|
| `var(--danger)` ×2（Input/Select） | エラー時の枠が**赤くならない** | W1 以前から |
| `var(--fg)` ×2（ブランドマーク） | 下段カード2枚が**塗られない** | 同上 |
| `.danger` クラス（未定義・9箇所） | **コンソールの全エラーメッセージが本文色**で描画 | 同上 |

`--danger` / `--fg` は serve のトークン語彙に**一度も存在したことがない**。issue（serve #156）として起票はされたが **15日 open のまま**だった。**装置（人の目）は動いていたが、機械検査が無かったので優先度が上がらなかった。**

### 実装の型（写せる形）

`nene-serve/frontend/src/shared/ui/theme/serve-css-contract.test.ts`（serve PR #197）

```
describe('serve.css token contract', () => {
  it('declares tokens at all (guards the parser itself)', …)   // 空振り防止
  it('has no fallback-less var() reference to an undeclared token', …)
})
```

serve 実測: 宣言 58 / 使用 58 / 宙吊り **0**。負テスト実証: `var(--fg)` を戻すと `shared/ui/icons.tsx → var(--fg)` を挙げて fail。

🔴 **移植時の注意（起草側の明示）**: **テーマファイルが複数枚ある艦は「宣言側」の集合をその全部から取る必要がある**（serve は `shared/ui/theme/serve.css` 1枚）。`themes/default.css` 等を持つ艦がそのまま写すと、**別テーマで宣言されたトークンを「未宣言」と誤判定する**。

---

## 3. ② DR-x（→ EX-2）未着地の出口 — 記録は出口ではない

### 条文（新章 `06-testing-verification.md`・暫定 `05 §5`）

> 日報に「判断待ち」「留置」「未検証」「要相談」として書いた項目は、同じセッション内で
> **issue**（単一リポで完結・判断や調査が要る）または **board**（複数リポ横断・期日がある）
> のいずれかに落とす（MUST）。**日報への記載のみで閉じてはならない。**
>
> - 落とし先を書けない項目が残った場合、**そのこと自体を日報の末尾に明示**する
> - 判定基準は「**次にこれを見る人は、どの一覧を見て気づくか**」。その一覧が「その日の日報」しかないなら、出口が無い

### 根拠（serve 実測・fleet では再実測していない）

serve の 2026-07-18 日報は、孤児ブランド WIP（ブランド刷新9ファイル）を**きちんと記録していた**。索引にも1行あり、hub 裁定で「意匠領域なので留置」まで決まっていた。

それでも **11日後に `git ls-remote` と main の ref 差分で再発見する羽目になった**。PR も issue も無いので PR/Issue ビューに出ず、日報は日が変われば読み返されない。**「留置」という決定は記録されたが、留置されたものを次に誰が見るかがどこにも書かれていなかった。**

2026-07-29 に issue（serve #194）へ落として初めて、open issue 一覧＝**セッション開始時に必ず見る場所**に現れた。

### なぜ族の一員か（起草側の対比表をそのまま採る）

| | 機械の側 | 人の側 |
|---|---|---|
| 動いていた装置 | ゲート（lint / check / CI） | 記録（日報） |
| 検査されていたこと | 「入れた」 | 「見つけた」 |
| 検査されていなかったこと | 「効いている」 | **「次に誰かが気づく」** |
| 結果 | 3/4 経路が壊れたまま緑 | **11日埋没** |

ガードテストが「導入 ≠ 実行」を埋めるのと同じ位置に、この1条が「**検出 ≠ 着地**」を埋める。

🔴 **fleet の補記**: これは repo-status スキル §G（齟齬の出口）の**日報版**である、と起草側が書いている。§G は 2026-07-29 に「検出する仕組みは動いていた。欠けていたのは出口だけだった」という同型の事故から追加されたもので、**独立に同じ結論へ到達している**（fleet 本日確認）。

---

## 4. ③ DA-x（→ EX-3）時限つき例外の解除条件は「壊れているもの」で書く

### 条文（`05 §5.6`・#155 ③ の隣）

> allowlist / waiver / 暫定措置の解除条件は、**実際に壊れているもの**の名前で書く（MUST）。
> 「直すと期待されているアップグレード」の名前で書いてはならない（MUST NOT）。
>
> - ✗「eslint 10 波が来たら解除」
> - ○「eslint-plugin-import と eslint-plugin-jsx-a11y が minimatch@3 を降りたら解除」
> - **確認コマンドを併記する**（例: `npm ls minimatch --all`）

### 根拠（serve 実測・fleet では再実測していない）

serve 実測: eslint 本体は**すでに 10.8.0**（minimatch@10 を連れてくる）。それでも `GHSA-mh99-v99m-4gvg` は生きている — **minimatch@3 を留めているのはプラグイン2本のほう**だから。

「eslint 10 波」と書いていると、**波が来た時点で解除条件が満たされたと読める**が勧告はまだ生きている。姉妹製品（contact）が実際に eslint 10 でも壊れていたのが根拠（origin 裁定）。serve は PR #198 で文言差し替えを実施し、一般則を `docs/development/dependency-audit.md` に明文化した。

### なぜ族の一員か

時限つき例外は**「入れた」時点では正しい**。腐るのは解除の判定である。条文（時限＋解除条件を書け）だけでは今回のズレは防げず、**書き方の指定**まで降りて初めて機能する — これも「導入は検査されたが実行（この場合は解除の判定）は検査されていない」の一形態。

---

## 5. 族のメンバー台帳（2026-07-31 時点）

**fleet が本日、各件の実在を確認した**。確認方法と結果を分けて書く。

| # | 実例 | 「導入」として検査されていたもの | 「実行」で起きていたこと | fleet の確認 |
|---|---|---|---|---|
| 1 | **TC-x**（serve #156 / PR #197） | トークンを宣言した | 参照4本＋クラス9箇所が無効・15日 open | 🟡 起草側の実測（再実測せず） |
| 2 | **DR-x**（serve #194） | 未着地を日報に記録した | 11日埋没 | 🟡 起草側の実測（再実測せず） |
| 3 | **DA-x**（serve PR #198） | 時限例外に解除条件を書いた | 条件が「波」で書かれ、波が来ても勧告は生きていた | 🟡 起草側の実測（再実測せず） |
| 4 | **依存 override**（deal PR #175） | `overrides` で脆弱性を潰した | minimatch 3/4 経路が throw・**lint も codegen も check も exit 0** | 🟡 board 記載を参照（再実測せず） |
| 5 | **生成物ドリフト**（#165） | CI がビルドを走らせていた | 生成物が同期しておらず 2回ドリフト | 🟢 **fleet issue #165 として実在を確認** |
| 6 | **closing keyword**（#160 / PR #207） | PR が緑で merge された | Issue が閉じ残る／逆に観測前に閉じる | 🟢 **fleet が本日実踏**（PR #205・#207） |

### 🔴 hub から挙がったが**確認できなかった**メンバー候補（1件）

hub の 2026-07-31 の裁定便に、族の初期メンバー候補として次が挙がっていた:

> 今日 payout/deal で見つかった「CI が本番 build を走らせない」(fleet#178) も同族

**fleet では確認できなかった。** 実測:

```
$ gh issue view 178 --json title -q .title
(c) 素振りレーン: W1 対象リポ横断の red 集約 — origin 初報（…）
$ gh issue view 178 --json title,body,comments | grep -icE 'build|本番'
0
$ gh issue list --state all --limit 200 | grep -iE 'build'
28 init --scan が非標準 outDir(dist-e2e/)と .gitignore を無視して build 出力を走査する   ← 別件
（陽性対照: 同じ検索で 'conformance' は 2件ヒット＝検出器は生きている）
```

＝ **#178 は (c) 素振りの red 集約であり、build に関する記述は本文にもコメントにも無い。** 番号の取り違えか、payout/deal 側の issue 番号である可能性が高い（**推測**）。族の性質としては**同族に見える**ので、正しい出典が判明したら台帳に #7 として加える。**出典が確定するまで台帳に載せない。**

---

## 6. 別 issue へ送るもの（本 patch のスコープ外）

| 内容 | 送り先 |
|---|---|
| ID 名前空間の衝突（D-2/D3/D4 が3義）＋ contact patch の「`05 §7` に D4 として既存」の訂正 | **#208**（本 patch の §1.3 の動機・fleet 本日起票） |
| 生成物ドリフトゲートの**射程横断実測**（EX-5 の前提） | #165（「射程が決まる前に参照実装を配らない」は hub 承認済み） |
| TC-x 検査器の配布物化（複数テーマ艦への対応込み） | 未起票（凍結明けの検査器レーン） |
| 新章 `06-testing-verification.md` の新設 | 施主・統合リナ裁定済み（#154 §7・#155 §5・#160 と共同） |
| 「陰性対照を取らない N 回 green」 | #206（族とは別レイヤー＝検証手順の規律） |

---

## 7. 誠実性の注記

- **§2〜§4 の根拠（serve の宙吊り参照4本・クラス9箇所・15日 open・11日埋没・eslint 10.8.0 でも勧告が生きている・4経路中3経路 throw）は、すべて起草側（serve リナ）の実測であり fleet では再実測していない。** 私が自分で確かめた数字ではない。
- **§1.3（既存 ID 名前空間の分布・G-x/D-x の意味・`EX-` が未使用であること）と §5 の確認欄は fleet が本日実測した。** 実測コマンドは #208 に記載。
- **族の設立そのものは hub 裁定**（2026-07-31）。fleet の見立て（括る）と一致した。**族名「導入 ≠ 実行」ガード族は仮**で、hub レビューで変わりうる。
- **ID 体系 `EX-x` は fleet の案であり、hub レビュー未了。** 綴りも配置（`05` 第0章の直後）も未確定。
- 🔴 **hub が挙げた族メンバー候補1件（「CI が本番 build を走らせない」fleet#178）は確認できなかった**（§5 末尾）。番号の取り違えと見ているが**これは推測**で、断定していない。出典が確定するまで台帳に載せない。
- 起草側の原文から**規範の強さを変えた箇所**: 無し。TC-x / DR-x / DA-x の文面は起草側のものをほぼそのまま採り、**族への編入と ID の割り当てだけが fleet の追加**である。
- **起草側（serve リナ）への確認は未実施**（本 patch 作成時点）。族への編入と `EX-x` 採番は起草側の仮 ID を変える提案なので、**W0b 収載前に一度返すこと**。
